import { readFileSync, writeFileSync, mkdirSync } from "node:fs";
import { resolve, dirname } from "node:path";
const args=process.argv.slice(2);const option=(n,f)=>{const i=args.indexOf(n);return i>=0?args[i+1]:f;};const input=resolve(option("--input","docs/privacy/dpia.md"));const output=resolve(option("--output","artifacts/privacy/report.json"));const text=readFileSync(input,"utf8");const markers=["no-account","Data minimization","deleted within 30 days","deletion/correction","free-form public AI","Incident response","one business day","three business days"];const checks=markers.map(marker=>({marker,passed:text.toLowerCase().includes(marker.toLowerCase())}));const report={generatedAt:new Date().toISOString(),input,status:checks.every(c=>c.passed)?"PASS":"FAIL",checks,publicAi:"DISABLED_PENDING_PRIVACY_REVIEW",databaseMutation:false};mkdirSync(dirname(output),{recursive:true});writeFileSync(output,`${JSON.stringify(report,null,2)}\n`);writeFileSync(output.replace(/\.json$/,'.md'),`# Privacy check\n\n- Status: **${report.status}**\n- Public AI: **DISABLED**\n- Database mutation: **NO**\n`);process.stdout.write(`${JSON.stringify(report)}\n`);if(report.status!=="PASS")process.exitCode=1;
