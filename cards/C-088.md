# C-088 — Align public transparency dashboard with release evidence

status: done
deps: [C-087]
implements: [transparency]

## Scope

Update the transparency dashboard generator to consume the current production-readiness
schema and external evidence ledger. Show local recovery/performance/security scope,
all 11 external blockers, and an explicit no-endorsement/no-Public-Beta disclosure.

## Allowed files

- `scripts/transparency-dashboard.mjs`
- `tests/operations/transparency-dashboard.test.ts`
- `cards/C-088.md`
- `artifacts/transparency/dashboard.json`
- `artifacts/transparency/dashboard.md`

## Verify (run these before calling the card done)

- [x] `npm run transparency:dashboard` exits 0 and writes all 11 external blockers.
- [x] `npm test -- --testTimeout=15000 tests/operations/transparency-dashboard.test.ts`
      verifies no stale `verified` field and Public Beta remains disabled.
- [x] `npm run typecheck` and `npm run lint` pass.

## Done-evidence (world-state proof, named BEFORE building)

The public transparency route renders a dashboard whose operations and blocker
claims match the current evidence artifacts; it does not imply Council or Public
Beta approval.

## Evidence (paste the actual proof here when done)

`npm run transparency:dashboard` → release `NOT_READY`, Public Beta `false`, 11
external blockers, backup/restore `PASS_DISPOSABLE_ONLY`. The dashboard now shows
local uptime/performance/security scope and independent security `PENDING_EXTERNAL`.
The dedicated test passes; typecheck passes; lint passes with 7 existing warnings.
The public transparency component accepts the new evidence fields without stale
schema errors. Disclosure explicitly states this is not Council, legal, security,
school-pilot or Public Beta approval.
