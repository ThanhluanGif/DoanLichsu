# C-003 — Hoàn thiện public read API với 50 nội dung song ngữ

status: done
deps: C-002
implements: FR1, FR2, FR3, FR4, FR5, FR6, FR7

## Scope

Xây toàn bộ surface đọc công khai trong contract trên data model thật: home, periods, timeline, content list/detail, search không dấu, taxonomies và alternate locale. Seed idempotent đúng 50 content nodes/100 translations có source, gồm 6 periods, 20 events, 10 people, 10 artifacts và 4 topics; API chỉ lộ translation đã published và mọi endpoint xuất hiện trong live OpenAPI/docs.

## Allowed files

- `.gitignore`
- `package.json`
- `package-lock.json`
- `migrations/**`
- `scripts/seed.*`
- `src/data/**`
- `src/lib/db/**`
- `src/lib/content/**`
- `src/lib/search/**`
- `src/lib/openapi/**`
- `src/app/docs/**`
- `src/app/api/v1/public/**`
- `src/app/api/v1/[locale]/**`
- `src/app/api/v1/contents/**`
- `tests/public-api/**`
- `tests/foundation/migrations.test.ts`
- `cards/C-003.md`

## Verify (run these before calling the card done)

- [x] `npm run db:migrate && npm run db:seed` hai lần đều exit 0; query DB trả `content_nodes=50`, `content_translations=100`, `sources>=50`, đúng phân bố 6/20/10/10/4.
- [x] `npm test -- tests/public-api` và `npm run typecheck` exit 0, gồm edge cases invalid locale/type/page, unpublished translation, date precision, pagination tie-breaker.
- [x] Trên production server, curl home/timeline/list/detail/taxonomies trả shape contract và chỉ `PUBLISHED`; mỗi endpoint hiện trong `/openapi.json` và `/docs`.
- [x] Curl search `q=Điện%20Biên%20Phủ` và `q=dien%20bien%20phu` trả cùng content id đầu; filter type/period/page giữ deterministic meta.
- [x] Curl alternate của một record song ngữ trả đúng cặp URL; fixture có EN chưa published trả `alternate:null`, không 404 sai trang.

## Done-evidence (world-state proof, named BEFORE building)

URL live `/api/v1/vi/timeline`, `/api/v1/vi/search?q=dien%20bien%20phu` và một detail VI/EN trả dữ liệu nguồn thật; curl/DB output chứng minh đúng 50 nodes, 100 translations và public không lộ draft.

## Evidence (paste the actual proof here when done)

- Full clean gate trên `data/c003-final.db`: `npm ci`, migrate, seed hai lần, lint,
  typecheck, 6 test files/30 tests, Next production build và standalone artifact check đều
  exit 0. Hai lần seed cùng trả
  `{"contentNodes":50,"translations":100,"sources":50,"users":0}`; `users:0` là
  shape-compatible stub được C-004 thay bằng 3 khi user/auth schema tồn tại.
- DB proof: `contentNodes=50`, `translations=100`, `sources=50`, `media=10`,
  `publishedTranslations=99`; distribution `PERIOD=6`, `EVENT=20`, `PERSON=10`,
  `ARTIFACT=10`, `TOPIC=4`. EN fixture thứ 100 ở `READY_FOR_REVIEW`, không lộ public.
- Production artifact đang chạy tại `http://127.0.0.1:3303`. Các URL `/api/v1/vi/home`,
  `/periods`, `/timeline`, `/contents`, detail VI/EN, `/taxonomies` và alternate đều HTTP
  200. `http://127.0.0.1:3303/docs` HTTP 200; `/openapi.json` là 3.1.0 và chứa đủ 8
  public paths cùng search `sort`.
- Search live `Điện Biên Phủ` và `dien bien phu` cùng trả id đầu
  `event-dien-bien-phu`; filter EVENT + period trả meta
  `{"page":1,"pageSize":5,"total":7,"totalPages":2}`. Detail VI/EN cùng id và
  alternate hai chiều; `artifact-mig21-4324` trả `alternate:null`, còn EN detail trực tiếp
  trả 404 `CONTENT_NOT_FOUND`.
- API timing sau warm-up (20 request): home p95 `23.9 ms`, search p95 `9.8 ms`.
  Standalone bundle chứa và chạy thật `scripts/seed.mjs` (53.2 KiB).
- Adversarial review sửa unsafe pagination, mixed-locale fields, date invariants, seed
  overwrite guard, media/source metadata và OpenAPI drift; Antigravity Gemini cross-vendor
  review cuối trả `approve`, không có finding.
