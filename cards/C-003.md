# C-003 — Hoàn thiện public read API với 50 nội dung song ngữ

status: todo
deps: C-002
implements: FR1, FR2, FR3, FR4, FR5, FR6, FR7

## Scope

Xây toàn bộ surface đọc công khai trong contract trên data model thật: home, periods, timeline, content list/detail, search không dấu, taxonomies và alternate locale. Seed idempotent đúng 50 content nodes/100 translations có source, gồm 6 periods, 20 events, 10 people, 10 artifacts và 4 topics; API chỉ lộ translation đã published và mọi endpoint xuất hiện trong live OpenAPI/docs.

## Allowed files

- `package.json`
- `package-lock.json`
- `migrations/**`
- `scripts/seed.*`
- `src/data/**`
- `src/lib/db/**`
- `src/lib/content/**`
- `src/lib/search/**`
- `src/lib/openapi/**`
- `src/app/api/v1/public/**`
- `src/app/api/v1/[locale]/**`
- `src/app/api/v1/contents/**`
- `tests/public-api/**`
- `cards/C-003.md`

## Verify (run these before calling the card done)

- [ ] `npm run db:migrate && npm run db:seed` hai lần đều exit 0; query DB trả `content_nodes=50`, `content_translations=100`, `sources>=50`, đúng phân bố 6/20/10/10/4.
- [ ] `npm test -- tests/public-api` và `npm run typecheck` exit 0, gồm edge cases invalid locale/type/page, unpublished translation, date precision, pagination tie-breaker.
- [ ] Trên production server, curl home/timeline/list/detail/taxonomies trả shape contract và chỉ `PUBLISHED`; mỗi endpoint hiện trong `/openapi.json` và `/docs`.
- [ ] Curl search `q=Điện%20Biên%20Phủ` và `q=dien%20bien%20phu` trả cùng content id đầu; filter type/period/page giữ deterministic meta.
- [ ] Curl alternate của một record song ngữ trả đúng cặp URL; fixture có EN chưa published trả `alternate:null`, không 404 sai trang.

## Done-evidence (world-state proof, named BEFORE building)

URL live `/api/v1/vi/timeline`, `/api/v1/vi/search?q=dien%20bien%20phu` và một detail VI/EN trả dữ liệu nguồn thật; curl/DB output chứng minh đúng 50 nodes, 100 translations và public không lộ draft.

## Evidence (paste the actual proof here when done)

(empty until done)
