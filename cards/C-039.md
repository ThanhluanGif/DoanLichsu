# C-039 — N-001 staging smoke và live alert probe

status: done
deps: [C-038]
implements: [infra]

## Scope

Thêm một smoke/alert probe có thể chạy trên staging hoặc production-like URL. Probe kiểm tra HTTPS endpoint, health/database, OpenAPI, public VI/EN routes, curriculum transparency và reconstruction/map API; timeout, status, content-type và database status đều fail closed. Probe không sửa dữ liệu và ghi JSON/Markdown artifact để trực vận hành đọc lại.

## Allowed files

- `scripts/live-smoke.mjs`
- `package.json`
- `artifacts/staging-smoke/**`
- `cards/C-039.md`

## Verify (run these before calling the card done)

- [x] `node scripts/live-smoke.mjs --origin <HTTPS origin> --output artifacts/staging-smoke/report.json` exits 0 on a healthy live origin and emits PASS Markdown/JSON.
- [x] Same probe exits non-zero against a fixture with a failing endpoint and records the failing check without mutating the database.
- [x] `npm run lint`, `npm run typecheck`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Live HTTPS smoke URL, machine-readable report, human-readable alert summary, and a red-fixture proof. The report names the origin, timestamp, status, endpoint checks, latency and remediation hint for every failed check.

## Evidence (paste the actual proof here when done)

- Live HTTPS probe passed against `https://promises-ports-hawaiian-interactive.trycloudflare.com`: 7/7 checks passed (healthz/database, OpenAPI, VI/EN places APIs, reconstruction API, map HTML, transparency HTML), all HTTP 200; machine report: `artifacts/staging-smoke/live-report.json`; operator summary: `artifacts/staging-smoke/live-report.md`.
- Red fixture `https://127.0.0.1:9` exited non-zero (`1`), reported `FAIL` for all 7 checks with remediation text, and did not access or mutate the database; reports: `artifacts/staging-smoke/red-report.json`, `artifacts/staging-smoke/red-report.md`.
- Consolidated proof: `artifacts/staging-smoke/smoke-proof.json`.
- `npm run lint`, `npm run typecheck`, and `npm run build` all exited 0 on 2026-08-13. Build regenerated `next-env.d.ts`; it was restored to the repository's dev-type references after verification.
