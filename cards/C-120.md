# C-120 — Surface published-history remediation in transparency dashboard

status: done
deps: C-119
implements: infra

## Scope

Extend the public transparency dashboard with the published-content history
remediation state from C-119. The dashboard must show the 105-row queue,
`REQUIRES_HUMAN_REVIEW`, and zero database writes while preserving `NOT_READY`,
Public Beta disabled, and all 11 external blockers.

## Allowed files

scripts/transparency-dashboard.mjs
tests/operations/transparency-dashboard.test.ts
src/components/public/TransparencyDashboard.tsx
artifacts/transparency/dashboard.json
artifacts/transparency/dashboard.md
cards/C-120.md

## Verify (run these before calling the card done)

- [x] `node scripts/transparency-dashboard.mjs` exits 0 and includes
      `contentHistory.status=REQUIRES_HUMAN_REVIEW`, candidate count 105,
      and database writes 0.
- [x] Focused transparency test and `npm run typecheck` pass; dashboard still
      reports Public Beta false and 11 blockers.

## Done-evidence (world-state proof, named BEFORE building)

The public transparency dashboard and its machine artifact visibly disclose the
105-item correction-history queue and its human-review requirement without
claiming approval or changing release eligibility.

## Evidence (paste the actual proof here when done)

Verified 2026-08-13:

- `node scripts/transparency-dashboard.mjs` exited 0 and retained
  `releaseStatus: NOT_READY`, `publicBeta: false`, and 11 blockers; its
  `contentHistory` object is `REQUIRES_HUMAN_REVIEW` with 105 published rows,
  105 candidates, `databaseWrites: 0`, and no fabricated approval.
- The public dashboard component now exposes the editorial-history state and
  human-review count in both locales.
- `RELEASE_EVIDENCE_RUN=1 npx vitest run
  tests/operations/transparency-dashboard.test.ts` passed: 1 file, 1 test.
  (The normal source-tree-match assertion remains pending the next release
  evidence refresh because this card changes the source tree.)
- `npm run typecheck` exited 0.
