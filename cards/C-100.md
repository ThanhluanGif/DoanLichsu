# C-100 — Add source provenance and scope disclosure to release check

status: done
deps: C-099
implements: infra

## Scope

Extend the full release-check report with a deterministic source-tree SHA-256,
tested commit, and an explicit scope disclosure that HTTPS E2E requires a real
deployed HTTPS origin. The release check must never treat its local recovery or
contract run as official production, and must preserve its existing secret-log
scan and external-origin requirement.

## Allowed files

scripts/release-check.mjs
scripts/source-tree-hash.mjs
tests/operations/release-check-contract.test.ts
cards/C-100.md

## Verify (run these before calling the card done)

- [x] `npm test -- --run tests/operations/release-check-contract.test.ts` passes
      and verifies the report schema/disclosures in the release-check source.
- [x] `npm run typecheck` passes.
- [x] `node --check scripts/release-check.mjs` passes.

## Done-evidence (world-state proof, named BEFORE building)

The release-check script's report shape includes `testedCommit`,
`sourceTreeSha256`, `originKind: deployed HTTPS required`, and explicit local
recovery/contract scope disclosure while retaining `passed: false` whenever the
external HTTPS origin is unavailable.

## Evidence (paste the actual proof here when done)

Focused release-check contract test passed (1 test), `npm run typecheck` passed,
and `node --check scripts/release-check.mjs` passed. The release-check source now
records `testedCommit` and `sourceTreeSha256`, and explicitly discloses disposable
local production-like recovery/contract scope, deployed HTTPS requirement for E2E,
and `officialProduction: false`.
