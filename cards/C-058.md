# C-058 — Grounded AI API và internal-alpha tutor surface

status: done
deps: [C-045, C-047, C-048]
implements: [ai-product-surface]

## Scope

Đưa grounded answer contract vào API thật `/api/v1/{locale}/ai/answer`, dùng SQLite approved
content/claims/source trực tiếp, không gọi model ngoài. API luôn trả citation hoặc abstain;
public beta flag mặc định `false`, chỉ cho internal alpha khi request có header/operator
flag. Thêm contextual tutor panel vào lesson detail với guided prompts, alpha disclosure,
privacy notice và fallback search/lesson. Không mở free-form public AI.

## Allowed files

- `src/app/api/v1/[locale]/ai/answer/route.ts`
- `src/lib/ai/**`
- `src/components/public/ContextualTutor.tsx`
- `src/app/[locale]/[kind]/[slug]/page.tsx`
- `src/app/globals.css`
- `src/lib/openapi/public-catalog.ts`
- `tests/contract/ai-answer.test.ts`
- `tests/e2e/contextual-tutor.spec.ts`
- `artifacts/ai-product/**`
- `cards/C-058.md`

## Verify (run these before calling the card done)

- [x] Live-like API returns contract-valid grounded answer and abstains on injection/out-of-
      corpus; public beta remains disabled without alpha header.
- [x] Lesson detail shows internal-alpha disclosure, guided prompts, citation/fallback.
- [x] `npm run lint`, `npm run typecheck`, `npm test`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Live API contract and browser-visible contextual tutor surface with explicit disabled-public
gate; evidence includes grounded/abstention HTTP responses and accessibility checks.

## Evidence (paste the actual proof here when done)

- Local live-like dev server exposes the API contract: public request receives `403 AI_BETA_DISABLED`; internal-alpha request returns grounded/abstain contract responses with source locator and snapshot ID. Browser lesson detail includes `Internal alpha` tutor disclosure and guided prompts.
- `npm run lint`, `npm run typecheck`, `npm test` (47 files/214 tests), and `npm run build` exit 0.
- Public beta remains disabled by design; no claim of public AI readiness is made while the 500-question eval gate is incomplete.
- Live proof from `https://wins-orbit-schools-pig.trycloudflare.com`: public AI 403, internal grounded response with citations, injection abstention, and browser-visible tutor. Consolidated evidence: `artifacts/ai-product/ai-product-proof.json`.
