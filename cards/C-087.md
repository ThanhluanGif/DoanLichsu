# C-087 — Align DoD audit with recovery evidence contract

status: done
deps: [C-086]
implements: [infra]

## Scope

Make the DoD audit understand the current backup/restore evidence contract while
retaining the honest `PASS_DISPOSABLE_ONLY` scope. Add a regression test so a valid
disposable rehearsal cannot be misreported as blocked, and ensure production RPO/RTO
remains an external gate.

## Allowed files

- `scripts/dod-audit.mjs`
- `tests/operations/dod-audit.test.ts`
- `cards/C-087.md`
- `artifacts/release/dod-audit.json`
- `artifacts/release/dod-audit.md`

## Verify (run these before calling the card done)

- [x] `npm test -- --testTimeout=15000 tests/operations/dod-audit.test.ts` passes.
- [x] `node scripts/dod-audit.mjs` returns `NOT_READY` with
      `backup-recovery-mechanism: PASS_DISPOSABLE_ONLY` and 11 external blockers.
- [x] `npm run typecheck` and `npm run lint` pass.

## Done-evidence (world-state proof, named BEFORE building)

The committed DoD report accurately reflects recovery mechanism evidence without
claiming production RPO/RTO or Public Beta readiness.

## Evidence (paste the actual proof here when done)

`npm test -- --testTimeout=15000 tests/operations/dod-audit.test.ts` → 1 test
passed. `node scripts/dod-audit.mjs` → `NOT_READY`, 14 checks, 11 external
blockers; recovery is correctly reported `PASS_DISPOSABLE_ONLY`, not production
RPO/RTO. Typecheck and lint pass with 7 existing warnings. Public Beta remains
disabled.
