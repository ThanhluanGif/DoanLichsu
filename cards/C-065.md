# C-065 — Production-like public smoke and latency evidence

status: done
deps: [C-064, C-061]
implements: [operations-observability]

## Scope

Chạy smoke/latency probe lặp lại trên origin production-like của HEAD: health, public home,
search, lesson detail, transparency, OpenAPI và public AI denial. Ghi status, p50/p95 và
không đưa kết quả tạm thời thành 90-day uptime/pen-test/fixed-domain claim.

## Allowed files

- `scripts/live-smoke.mjs`
- `artifacts/operations/**`
- `cards/C-065.md`

## Verify (run these before calling the card done)

- [x] `APP_ORIGIN=<quick-tunnel> npm run live:smoke` exits 0 for public routes and records
  latency samples; public AI remains denied.
- [x] Proof explicitly labels quick tunnel, sample window and external gates still pending.

## Done-evidence (world-state proof, named BEFORE building)

`artifacts/operations/live-smoke-proof.json` contains repeatable production-like route checks
with p95 latency and public AI 403/401 evidence, while release remains blocked for official
domain, uptime and independent security review.

## Evidence (paste the actual proof here when done)

Proof: `APP_ORIGIN=https://portal-focus-uses-noon.trycloudflare.com npm run live:smoke` → PASS, 7/7 routes HTTP 200, approximate p95 955ms (single short run). `artifacts/operations/live-smoke-proof.json` labels the origin as temporary quick tunnel and lists official domain, 90-day uptime, independent load/security review and on-call owner as pending.
