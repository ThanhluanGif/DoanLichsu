# C-041 — N-003 source hierarchy và rights/legal playbook

status: done
deps: [C-040]
implements: [source-governance]

## Scope

Tạo playbook vận hành cho source hierarchy, provenance, license/permission, Wikimedia
metadata, media derivatives, LINK_ONLY, takedown và re-check. Thêm rights ledger
machine-readable với validator fail-closed: không cho `SERVED` nếu thiếu license,
attribution, source URL, reviewer hoặc decision; `LINK_ONLY` là trạng thái an toàn khi
quyền chưa rõ. Đây là quy trình nội bộ, không phải ý kiến pháp lý; trường hợp mơ hồ
phải chuyển legal/IP reviewer.

## Allowed files

- `docs/rights/**`
- `scripts/rights-check.mjs`
- `package.json`
- `artifacts/rights/**`
- `cards/C-041.md`

## Verify (run these before calling the card done)

- [x] `npm run rights:check` exits 0 for the reviewed ledger and emits JSON/Markdown.
- [x] Red fixture with missing license/attribution exits non-zero and does not mutate DB.
- [x] `npm run lint`, `npm run typecheck`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Versioned playbook plus a rights ledger report that proves each served asset has an
auditable rights decision, while unclear assets are explicitly LINK_ONLY or blocked.

## Evidence (paste the actual proof here when done)

- `npm run rights:check -- --input artifacts/rights/ledger.json --output artifacts/rights/report.json` exited 0: one `PERMITTED` binary with complete license/credit/recheck fields and one `LINK_ONLY` record that cannot serve binary. Reports: `artifacts/rights/report.json`, `artifacts/rights/report.md`.
- Red ledger `artifacts/rights/red-ledger.json` exited 1; missing license, attribution, recheck and takedown fields were listed; no database is opened or mutated. Report: `artifacts/rights/red-report.json`.
- Versioned playbook: `docs/rights/source-hierarchy.md`.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit 0; generated `next-env.d.ts` was restored to dev-type references.
