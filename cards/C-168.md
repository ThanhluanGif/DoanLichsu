# C-168 — Patch high-severity transitive dependency

status: done
deps: C-167
implements: infra

## Scope

Patch the high-severity `nanoid` transitive dependency vulnerability
`GHSA-2v37-7h3g-55p8` by refreshing the lockfile from `3.3.17` to the fixed
`3.3.18` release selected by `npm audit fix`. Preserve the application API and
the fail-closed external security boundary. Regenerate the local security
review pack so dependency audit is green while independent security review and
pen-test remain explicitly pending external evidence.

## Allowed files

package-lock.json
artifacts/security/security-review-pack.json
artifacts/security/security-review-pack.md
artifacts/security/security-handoff-readiness.json
artifacts/production-readiness/report.json
artifacts/release/current-head-evidence.json
artifacts/release/current-head-evidence.md
artifacts/release/dod-audit.json
artifacts/release/dod-matrix-consistency.json
artifacts/release/year-one-dod-matrix.json
artifacts/transparency/dashboard.json
tests/operations/global-master-consistency-check.test.ts
cards/C-168.md

## Verify (run these before calling the card done)

- [x] While C-168 is in flight, the Global Master consistency fixture accepts
      one in-flight card and its focused test passes.
- [x] `npm audit --audit-level=high` reports zero high/critical vulnerabilities
      and `npm ls nanoid --all` resolves `nanoid@3.3.18`.
- [x] `npm run security:review-pack` reports `PASS_LOCAL_SECURITY_EVIDENCE`,
      dependency audit high/critical `0/0`, while independent review remains
      `PENDING_EXTERNAL` and pen-test remains `NOT_PERFORMED`.
- [x] `npm test -- --testTimeout=15000` passes after the card is finalized and
      `bash /Users/admin/.agents/skills/flow/runner/flow.sh check C-168` passes.

## Done-evidence (world-state proof, named BEFORE building)

The repository's dependency and security evidence no longer contains the known
high-severity `nanoid` vulnerability, CI can install the fixed lockfile, and
the security pack records a green local dependency audit without promoting
local checks to an independent security approval.

## Evidence (paste the actual proof here when done)

- `npm audit --audit-level=high` exited 0 with `found 0 vulnerabilities`; the
  dependency tree resolves `next@16.3.0 → postcss@8.5.23 → nanoid@3.3.18`.
- `npm run security:review-pack` exited 0 with
  `PASS_LOCAL_SECURITY_EVIDENCE`, `independentReview=PENDING_EXTERNAL`,
  `penTest=NOT_PERFORMED` and `publicBetaAllowed=false`.
- `RELEASE_EVIDENCE_RUN=1 npm run release:evidence:current` exited 0 with
  tested commit `bb1a55d5783d802e5622373c8864047725a28e08`, source-tree hash
  `908f783238407e325d6718b493c4a8b372c0ab5628d4a42374b174e438c9b2d7`,
  quality `[0,0,0,0]`, local health/OpenAPI/search `200/200/200`, security
  headers `true`, search p95 `39.74ms`, backup/restore checksums `true/true`, and no database
  mutation.
- Full `npm test -- --testTimeout=15000` exited 0: **83 test files, 288
  tests passed** after C-168 was finalized. Independent security review and
  pen-test remain external requirements.
