# C-103 — Generate an external-gate handoff checklist

status: done
deps: C-102
implements: infra

## Scope

Create a machine-readable handoff report for the eleven external Public Beta
gates. It must derive each gate's required evidence and owner role from the
canonical ledger/docs, identify missing owner/artifact fields without inventing
names, and state the exact conditions under which an operator may update a gate.
The report is a handoff aid only and must not mutate the ledger or enable release.

## Allowed files

scripts/external-evidence-handoff.mjs
tests/operations/external-evidence-handoff.test.ts
artifacts/operations/external-evidence-handoff.json
artifacts/operations/external-evidence-handoff.md
cards/C-103.md

## Verify (run these before calling the card done)

- [x] `node scripts/external-evidence-handoff.mjs` exits 0 and reports 11
      pending gates with no named owners or artifacts.
- [x] `npm test -- --run tests/operations/external-evidence-handoff.test.ts`
      passes and verifies no fabricated identities/approvals.
- [x] `npm run typecheck` passes.

## Done-evidence (world-state proof, named BEFORE building)

The handoff artifact lists all eleven external blockers, the required evidence
shape, owner role, current `PENDING` status, and a `noFabricatedEvidence: true`
disclosure. The canonical ledger remains unchanged and Public Beta remains off.

## Evidence (paste the actual proof here when done)

`node scripts/external-evidence-handoff.mjs` exited 0 with `pendingCount: 11`,
ledger SHA-256 `d85b395290e9bbd3b4bdf78c79008423262ea54f923ff61f580d3dd57d93c51d`,
and `noFabricatedEvidence: true`. Focused handoff test passed (1 test); typecheck
passed. All rows remain `PENDING` with `owner: null` and `artifact: null`; Public
Beta remains disabled and the canonical ledger was not mutated.
