# C-150 — Hiển thị trạng thái evidence trong hàng đợi kiểm duyệt lịch sử

status: todo
deps: C-149
implements: infra

## Scope

Extend the authenticated published-history reviewer queue so each item exposes
source-locator and claim-locator readiness, plus the two translation statuses.
Render those states in the existing Vietnamese admin table with plain-language
labels and a link to the content detail. The states are read-only hints for a
human reviewer; this card must not create audit rows, auto-approve content, or
change the DoD/Public Beta state. Amend the interface contract/OpenAPI shape
before changing the producer or UI consumer.

## Allowed files

flow/05-contract.md
src/lib/openapi/editorial-catalog.ts
src/lib/content/editorial.ts
src/lib/admin-client/types.ts
src/components/admin/PublishedHistoryQueue.tsx
tests/editorial-api/published-history-queue.test.ts
KE_HOACH_12_THANG_CONG_TRI_THUC_LICH_SU_VIET_NAM_AI.md
artifacts/release/current-head-evidence.json
artifacts/release/current-head-evidence.md
artifacts/production-readiness/report.json
artifacts/production-readiness/report.md
artifacts/release/dod-audit.json
artifacts/release/dod-audit.md
artifacts/release/dod-matrix-consistency.json
artifacts/release/dod-matrix-consistency.md
artifacts/release/year-one-dod-matrix.json
artifacts/release/year-one-dod-matrix.md
artifacts/transparency/dashboard.json
artifacts/transparency/dashboard.md
cards/C-150.md

## Verify (run these before calling the card done)

- [ ] The planning contract and runtime OpenAPI expose the new queue fields;
      the standalone OpenAPI probe and focused queue test pass.
- [ ] Reviewer/Admin receives the queue with deterministic `READY` or
      `MISSING_OR_UNVERIFIED` source/claim states and `vi`/`en` translation
      statuses; an Editor remains `403` and the request is read-only.
- [ ] The admin table renders the evidence states in Vietnamese, keeps keyboard
      links and the existing pagination, and contains no auto-approval action.
- [ ] Full lint/typecheck/build/test and release evidence pass; DoD remains
      `NOT_READY`, Public Beta false, 105 internal history gaps and 11 external
      blockers.

## Done-evidence (world-state proof, named BEFORE building)

An authenticated Reviewer/Admin opening `/admin/published-history` can see each
queued item’s evidence-readiness labels and translation statuses, while the
underlying API response and OpenAPI schema expose the same fields. The queue
remains read-only until a real reviewer submits the existing explicit
`HUMAN_REVIEWED` attestation.

## Evidence (paste the actual proof here when done)

(empty until done)
