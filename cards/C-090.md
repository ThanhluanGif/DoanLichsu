# C-090 — Publish bilingual privacy and AI safety notice

status: done
deps: [C-089]
implements: [privacy]

## Scope

Add public Vietnamese and English privacy/AI safety notices and link them from the
public footer. Explain data minimization, no public AI beta, internal-alpha access,
retention/delete targets, child/guardian safeguards, correction/rights contact path,
incident response and the fact that DPIA approval is still pending.

## Allowed files

- `src/app/[locale]/privacy/page.tsx`
- `src/components/public/PublicShell.tsx`
- `tests/public-ui/privacy-notice.test.ts`
- `cards/C-090.md`
- `artifacts/privacy/live-privacy-proof.json`

## Verify (run these before calling the card done)

- [x] `npm test -- --testTimeout=15000 tests/public-ui/privacy-notice.test.ts` passes
      route, copy and footer-link assertions.
- [x] `npm run typecheck` and `npm run lint` pass.
- [x] Live HTTPS pages `/vi/privacy` and `/en/privacy` render the notice and links.

## Done-evidence (world-state proof, named BEFORE building)

The public site exposes a bilingual privacy/safety notice reachable from both
footers. It is a transparency/control surface, not legal/DPIA approval.

## Evidence (paste the actual proof here when done)

`privacy-notice.test.ts` → 1 test passed. Typecheck, lint and build pass (lint has 7
pre-existing warnings). After rebuilding/restarting the standalone server with the
current bundle, live HTTPS checks against
`https://tops-gone-rose-trademark.trycloudflare.com/vi/privacy` and `/en/privacy`
returned HTTP 200 HTML containing the bilingual title, AI-disabled disclosure and
`DRAFT_PENDING_PRIVACY_REVIEW`. The notice is public transparency only; it does not
claim DPIA/legal approval.
