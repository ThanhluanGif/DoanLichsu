# C-089 — Enforce current-HEAD release evidence coherence

status: done
deps: [C-088]
implements: [infra]

## Scope

Add a coherence check for `artifacts/release/current-head-evidence.json` and refresh
the official local production-like evidence on the current HEAD. The check must fail
if the report commit differs from `git rev-parse HEAD`, or if quality steps are
missing/failed. It must preserve disclosures that the origin is not official
production and HTTPS E2E was not run in this local run.

## Allowed files

- `scripts/release-evidence-current.mjs`
- `tests/operations/current-head-evidence.test.ts`
- `cards/C-089.md`
- `artifacts/release/current-head-evidence.json`
- `artifacts/release/current-head-evidence.md`

## Verify (run these before calling the card done)

- [x] `npm run release:evidence:current` exits 0 and records the current HEAD SHA.
- [x] `npm test -- --testTimeout=15000 tests/operations/current-head-evidence.test.ts`
      passes commit mismatch and disclosure checks.
- [x] `npm run typecheck` and `npm run lint` pass.

## Done-evidence (world-state proof, named BEFORE building)

The committed release evidence report contains the exact current HEAD commit and
fresh local standalone health/OpenAPI/search/security/backup observations. A future
deployment can compare its own commit before accepting the artifact.

## Evidence (paste the actual proof here when done)

`npm run release:evidence:current` → exit 0; report commit
`6889ad3e81a76e6df88874c507a90601a0fd53cb`, quality `[0,0,0,0]`, local
health/OpenAPI/search `200/200/200`, security headers true, backup/restore checksums
true/true. The coherence test passes against the current HEAD and preserves the
disclosures that this is local production-like evidence, not official production;
HTTPS E2E was not run in this local run. Typecheck and lint pass (7 existing
warnings).
