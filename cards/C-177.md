# C-177 — Browser proof for published-history review handoff

status: done
deps: C-175, C-176
implements: infra

## Scope

Add a dedicated Playwright release test for the existing Reviewer-only published-history
attestation surface. The test must open one already-published seeded item over the configured
HTTPS release origin, verify that an Editor cannot call the history-review endpoint, then as a
Reviewer enter an evidence locator and note, check the explicit `HUMAN_REVIEWED` confirmation,
submit it through the browser UI, and verify the success state plus the read-back queue/API state.
This card proves UI/API wiring on the disposable release environment only; it must not alter the
canonical packet, claim Council approval, or turn any external evidence ledger entry into PASS.

## Allowed files

tests/e2e/published-history-review.spec.ts
KE_HOACH_12_THANG_CONG_TRI_THUC_LICH_SU_VIET_NAM_AI.md
cards/C-177.md
artifacts/operations/external-evidence-handoff.json
artifacts/operations/external-evidence-handoff.md
artifacts/operations/external-evidence-verification.json
artifacts/operations/local-standalone-smoke.json
artifacts/operations/production-handoff-check.json
artifacts/operations/report.json
artifacts/production-readiness/report.json
artifacts/production-readiness/report.md
artifacts/release/dod-audit.json
artifacts/release/dod-audit.md
artifacts/release/dod-matrix-consistency.json
artifacts/release/global-master-consistency.json
artifacts/release/global-master-consistency.md
artifacts/release/current-head-evidence.json
artifacts/release/current-head-evidence.md
artifacts/release/playwright-report/index.html
artifacts/release/year-one-dod-matrix.json
artifacts/release/year-one-dod-matrix.md
artifacts/transparency/dashboard.json

## Verify (run these before calling the card done)

- [x] `npx playwright test tests/e2e/published-history-review.spec.ts` with `E2E_BASE_URL` set to
      an HTTPS disposable release environment and `E2E_EDITOR_*`/`E2E_REVIEWER_*` credentials;
      the test reports one passing browser journey.
- [x] The journey opens `/admin/contents/artifact-bach-dang-stakes`, observes the published
      history attestation panel, submits locator/note/checkbox as Reviewer, and observes the
      visible success alert; the queue no longer lists that item and a duplicate POST returns
      `HISTORY_ALREADY_REVIEWED` (409).
- [x] `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`, and
      `npm run global:consistency:check` all pass after the card evidence is recorded.

## Done-evidence (world-state proof, named BEFORE building)

On the configured HTTPS disposable release origin, a real browser session can open the published
content editor as Reviewer, submit the explicit history attestation, see the success alert, and
read back the queue/API state proving the item is no longer pending and duplicate review is
rejected. The canonical
Global Master packet remains `105 pending / 0 reviewed`; this is wiring evidence, not an external
human-review approval.

## Evidence (paste the actual proof here when done)

- Browser release proof (HTTPS disposable origin `https://127.0.0.1:4443`, self-signed cert
  accepted only for this local disposable run, `E2E_DISPOSABLE_RELEASE=1`):
  `1 passed (4.6s)` for `tests/e2e/published-history-review.spec.ts`.
- The journey proved Editor `POST /history-review` → `403`, Reviewer UI submission → visible
  `Đã ghi nhận xác nhận lịch sử bởi người duyệt.`, queue read-back no longer contained
  `artifact-bach-dang-stakes`, and duplicate POST → `409 HISTORY_ALREADY_REVIEWED`.
- Focused API regression: `2 files passed`, `3 tests passed` for published-history review/queue.
- Full regression: `84 test files passed`, `293 tests passed`.
- `npm run lint`: exit 0, 12 pre-existing warnings, 0 errors.
- `npm run typecheck`: exit 0.
- `npm run build`: exit 0; production route build includes `/api/v1/admin/contents/[id]/history-review`.
- `npm run global:consistency:check` before closing: `PASS_GLOBAL_MASTER_CONSISTENT`,
  `177/177`, `inFlightCards: 0`, `105 pending`, `0 reviewed`, `11 externalBlockers`,
  `publicBeta: false`, `releaseAllowed: false`, `databaseMutation: false`.
- Release evidence regenerated on current main source tree: quality `4/4` zero exit codes,
  health/OpenAPI/search `200/200/200`, security headers pass, backup/restore checks pass,
  database mutation `false`; this remains local-only evidence.
