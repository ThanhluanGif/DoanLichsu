# C-063 — Production-like backup and restore rehearsal evidence

status: done
deps: [C-054, C-062]
implements: [operations-recovery]

## Scope

Chạy rehearsal độc lập với database tạm: migrate + seed, SQLite online backup có manifest
SHA-256, restore sang file mới, integrity/count check và tamper rejection. Lưu proof trung
thực; không sửa production database và không claim RPO/RTO đã đạt nếu chưa đo thời gian.

## Allowed files

- `artifacts/operations/**`
- `cards/C-063.md`

## Verify (run these before calling the card done)

- [x] `npm run test:recovery` passes both backup/restore and tamper rejection tests.
- [x] A generated proof records source/restore paths, SHA-256 verification, schema/counts,
  integrity check and `databaseMutation=false`.

## Done-evidence (world-state proof, named BEFORE building)

`artifacts/operations/backup-restore-proof.json` documents a real disposable-database
rehearsal with verified checksum and restored counts. Production readiness remains blocked
until fixed-domain load, uptime and named operational ownership exist.

## Evidence (paste the actual proof here when done)

Proof: `npm run test:recovery` → 1 file, 2 tests PASS. `artifacts/operations/backup-restore-proof.json` records checksum verification, schema version 9, restored counts, tamper rejection and `databaseMutation=false` on a disposable temporary SQLite database.

Boundary: this is a verified recovery-mechanism rehearsal only; it does not fabricate production RPO/RTO, fixed-domain uptime, load or on-call ownership.
