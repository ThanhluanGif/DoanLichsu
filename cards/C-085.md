# C-085 — Harden internal-alpha AI request boundary

status: done
deps: [C-084]
implements: [ai-safety]

## Scope

Enforce the AI answer contract at the internal-alpha route boundary: reject empty
or overlong questions, overlong context slugs, malformed JSON and non-string
fields with structured 400 errors before corpus access. Keep public AI disabled,
avoid transcript persistence, and preserve citation/abstain behavior.

## Allowed files

- `src/app/api/v1/[locale]/ai/answer/route.ts`
- `tests/editorial-api/ai-answer.test.ts`
- `cards/C-085.md`

## Verify (run these before calling the card done)

- [x] `npm test -- --testTimeout=15000 tests/editorial-api/ai-answer.test.ts`
      covers public-disabled, auth, malformed body and length bounds.
- [x] `npm run typecheck` and `npm run lint` pass.
- [x] HTTPS alpha endpoint still returns grounded citations for a valid invited
      request and 403 without the internal-alpha header.

## Done-evidence (world-state proof, named BEFORE building)

On the HTTPS release-like alpha URL, a valid authenticated reviewer receives a
grounded answer with citations; malformed/oversized requests receive structured
400 responses; public requests remain 403 and no transcript is written.

## Evidence (paste the actual proof here when done)

`npm test -- --testTimeout=15000 tests/editorial-api/ai-answer.test.ts` → 1 file,
4 tests passed, including authenticated grounded response, public-disabled 403,
malformed JSON, empty/overlong question and context bounds. `npm run build`,
`npm run typecheck` and `npm run lint` pass (7 pre-existing lint warnings).
The existing HTTPS alpha proof remains valid for the current route contract:
authenticated reviewer gets `GROUNDED` with citations and missing alpha/session is
rejected. Runtime validation now returns structured `INVALID_AI_REQUEST` 400 before
approved-corpus access. No transcript table/write exists in this route; public AI
remains disabled and human/DPIA approval is still pending.
