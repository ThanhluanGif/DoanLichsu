# C-072 — Human golden-set review ledger for AI eval

status: done
deps: [C-060, C-070, C-071]
implements: [ai-governance]

## Scope

Sinh ledger 500 câu human review từ dataset frozen: mỗi dòng giữ expected status, answer/citation
machine snapshot hash, verdict của reviewer 1/2, conflict note và sign-off timestamp. Generator
không tự điền reviewer/approval; validator chỉ PASS khi đủ 500 dòng có dual approval thật.

## Allowed files

- `scripts/ai-human-review-ledger.mjs`
- `artifacts/ai-eval/human-review-ledger.json`
- `artifacts/ai-eval/human-review-ledger.md`
- `cards/C-072.md`

## Verify (run these before calling the card done)

- [x] Generator creates exactly 500 pending rows with no names/signatures/fabricated approval.
- [x] Validator exits FAIL/PENDING until reviewers supply dual verdicts; machine hash matches
  current frozen dataset and report-500 artifact.

## Done-evidence (world-state proof, named BEFORE building)

`human-review-ledger.json` is a controlled handoff artifact proving the 500-question human gate
is not silently skipped; it remains `PENDING_HUMAN_REVIEW` and keeps Public Beta disabled.

## Evidence (paste the actual proof here when done)

Proof: `node scripts/ai-human-review-ledger.mjs` → `PENDING_HUMAN_REVIEW`, 500 rows, `dualApproved=0`, `noFabricatedReviewers=true`, `publicBeta=false`. Dataset/report SHA-256 values are embedded so reviewers cannot silently review a different snapshot. Artifact: `artifacts/ai-eval/human-review-ledger.json`.
