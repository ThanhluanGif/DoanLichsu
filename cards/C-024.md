# C-024 — Mô hình chương trình và API coverage lớp 6–12

status: done
deps: [C-023]
implements: [FR15]

## Scope

Thêm requirement chuẩn lớp 6–12 theo văn bản Bộ GDĐT, phân biệt bắt buộc/lựa chọn, bảng
nối nhiều-nhiều content và phép đếm `published`/`verified` trung thực. Ship API public chỉ
trả grade/topic có bài public, API admin thấy cả requirement thiếu và mapping mutation có
audit; migration/seed không tự nâng nguồn hoặc coverage thành verified.

## Allowed files

- `flow/05-contract.md`
- `migrations/0005_curriculum.sql`
- `src/data/curriculum/**`
- `src/lib/content/**`
- `src/lib/openapi/**`
- `src/app/api/v1/[locale]/curriculum/**`
- `src/app/api/v1/admin/curriculum/**`
- `src/app/api/v1/admin/contents/[id]/curriculum/**`
- `scripts/seed.ts`
- `scripts/backup.mjs`
- `scripts/restore.mjs`
- `scripts/contract-check.mjs`
- `scripts/contract-shape.*`
- `package.json`
- `tests/foundation/**`
- `tests/contract/**`
- `tests/public-api/**`
- `tests/editorial-api/**`
- `tests/e2e/public-collection-filters.spec.ts`
- `tests/recovery/recovery.test.ts`
- `cards/C-024.md`

## Verify (run these before calling the card done)

- [x] Migration/seed/recovery và contract/OpenAPI tests thoát 0; backup/restore giữ counts mới.
- [x] Requirement có official ref, grade 6–12, track, outcomes/sort; mapping nhiều-nhiều có FK.
- [x] Public arrays loại count 0; protected coverage trả missing/draft/published/verified đúng.
- [x] Negative tests chứng minh mapping/publish không tự tăng verified coverage.
- [x] Clean checkout thực sự track protected coverage route; regression test có red→green proof.

## Done-evidence (world-state proof, named BEFORE building)

Live `/docs`, public curriculum JSON và authenticated coverage JSON khớp contract; DB proof
cho một requirement thiếu và một mapping public nhưng chưa verified.

## Evidence (paste the actual proof here when done)

### Local verification

- `npm run typecheck` — exit 0.
- `npm run lint` — exit 0.
- `npm test` — `31` files / `164` tests passed.
- `npm run build` — exit 0; production route table includes
  `/api/v1/{locale}/curriculum`, `/api/v1/{locale}/curriculum/{grade}`,
  `/api/v1/admin/curriculum/coverage` and `/api/v1/admin/contents/[id]/curriculum`.
- `npm run verify:standalone` — exit 0 and includes
  `migrations/0005_curriculum.sql` plus the standalone seed bundle.
- `npx vitest run tests/recovery/recovery.test.ts tests/editorial-api/curriculum.test.ts tests/public-api/seed-openapi.test.ts`
  — `3` files / `5` tests passed. Recovery assertion retains schema version `5`,
  `curriculumRequirements=55`, and `curriculumMappings=23`; the negative editorial
  cases keep `verifiedCount=0` after mapping/publish without verified claim evidence.

### Live HTTPS verification

- URL: https://automobile-sunglasses-swimming-monitored.trycloudflare.com
- `GET /healthz` — `{"status":"ok","version":"0.1.0","database":"ok",...}`.
- `GET /openapi.json` — contains both
  `/api/v1/{locale}/curriculum` and `/api/v1/{locale}/curriculum/{grade}`.
- `GET /api/v1/vi/curriculum` — returns exactly grades `6,7,8,9,10,11,12`; each
  public grade has `publishedRequirementCount > 0`, so zero-result grades are not
  exposed in the public array.
- `GET /api/v1/vi/curriculum/12` — `requirementCount=9`,
  `publishedRequirementCount=4`, `verifiedRequirementCount=0`; every returned
  `officialProgramRef` includes the Bộ GDĐT update `17/2025/TT-BGDĐT`.
- Authenticated `GET /api/v1/admin/curriculum/coverage` — `asOf=2026-08-10`,
  grade coverage counts are:
  `6: MISSING 7/PUBLISHED 1`, `7: 3/3`, `8: 4/3`, `9: 4/2`,
  `10: 8/2`, `11: 7/2`, `12: 5/4`; all `verifiedRequirementCount=0`.

### Database proof from the running container

The read-only SQLite probe returned:

```json
{"counts":{"requirements":55,"mappings":23,"schemaVersion":5},"missing":{"id":"g6-human-origins","grade":6,"track":"MANDATORY","topic_vi":"Nguồn gốc loài người và xã hội nguyên thủy"},"mapped":{"content_id":"event-dien-bien-phu","requirement_id":"g12-vietnam-1945-1975","content_status":"PUBLISHED","verified_claims":0,"verified_evidence":0}}
```

This is the required world-state proof that a requirement can remain `MISSING`, while
a public mapping remains unverified until claims and their evidence sources are both
verified.

### Clean-checkout recovery — 2026-08-11

- Failure-world proof: GitHub Actions runs `31480640244` và `31480757227` đều dừng ở
  `npm run typecheck` với `TS2307` vì
  `src/app/api/v1/admin/curriculum/coverage/route.ts` tồn tại trên máy phát triển nhưng
  rule `coverage/` đã làm file không xuất hiện trong `git ls-files` và clean checkout.
- Regression RED trước fix:
  `npx vitest run tests/foundation/routes.test.ts` — `1 failed / 6 passed`; assertion
  `git ls-files --error-unmatch src/app/api/v1/admin/curriculum/coverage/route.ts`
  thoát lỗi đúng path bị thiếu.
- Regression GREEN sau `git add -f` route thuộc Allowed files C-024:
  cùng lệnh trả `7/7 passed`; `git ls-files --error-unmatch .../coverage/route.ts`
  in đúng route. Test giữ failure mode này để một checkout chỉ có file ignored không thể
  làm local suite xanh giả lần nữa.
- Verification tại đúng điểm lịch sử C-024: `npm run lint`, `npm run typecheck`,
  `npm run build`, `npm run verify:standalone` đều exit 0; `npm test` chạy độc lập trả
  `31/31 files`, `165/165 tests` pass. Lần chạy song song ba tiến trình đã chạm timeout
  15 giây của seed test cũ nhưng không hỏng assertion; chạy tuần tự như CI pass toàn bộ.
- Live recovery release trên DB schema 5 riêng:
  [health](https://transportation-verification-nominated-biotechnology.trycloudflare.com/healthz)
  trả HTTP 200 `database: ok`; unauthenticated
  [protected coverage](https://transportation-verification-nominated-biotechnology.trycloudflare.com/api/v1/admin/curriculum/coverage)
  trả HTTP 401 `UNAUTHENTICATED` thay vì 404; live `/openapi.json` chứa cả coverage và
  mapping route. SQLite proof: `integrity_check=ok`, migration max `5`, requirements `55`.
