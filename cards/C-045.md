# C-045 — N-008 AI answer contract và grounded model gateway

status: done
deps: [C-044]
implements: [ai-contract]

## Scope

Định nghĩa JSON answer contract và gateway deterministic dùng approved-corpus snapshot.
Gateway có query planner đơn giản theo token, trả citations tới source/locator, confidence
theo rule và abstain khi không tìm thấy claim/evidence. Không gọi model bên ngoài trong
card này; adapter model là interface có thể thay thế. Prompt injection, yêu cầu bỏ nguồn
và câu hỏi ngoài corpus phải đi vào no-answer path.

## Allowed files

- `scripts/ai-gateway.mjs`
- `package.json`
- `artifacts/ai-contract/**`
- `cards/C-045.md`

## Verify (run these before calling the card done)

- [x] Supported question returns contract-valid answer, keyPoints, citation source/locator,
      snapshot ID and policy/model versions.
- [x] Unsupported/injection question returns `ABSTAIN` with limitations and no invented
      citation/claim.
- [x] `npm run lint`, `npm run typecheck`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Two machine-readable gateway responses (grounded and abstention) demonstrate the stable
answer contract and prove model-independent, approved-only retrieval.

## Evidence (paste the actual proof here when done)

- Grounded question response passed the `ai-answer-v1` contract with key point, 2 citations containing source IDs/URLs/locators, confidence, snapshot ID and model/policy versions: `artifacts/ai-contract/grounded.json`.
- Prompt-injection response returned `ABSTAIN` with zero citations and `PROMPT_INJECTION_BLOCKED`; no model call or data mutation: `artifacts/ai-contract/abstain.json`.
- Consolidated proof: `artifacts/ai-contract/ai-contract-proof.json`.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit 0; this card does not open public AI or claim eval readiness.
