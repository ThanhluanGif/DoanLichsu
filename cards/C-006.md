# C-006 — Ship public bilingual discovery journey

status: done
deps: C-005
implements: FR1, FR2, FR3, FR4, FR5, FR6, FR7, FR12

## Scope

Triển khai journey công khai đã duyệt trong mock: home→timeline/period→content detail/source/related, browse theo type, search/filter có URL state và locale switch giữ đúng content. UI dùng đúng contract field, design tokens/vocabulary, responsive/a11y states; thêm metadata/canonical/hreflang/OG/JSON-LD, sitemap và robots cho published routes, không chứa admin logic. Chuyển contract probe tạm thời của C-005 từ sitemap rỗng sang sitemap canonical đã populate ở C-006 để suite nền tảng không tự mâu thuẫn sau khi frontend ship.

## Allowed files

- `DESIGN.md`
- `package.json`
- `package-lock.json`
- `src/app/globals.css`
- `src/app/layout.tsx`
- `src/app/page.tsx`
- `src/app/[locale]/**`
- `src/app/sitemap.*`
- `src/app/robots.*`
- `src/components/public/**`
- `src/components/icons/**`
- `src/lib/i18n/**`
- `src/lib/public-client/**`
- `scripts/contract-check.mjs`
- `public/images/**`
- `tests/public-ui/**`
- `artifacts/public-ui/.gitkeep`
- `cards/C-006.md`

## Verify (run these before calling the card done)

- [x] `npm run lint && npm run typecheck && npm test -- tests/public-ui && npm run build` exit 0; `flow.sh design` và `flow.sh tokens` không báo violation chưa xử lý.
- [x] Browser desktop + mobile hoàn thành home→timeline→Điện Biên Phủ→source→related; mọi link dùng published contract data, loading/empty/error states có copy rõ.
- [x] Search `dien bien phu`, filter EVENT/period và pagination cập nhật URL; back/forward giữ state; kết quả đầu đúng như API.
- [x] Locale switch VI↔EN giữ cùng content id; fixture thiếu EN hiển thị thông báo thay vì link 404; copy link trả canonical URL hiện tại.
- [x] Keyboard-only đi qua skip link/nav/filter/timeline/detail/source; focus visible, heading/label/alt đúng; axe trên home/search/detail không có Critical/Serious.
- [x] Curl HTML một home/detail VI/EN thấy title/description/canonical/hreflang/OG/JSON-LD; sitemap chỉ chứa published URLs và robots chặn admin.

## Done-evidence (world-state proof, named BEFORE building)

URL live `/vi`, `/vi/timeline`, `/vi/tim-kiem?q=dien+bien+phu` và cặp detail VI/EN có thể click như người dùng; screenshot desktop/mobile cùng HTML metadata/canonical thực được lưu tại `artifacts/public-ui/`.

## Evidence (paste the actual proof here when done)

READY FOR GATE — implementation, automated checks, and the required browser proof are complete; the card remains `todo` for operator close/merge.

- Standalone production tại `http://127.0.0.1:3006`: `/vi` 200 (44,397 bytes), search có q/type/period 200 (23,627 bytes), detail EN 200 (30,572 bytes), `/sitemap.xml` 200 (14,138 bytes), hero WebP 200 (228,206 bytes), robots 200 và chặn `/admin`, `/api/v1/admin`.
- `artifacts/public-ui/runtime-proof.md` lưu title/description/canonical/hreflang/OG/JSON-LD thực của home/detail VI+EN; `axe-runtime.json` ghi 5 trang HTTP thật, 0 Critical/Serious. Giới hạn được ghi trong report: JSDOM không chứng minh color contrast, responsive layout, keyboard/focus order.
- Search `dien Bien phu` + EVENT + thời kỳ trả `Chiến dịch Điện Biên Phủ`; browse chỉ theo type gọi `/contents`; locale link VI→EN là `/en/events/battle-of-dien-bien-phu`; fixture MiG-21 không có EN hiển thị thông báo và không tạo link 404.
- Contract suite sau khi sitemap được populate: 37/37 operations, 189/189 cases, 0 fail; probe sitemap đối chiếu exact canonical set từ hai public content lists và cấm API/admin URLs.
- Full verification: 11 test files, 78/78 tests; lint, typecheck, production build và `verify:standalone` exit 0. Mechanical DESIGN pass trên HTML production home/search/detail. Token audit scoped `DESIGN.md + globals.css` pass; chỉ ba font-family variables được báo informational.
- Browser automation was unavailable during the earlier proof run; the completed in-app browser run below supersedes that limitation.
- Operator approval: sau khi nhận production URLs và yêu cầu duyệt desktop/mobile, user trả lời nguyên văn `duyệt` ngày 2026-08-07; bản ghi tại `artifacts/public-ui/operator-approval.md`. Approval này đóng browser journey box nhưng không được dùng để bịa screenshot hoặc ba interaction proofs còn thiếu.

Browser evidence completed on 2026-08-07 against the production standalone:

- `artifacts/public-ui/desktop-search.png` (1440×1000) and `mobile-detail.png` (390×844) are the requested desktop/mobile screenshots. Detailed assertions are in `browser-proof.md`.
- Keyboard focus covered skip/nav/search controls/timeline/detail/copy/source/related. All observed targets were native sequential controls with `tabIndex=0` and a visible `3px solid` accent outline; refreshed axe proof remains 0 Critical/Serious across five real HTTP pages.
- Back restored the exact q/type/period controls and Điện Biên Phủ first result; Forward restored the no-period state. The run found and repaired a native-form history mismatch by switching search submission to controlled client navigation keyed by URL state.
- Copy-link changed to `Đã sao chép` and clipboard text exactly matched the current canonical detail URL. The VI→EN route kept the same record, while the missing-English MiG-21 fixture rendered its unavailable notice without a 404 link.
