# C-083 — Build reproducible security review pack

status: done
deps: [C-082]
implements: [infra]

## Scope

Create a deterministic security review pack that records repository static security
checks, auth/RBAC/security workflow tests, dependency audit summary and the current
independent-review status. It must fail closed when any static check fails and must
never label local evidence as an independent pen-test.

## Allowed files

- `scripts/security-review-pack.mjs`
- `docs/operations/security-review-pack.md`
- `tests/security/security-review-pack.test.ts`
- `package.json`
- `cards/C-083.md`
- `artifacts/security/security-review-pack.json`
- `artifacts/security/security-review-pack.md`

## Verify (run these before calling the card done)

- [x] `npm run security:review-pack` exits 0 and reports static evidence pass with
      `independentReview: PENDING_EXTERNAL`.
- [x] `npm run test:security` and the security-pack test pass.
- [x] `npm run lint` and `npm run typecheck` pass.

## Done-evidence (world-state proof, named BEFORE building)

The repository contains a timestamped security review pack with command exit codes,
artifact hashes and explicit disclosure that no independent security reviewer or
pen-test has been completed. A future named reviewer can attach their signed report
without changing the static evidence semantics.

## Evidence (paste the actual proof here when done)

`npm run security:review-pack` → `PASS_LOCAL_SECURITY_EVIDENCE`; release-contract
and auth/RBAC workflow checks pass; production dependency audit reports high/critical
`0/0`. `npm run test:security` → 2 files, 5 tests passed. The dedicated pack test
→ 1 file, 2 tests passed. `npm run lint` → pass with 7 pre-existing warnings;
`npm run typecheck` → pass. The report records
`independentReview:PENDING_EXTERNAL`, `penTest:NOT_PERFORMED`,
`publicBetaAllowed:false`; it is local evidence only and does not claim an
independent pen-test.
