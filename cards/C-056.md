# C-056 — N-006 knowledge graph relation export

status: done
deps: [C-035, C-044]
implements: [knowledge-graph]

## Scope

Xuất knowledge graph machine-readable từ content, period, place, reconstruction và
curriculum relations hiện có; chỉ dùng node public/verified source, ghi relation type,
confidence và provenance. Red graph thiếu source bị fail-closed.

## Allowed files

- `scripts/knowledge-graph.mjs`
- `package.json`
- `artifacts/knowledge-graph/**`
- `cards/C-056.md`

## Verify (run these before calling the card done)

- [x] Graph export emits nodes/edges with source IDs and confidence; red fixture fails.
- [x] `npm run lint`, `npm run typecheck`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Auditable graph JSON/Markdown artifact suitable for timeline/map/tutor relation retrieval.

## Evidence (paste the actual proof here when done)

- Graph export passed with 110 nodes and 220 `SUPPORTED_BY` edges; every edge has source ID, locator and `VERIFIED` confidence: `artifacts/knowledge-graph/graph.json`.
- Consolidated proof: `artifacts/knowledge-graph/graph-proof.json`; exporter throws on missing provenance and does not mutate DB.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit 0.
