# C-049 — N-012 teacher lesson pack và quiz export

status: done
deps: [C-038, C-048]
implements: [teacher-tools]

## Scope

Tạo generator lesson pack cho giáo viên từ approved corpus: mục tiêu học tập, hoạt động
kiểm chứng nguồn, câu hỏi quiz, đáp án có citation, thời lượng và lưu ý nhạy cảm. Xuất
Markdown/JSON offline; chỉ nhận lesson đã snapshot, không tự thêm dữ kiện hay AI-generated
claim.

## Allowed files

- `scripts/teacher-pack.mjs`
- `package.json`
- `artifacts/teacher-pack/**`
- `cards/C-049.md`

## Verify (run these before calling the card done)

- [x] Approved lesson pack emits Markdown/JSON with objectives, quiz answers and source
      locators; unsupported lesson fails closed.
- [x] `npm run lint`, `npm run typecheck`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Teacher-downloadable pack artifact tied to corpus snapshot hash, with quiz and citation
answer key; red report proves no unapproved lesson is exported.

## Evidence (paste the actual proof here when done)

- Teacher pack passed for approved `lesson-g6-why-study-history`: 2 objectives/activities, 2 quiz answers with source IDs, corpus hash and Markdown/JSON export: `artifacts/teacher-pack/lesson-pack.json`.
- Unapproved `event-dien-bien-phu` export failed closed: `artifacts/teacher-pack/red-report.json`.
- Consolidated proof: `artifacts/teacher-pack/teacher-pack-proof.json`; generator adds no AI claims.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit 0.
