# C-068 — Year-one Definition of Done audit artifact

status: done
deps: [C-067]
implements: [release-gate-audit]

## Scope

Tạo audit machine-readable đối chiếu từng nhóm Definition of Done Mục 17 với artifact hiện có.
Audit chỉ PASS khi mọi nhóm đạt; các mục external thiếu phải hiện `BLOCKED_EXTERNAL` với
link tới ledger. Không thay đổi release status và không tuyên bố Public Beta hoàn tất.

## Allowed files

- `scripts/dod-audit.mjs`
- `artifacts/release/dod-audit.json`
- `artifacts/release/dod-audit.md`
- `cards/C-068.md`

## Verify (run these before calling the card done)

- [x] `node scripts/dod-audit.mjs` exits 1 with overall `NOT_READY` and enumerates every
  unmet external condition; no fabricated approvals/users/domains.
- [x] Audit marks implemented product/content/AI/operations evidence separately from external
  blockers and links the source artifacts.

## Done-evidence (world-state proof, named BEFORE building)

`artifacts/release/dod-audit.json` is the final handoff gate: it proves what is built, what is
verified, and why Public Beta must remain closed until authorized external evidence arrives.

## Evidence (paste the actual proof here when done)

Proof: `node scripts/dod-audit.mjs` → `NOT_READY`, 7 checks represented, 11 external blockers, `publicBeta=false`, `noFabricatedEvidence=true`. Full report is `artifacts/release/dod-audit.json` and handoff markdown is adjacent. The audit links product smoke, 46/46 coverage, 500/500 machine eval, disposable recovery, governance and external ledger evidence.
