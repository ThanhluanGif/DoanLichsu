# C-105 — Build a year-one DoD completion matrix

status: done
deps: C-104
implements: infra

## Scope

Create a requirement-by-requirement completion matrix for Section 17 of the
12-month plan. Each row must name the requirement, authoritative evidence path,
current state, and the exact missing external condition where applicable. Derive
the external rows from the canonical ledger; do not infer completion from nearby
tests or fabricate owners/approvals.

## Allowed files

scripts/year-one-dod-matrix.mjs
tests/operations/year-one-dod-matrix.test.ts
artifacts/release/year-one-dod-matrix.json
artifacts/release/year-one-dod-matrix.md
cards/C-105.md

## Verify (run these before calling the card done)

- [x] `node scripts/year-one-dod-matrix.mjs` exits 0 and reports every Section 17
      group, with 11 external rows still `BLOCKED_EXTERNAL`.
- [x] `npm test -- --run tests/operations/year-one-dod-matrix.test.ts` passes.
- [x] `npm run typecheck` passes.

## Done-evidence (world-state proof, named BEFORE building)

The matrix artifact explicitly shows which product/content/rights/AI/quality/
operations requirements are evidenced locally and which 11 Public Beta conditions
remain externally blocked, with no fabricated evidence claim.

## Evidence (paste the actual proof here when done)

`node scripts/year-one-dod-matrix.mjs` exited 0 with 17 mapped Section 17 rows,
`overall: NOT_READY`, `publicBeta: false`, and 11 `BLOCKED_EXTERNAL` rows.
Focused test passed (1 test); `npm run typecheck` passed. The matrix retains
evidence paths and marks external owner/artifact fields as null; no gate was
promoted.
