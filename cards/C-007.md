# C-007 — Ship editorial governance journey trong admin UI

status: done
deps: C-006
implements: FR8, FR9, FR10, FR11, FR13

## Scope

Triển khai journey quản trị đã duyệt trong mock: login, dashboard/list, object-first content editor VI/EN, source/media metadata, preview, gửi duyệt, review/reject/approve/publish, users và audit log theo role. UI không tự quyết quyền, chỉ phản ánh 401/403/409/422 từ contract; form giữ ≤6 field chính trước “Tùy chọn thêm”, cảnh báo unsaved/stale và dùng copy Việt tự nhiên thay engine words.

## Allowed files

- `DESIGN.md`
- `package.json`
- `package-lock.json`
- `src/app/globals.css`
- `src/app/admin/**`
- `src/components/admin/**`
- `src/components/forms/**`
- `src/components/icons/**`
- `src/lib/admin-client/**`
- `src/lib/i18n/**`
- `tests/admin-ui/**`
- `artifacts/admin-ui/**`
- `cards/C-007.md`

## Verify (run these before calling the card done)

- [x] `npm run lint && npm run typecheck && npm test -- tests/admin-ui && npm run build` exit 0; design/tokens checks không có violation chưa xử lý.
- [x] Browser login Editor→tạo nội dung VI, thêm EN/source/media URL, preview, submit; Reviewer→reject có lý do→approve/publish; public detail xuất hiện sau publish và audit ghi actor/action.
- [x] Editor không thấy/call được publish/users; Reviewer không thấy/call được users; Admin thấy user/audit; server 403 được render không leak stack/secret.
- [x] Editor form có ≤6 field chính, “Tùy chọn thêm” giữ metadata còn lại; unsaved navigation có cảnh báo, stale version hiện conflict không ghi đè im lặng, validation bám đúng field.
- [x] Keyboard/axe trên login/list/editor/review không có Critical/Serious; mobile 390 px không tràn ngang và sticky savebar không che field/CTA.
- [x] Refresh giữ session, logout xóa session và back không mở lại admin data; failed login copy không tiết lộ email tồn tại.

## Done-evidence (world-state proof, named BEFORE building)

URL live `/admin/login`, `/admin/contents`, một editor và review queue hoàn thành create→publish qua browser; public URL mới xuất hiện, audit row/DB state và screenshot từng role được lưu tại `artifacts/admin-ui/`.

## Evidence (paste the actual proof here when done)

- Production standalone: `http://127.0.0.1:3001/admin/login`, `http://127.0.0.1:3001/admin/contents`, editor `http://127.0.0.1:3001/admin/contents/3f0b04c6-4edd-4001-af17-98196d79cd57`.
- Published VI detail: `http://127.0.0.1:3001/vi/su-kien/chien-dich-kiem-chung-c-007-lan-hai` returned HTTP 200; EN alternate also opened from the public page.
- DB row: node `3f0b04c6-4edd-4001-af17-98196d79cd57` is `PUBLISHED`, version 10, `updated_by=user-reviewer`; both VI/EN translations are `PUBLISHED`.
- Audit rows prove Editor submit → Reviewer reject → Editor resubmit → Reviewer approve → Reviewer publish. Editor publish and Reviewer users calls returned safe 403 `FORBIDDEN`; known/unknown failed logins returned identical 401 copy.
- Browser evidence and exact interactions: `artifacts/admin-ui/browser-proof.md`. Screenshots: `admin-audit-desktop.png`, `reviewer-desktop.png`, `editor-mobile.png`; hydrated DOM + axe report are stored beside them.
- Verify output after semantic-review fixes: lint/typecheck/build passed; `tests/admin-ui` 11/11 passed; final runtime axe reported `{"pages":4,"criticalOrSerious":0}`. Four mechanical DESIGN scans passed; source token comparison matched all 12 declared tokens with no missing/mismatch.
- Final repair proof on draft `2374a306-ae3e-4168-8a89-321ad7c357c4`: EN saved with SEO disclosure closed and defaulted SEO title/description; dirty sidebar navigation opened a native confirm; editor/audit/review each exposed exactly one selected nav item.
