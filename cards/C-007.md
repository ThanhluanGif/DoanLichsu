# C-007 — Ship editorial governance journey trong admin UI

status: todo
deps: C-006
implements: FR8, FR9, FR10, FR11, FR13

## Scope

Triển khai journey quản trị đã duyệt trong mock: login, dashboard/list, object-first content editor VI/EN, source/media metadata, preview, gửi duyệt, review/reject/approve/publish, users và audit log theo role. UI không tự quyết quyền, chỉ phản ánh 401/403/409/422 từ contract; form giữ ≤6 field chính trước “Tùy chọn thêm”, cảnh báo unsaved/stale và dùng copy Việt tự nhiên thay engine words.

## Allowed files

- `DESIGN.md`
- `package.json`
- `package-lock.json`
- `src/app/globals.css`
- `src/app/admin/**`
- `src/components/admin/**`
- `src/components/forms/**`
- `src/components/icons/**`
- `src/lib/admin-client/**`
- `src/lib/i18n/**`
- `tests/admin-ui/**`
- `artifacts/admin-ui/.gitkeep`
- `cards/C-007.md`

## Verify (run these before calling the card done)

- [ ] `npm run lint && npm run typecheck && npm test -- tests/admin-ui && npm run build` exit 0; design/tokens checks không có violation chưa xử lý.
- [ ] Browser login Editor→tạo nội dung VI, thêm EN/source/media URL, preview, submit; Reviewer→reject có lý do→approve/publish; public detail xuất hiện sau publish và audit ghi actor/action.
- [ ] Editor không thấy/call được publish/users; Reviewer không thấy/call được users; Admin thấy user/audit; server 403 được render không leak stack/secret.
- [ ] Editor form có ≤6 field chính, “Tùy chọn thêm” giữ metadata còn lại; unsaved navigation có cảnh báo, stale version hiện conflict không ghi đè im lặng, validation bám đúng field.
- [ ] Keyboard/axe trên login/list/editor/review không có Critical/Serious; mobile 390 px không tràn ngang và sticky savebar không che field/CTA.
- [ ] Refresh giữ session, logout xóa session và back không mở lại admin data; failed login copy không tiết lộ email tồn tại.

## Done-evidence (world-state proof, named BEFORE building)

URL live `/admin/login`, `/admin/contents`, một editor và review queue hoàn thành create→publish qua browser; public URL mới xuất hiện, audit row/DB state và screenshot từng role được lưu tại `artifacts/admin-ui/`.

## Evidence (paste the actual proof here when done)

(empty until done)
