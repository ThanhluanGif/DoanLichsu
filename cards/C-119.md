# C-119 — Prepare non-fabricating published-history remediation plan

status: done
deps: C-118
implements: infra

## Scope

Add a deterministic, read-only remediation-plan generator for the 105 published
content rows that lack an editorial audit-history entry. The plan may derive
candidate provenance fields from existing database metadata, but it must mark
every candidate `REQUIRES_HUMAN_REVIEW`, must never write to the database, and
must not change the existing `BLOCKED_INTERNAL_AUDIT` result.

## Allowed files

scripts/published-content-history-plan.mjs
tests/operations/published-content-history-plan.test.ts
artifacts/curriculum-completeness/published-content-history-plan.json
artifacts/curriculum-completeness/published-content-history-plan.md
cards/C-119.md

## Verify (run these before calling the card done)

- [x] `node scripts/published-content-history-plan.mjs` exits 0 and reports
      105 candidates, zero database writes, and `REQUIRES_HUMAN_REVIEW`.
- [x] The plan contains no fabricated actor/approval claim and the existing
      published-content audit remains `BLOCKED_INTERNAL_AUDIT` with 105 missing.
- [x] Focused remediation-plan test and `npm run typecheck` pass.

## Done-evidence (world-state proof, named BEFORE building)

An auditable JSON/Markdown repair plan is present for every affected published
content row, explicitly requiring human review before any history is recorded;
the live release audit still refuses Public Beta readiness until that review is
completed.

## Evidence (paste the actual proof here when done)

Verified 2026-08-13:

- `node scripts/published-content-history-plan.mjs` exited 0 with
  `{"status":"REQUIRES_HUMAN_REVIEW","publishedContent":105,"candidateCount":105,"databaseWrites":0}`.
- The generated JSON/Markdown queue has 105 candidates; every candidate is
  `REQUIRES_HUMAN_REVIEW`, with `fabricatedApproval: false`.
- `node scripts/published-content-audit.mjs` still reports
  `BLOCKED_INTERNAL_AUDIT`, `publishedContent: 105`, `completeContent: 0`,
  and `missingContent: 105`.
- `npx vitest run tests/operations/published-content-history-plan.test.ts`
  passed: 1 file, 1 test.
- `npm run typecheck` exited 0.
