# C-129 — Freeze final DoD and matrix audit snapshot

status: done
deps: C-127, C-128
implements: infra

## Scope

Generate the final DoD audit and matrix-consistency snapshot after the completed
history-review workflow and current-head evidence. Preserve `NOT_READY`, Public
Beta false, `BLOCKED_INTERNAL` for 105 published-history records, and all 11
external blockers.

## Allowed files

artifacts/release/dod-audit.json
artifacts/release/dod-audit.md
artifacts/release/dod-matrix-consistency.json
artifacts/release/dod-matrix-consistency.md
cards/C-129.md

## Verify (run these before calling the card done)

- [x] `node scripts/dod-audit.mjs` and `node scripts/dod-matrix-consistency.mjs`
      exit 0; DoD is `NOT_READY`, consistency is true, and current-head evidence
      remains `PASS_LOCAL_ONLY` with matching hashes.
- [x] Snapshot retains 11 external blockers and 105 internal history gaps.

## Done-evidence (world-state proof, named BEFORE building)

The final machine-readable DoD snapshot is internally coherent and ready for
handoff to the real reviewers, operators, partners, and pilot owners.

## Evidence (paste the actual proof here when done)

Verified 2026-08-14:

- `node scripts/dod-audit.mjs` exited 0 with `NOT_READY`, 15 checks, 11 unmet
  external blockers; current-head evidence is `PASS_LOCAL_ONLY` with matching
  source hashes.
- `node scripts/dod-matrix-consistency.mjs` exited 0 with `consistent: true`
  for overall status, Public Beta, and external blocker list.
- Snapshot retains `published-content-history: BLOCKED_INTERNAL` with
  `missingContent: 105`; no attestations were fabricated.
