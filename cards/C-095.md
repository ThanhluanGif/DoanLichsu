# C-095 — Bind production readiness to current release source

status: done
deps: C-094
implements: infra

## Scope

Require the production-readiness report to verify that the current checked-out
source tree matches the source-tree hash recorded by current release evidence.
Keep local evidence distinct from official production: a hash mismatch blocks the
local quality check, while a matching hash remains `PASS_LOCAL_ONLY` and does not
unlock Public Beta.

## Allowed files

scripts/production-readiness.mjs
tests/operations/production-readiness.test.ts
artifacts/production-readiness/report.json
artifacts/production-readiness/report.md
artifacts/release/current-head-evidence.json
artifacts/release/current-head-evidence.md
cards/C-095.md

## Verify (run these before calling the card done)

- [x] `npm test -- --run tests/operations/production-readiness.test.ts` passes and
      reports `PASS_LOCAL_ONLY` only when the release source hash matches the
      current source tree.
- [x] `npm run typecheck` passes.
- [x] `node scripts/production-readiness.mjs --database data/quan-su-viet.db`
      exits 0 with `publicBetaAllowed: false` and all 11 external blockers.

## Done-evidence (world-state proof, named BEFORE building)

The readiness artifact exposes `quality.sourceTreeSha256` and
`quality.currentSourceTreeSha256`; they match on the current release, while the
report remains `PASS_LOCAL_ONLY`, `officialProductionEvidence: false`, and
`publicBetaAllowed: false`.

## Evidence (paste the actual proof here when done)

`npm run release:evidence:current` passed with quality `[0,0,0,0]`, local
health/OpenAPI/search `200/200/200`, security headers true, and backup/restore
checksums true. `npm test -- --run tests/operations/production-readiness.test.ts`:
1 test passed; `npm run typecheck` passed. The readiness report records matching
source hashes `335ef0d66e2db948a6b0040d2d2987284cf560daf03d18927a7fdc386e4bb226`,
`PASS_LOCAL_ONLY`, `publicBetaAllowed: false`, and 11 external blockers.
