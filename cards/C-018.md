# C-018 — Điểm vào không gian biên tập từ website công khai

status: done
deps: [C-017]
implements: none

## Scope

Thêm một điểm vào nhẹ ở footer công khai VI/EN dẫn tới `/admin/login`, kèm giải thích
rằng đây là không gian dành cho đội ngũ biên tập và tài khoản được quản trị viên cấp,
không có đăng ký công khai. Giữ liên kết ngoài primary navigation, không thêm signup,
không đổi auth/RBAC, API, session, database, sitemap hoặc admin workflow.

## Allowed files

- `src/components/public/PublicShell.tsx`
- `src/app/globals.css`
- `tests/public-ui/editorial-entry.test.ts`
- `tests/e2e/public-editorial-entry.spec.ts`
- `artifacts/editorial-entry/**`
- `cards/C-018.md`

## Verify (run these before calling the card done)

- [x] `npm run lint`, `npm run typecheck`, `npm test` và `npm run build` đều thoát 0.
- [x] Footer VI/EN có đúng một liên kết tới `/admin/login`, copy nói rõ tài khoản nội bộ
  do Admin cấp và không tạo route/link/API signup.
- [x] Pointer và keyboard mở trang đăng nhập; back trở về đúng public URL, còn truy cập
  `/admin` khi chưa có session vẫn chuyển về `/admin/login`.
- [x] `/admin/login` vẫn `noindex`, sitemap không chứa `/admin`; public/admin API contract,
  cookie và RBAC không đổi.
- [x] Desktop/mobile không tràn ngang, axe không có Critical/Serious, LCP ≤ 2,5 giây;
  footer dùng token, 1px border, typography và affordance hiện có.

## Done-evidence (world-state proof, named BEFORE building)

- URL HTTPS `/vi` và `/en` hiển thị entry footer thật; pointer/keyboard đi tới
  `/admin/login`, back quay lại đúng public page và không có public signup.
- `artifacts/editorial-entry/editorial-entry-proof.json` ghi href/copy VI/EN,
  pointer/keyboard/back, redirect bảo vệ `/admin`, noindex/sitemap, accessibility,
  overflow và LCP.
- `artifacts/editorial-entry/editorial-entry-desktop.png` và
  `artifacts/editorial-entry/editorial-entry-mobile.png` chụp footer thật.

## Evidence (paste the actual proof here when done)

- Live HTTPS: `https://forecast-bachelor-sacrifice-elliott.trycloudflare.com`.
- Footer `/vi` và `/en` có đúng một entry `/admin/login`; copy VI/EN nói rõ tài khoản
  do quản trị viên cấp và không có đăng ký công khai.
- Playwright live `tests/e2e/public-editorial-entry.spec.ts`: 1/1 pass; pointer VI,
  keyboard EN, back về đúng locale và `/admin` chưa đăng nhập chuyển tới `/admin/login`.
- `/admin/login` có `noindex`; sitemap không chứa `/admin`; `/register` và
  `/admin/register` đều trả 404.
- `npm run lint`, `npm run typecheck`, `npm test` (25 files, 126 tests) và
  `npm run build`: exit 0.
- Proof: `artifacts/editorial-entry/editorial-entry-proof.json`; axe Critical/Serious = 0,
  không overflow; LCP desktop 1644 ms, mobile 924 ms.
- Screenshots: `artifacts/editorial-entry/editorial-entry-desktop.png` và
  `artifacts/editorial-entry/editorial-entry-mobile.png`.
