# C-147 — Stabilize standalone smoke timeout for CI

status: done
deps: C-146
implements: infra

## Scope

Raise only the Vitest timeout for the standalone production-like smoke test
from the default 5 seconds to a bounded 30 seconds, matching its existing
child-process timeout. Preserve the health/content assertions and fail-closed
behavior. Prove the bare `npm test` command passes, then refresh the current
release provenance and Global Build Master without changing any external gate
to PASS.

## Allowed files

tests/operations/local-standalone-smoke.test.ts
KE_HOACH_12_THANG_CONG_TRI_THUC_LICH_SU_VIET_NAM_AI.md
artifacts/release/current-head-evidence.json
artifacts/release/current-head-evidence.md
artifacts/production-readiness/report.json
artifacts/production-readiness/report.md
artifacts/release/dod-audit.json
artifacts/release/dod-audit.md
artifacts/release/dod-matrix-consistency.json
artifacts/release/dod-matrix-consistency.md
artifacts/release/year-one-dod-matrix.json
artifacts/release/year-one-dod-matrix.md
artifacts/transparency/dashboard.json
artifacts/transparency/dashboard.md
artifacts/operations/report.json
artifacts/operations/report.md
artifacts/operations/external-evidence-handoff.json
artifacts/operations/external-evidence-handoff.md
artifacts/operations/external-evidence-verification.json
artifacts/operations/external-evidence-verification.md
artifacts/operations/local-standalone-smoke.json
artifacts/operations/local-standalone-smoke.md
artifacts/operations/external-evidence-intake.json
artifacts/operations/external-evidence-intake.md
cards/C-147.md

## Verify (run these before calling the card done)

- [x] Bare `npm test` passes, including
      `tests/operations/local-standalone-smoke.test.ts`, with no timeout.
- [x] `npm run typecheck` and `npm run lint -- --quiet` pass; the smoke test
      still asserts all nine route/safety checks.
- [x] Current-head and dependent release artifacts share one source hash; DoD
      remains `NOT_READY`, Public Beta false, 105 internal gaps and 11 external
      blockers.
- [x] No production, Council, pilot, rights, DPIA or security approval is
      added by this test-only change.

## Done-evidence (world-state proof, named BEFORE building)

The CI-equivalent bare test command completes the local production-like smoke
within its explicit bounded timeout, and the release artifacts identify the
new source tree while retaining every real-world blocker.

## Evidence (paste the actual proof here when done)

Verified 2026-08-14 after implementation:

- The initial bare `npm test` run correctly exposed four stale-release-artifact
  assertions after the test file changed; the standalone smoke itself no
  longer timed out. After regenerating current-head/readiness/DoD/matrix/
  transparency artifacts, the exact CI command `npm test` passed **72 test
  files / 260 tests** with no timeout or failure.
- The change is limited to an explicit `30_000` Vitest timeout on
  `tests/operations/local-standalone-smoke.test.ts`; its nine route and AI
  safety assertions remain unchanged. `npm run typecheck` and
  `npm run lint -- --quiet` passed.
- Current-head release evidence now records source SHA-256
  `2e749652213fd2cda745ae575dda1701f4dbc825d36658fab96d1fc717cedf9e`, with
  lint/typecheck/test/build/migrate/seed/backup/restore exit code 0 and local
  health/OpenAPI/search `200/200/200`.
- Operations is `PASS`; intake is `PASS_INTAKE_SCHEMA` with 11 pending and
  `releaseAllowed: false`; DoD is `NOT_READY`, Public Beta false, matrix
  consistency true and 11 external blockers. Global Master v2.9 records
  147/147 cards done and late M11; no external approval was invented.
