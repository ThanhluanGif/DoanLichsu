# C-113 — Capture reproducible local standalone route smoke

status: done
deps: C-112
implements: infra

## Scope

Add a reproducible local standalone smoke script using Node fetch and an absolute
database path. It must start/observe the production-like bundle, verify public
HTML/API/privacy/transparency/sitemap/health/OpenAPI/search routes, record the
expected AI `403 AI_BETA_DISABLED` boundary, and explicitly disclose that this is
not official HTTPS production evidence.

## Allowed files

scripts/local-standalone-smoke.mjs
tests/operations/local-standalone-smoke.test.ts
artifacts/operations/local-standalone-smoke.json
artifacts/operations/local-standalone-smoke.md
cards/C-113.md

## Verify (run these before calling the card done)

- [x] `node scripts/local-standalone-smoke.mjs` exits 0 with all required routes
      passing and AI beta disabled.
- [x] `npm test -- --run tests/operations/local-standalone-smoke.test.ts` passes.
- [x] `npm run typecheck` passes.

## Done-evidence (world-state proof, named BEFORE building)

The smoke artifact records local HTTP 200 for public routes and AI HTTP 403 with
`AI_BETA_DISABLED`, absolute database path, source release context, and an explicit
non-production disclosure.

## Evidence (paste the actual proof here when done)

`node scripts/local-standalone-smoke.mjs` produced `PASS_LOCAL_ONLY` with 9/9
checks, absolute database path, public HTML/API/privacy/transparency/sitemap/
health/OpenAPI/search status 200, and AI `403 AI_BETA_DISABLED`. Focused test
passed (1 test); typecheck passed. Artifact explicitly discloses local-only scope
and the five external limitations.
