# C-142 — Add correction moderation queue and audited transitions

status: done
deps: C-141
implements: FR23

## Scope

Complete the internal moderation half of FR23: add contract-first admin
endpoints to list correction reports and transition them through the policy
states with role checks, optimistic versioning, allowed-transition validation
and actor-attributed audit logs. Add the reviewer/admin queue page with filters,
SLA/overdue visibility, evidence link and explicit reason fields. No transition
may alter public content or claim a Council decision without a real reviewer.

## Allowed files

flow/03-prd.md
flow/05-contract.md
src/lib/corrections/repository.ts
src/app/api/v1/admin/corrections/route.ts
src/app/api/v1/admin/corrections/[id]/transition/route.ts
src/components/admin/CorrectionQueue.tsx
src/app/admin/corrections/page.tsx
src/lib/admin-client/navigation.ts
src/lib/admin-client/types.ts
src/components/admin/AdminShell.tsx
src/app/globals.css
src/lib/openapi/editorial-catalog.ts
tests/editorial-api/corrections-moderation.test.ts
tests/admin-ui/static-contract.test.ts
tests/public-api/seed-openapi.test.ts
cards/C-142.md

## Verify (run these before calling the card done)

- [x] Contract consistency maps FR23 to both correction admin endpoints and
      OpenAPI exposes exact request/response/role shapes.
- [x] Reviewer/admin list is paginated/filterable; transition API enforces
      legal state changes, role restrictions, stale-version conflicts and audit
      metadata containing actor/state/reason without PII.
- [x] Admin queue renders evidence locator, SLA/overdue state, and controls
      that require a reason; Editor cannot perform reviewer-only final states.
- [x] Focused moderation/static/OpenAPI tests, typecheck, lint and production
      build pass.

## Done-evidence (world-state proof, named BEFORE building)

On a production-like local run an authenticated Reviewer/Admin can inspect a
correction, move it through a permitted moderation state, and see the actor,
version and reason in the audit log. Public content remains unchanged and no
approval or real-world resolution is fabricated.

## Evidence (paste the actual proof here when done)

Verified 2026-08-14 after implementation:

- Contract/PRD consistency passed; OpenAPI 3.1 exposes `GET
  /api/v1/admin/corrections` and `POST
  /api/v1/admin/corrections/{id}/transition`, including `AdminCorrectionView`
  `overdue`, correction input fields, role boundary and transition response.
- Focused regression passed: 4 files / 13 tests. `npm run typecheck`,
  `npm run lint -- --quiet`, and `npm run build` all exited 0. The build route
  table includes `/admin/corrections`, both admin correction endpoints and the
  existing public `/api/v1/corrections` endpoint.
- Disposable standalone HTTP proof passed against a temporary database:
  `health: 200`, public correction create `201`, reviewer login `200`, admin
  list `200` with `listCount: 1`, transition `200` to `TRIAGED`, and
  `reporterStored: false`.
- The focused integration test also verified Editor transition to a reviewer
  state returns `403`, stale version returns `409`, legal reviewer transitions
  reach `CORRECTED` at version 4, and three actor-attributed
  `correction.transition` audit rows are written without reporter identity.
- A full-suite rerun reached 70/71 files and 256/257 tests before one
  pre-existing `tests/operations/local-standalone-smoke.test.ts` 5-second
  harness timeout; this card's focused suite, typecheck, lint, build and live
  moderation proof are green. No public content or Council approval was
  changed or fabricated.
