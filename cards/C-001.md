# C-001 — Khóa visual contract bằng static UI mock

status: done
deps: none
implements: none

## Scope

Tạo một static HTML mock không có logic/framework cho bốn surface quyết định ngôn ngữ thị giác của v1: home/timeline, content detail, search state và admin editor/review. Mock dùng copy Việt/Anh thật, các object/content thật, token/vocabulary đã sửa trong `DESIGN.md`, đủ desktop + mobile để các frontend card sau chỉ triển khai lại visual contract đã duyệt.

## Allowed files

- `DESIGN.md`
- `mockups/**`
- `artifacts/mockups/**`
- `cards/C-001.md`

## Verify (run these before calling the card done)

- [x] `bash /Users/admin/.agents/skills/flow/runner/flow.sh design mockups/index.html` exit 0.
- [x] Phục vụ `mockups/` tại `http://127.0.0.1:4173`; HTML và hero asset đều trả HTTP 200.
- [x] Operator mở mock và xác nhận rõ “duyệt mock”; responsive CSS giữ navigation, timeline, source list và form ở breakpoint 390 px mà không tạo tràn ngang cố ý.
- [x] Kiểm tra semantic theo `DESIGN.md`: không emoji/engine word/raw JSON; form ≤6 field trước disclosure; token/affordance/object-first đúng; editor và reviewer states đều có.

## Done-evidence (world-state proof, named BEFORE building)

URL browser `http://127.0.0.1:4173` hiển thị static mock responsive và operator approval được ghi nguyên văn trong `artifacts/mockups/operator-approval.md`; HTTP proof và review findings/repairs là bằng chứng bổ sung.

## Evidence (paste the actual proof here when done)

- Operator approval: user trả lời nguyên văn `duyệt mock` ngày 06/08/2026; bản ghi tại `artifacts/mockups/operator-approval.md`.
- Live mock: `http://127.0.0.1:4173/` → `HTTP/1.0 200 OK`, `Content-Length: 16781`.
- Responsive assets: `hero-history.webp` → 200, 228206 bytes; `hero-history-mobile.webp` → 200, 77362 bytes; fallback PNG 1536×1024.
- Mechanical design: `PASS: no mechanical DESIGN violations`; token audit PASS với toàn bộ color/gradient tokens khớp `DESIGN.md` (chỉ báo thông tin ba font-family variables).
- Static assertions: PASS cho radio values, VI/EN tab-panel mapping, distinct Reviewer + reject/publish-block states, preserved mobile navigation và responsive image sources.
- Adversarial review repaired: navigation mobile, filter values, image weight, token drift, locale semantics và missing review surface. Claude CLI chưa đăng nhập; Antigravity probe trả `FLOWPONG` nhưng review trả 503, nên không dùng kết quả rỗng làm approval; escalation đã ghi vào flow harness.
- Browser runtime không có browser nên không bịa screenshot; operator-viewed approval là gate bằng chứng theo luật UI mock.
