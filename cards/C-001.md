# C-001 — Khóa visual contract bằng static UI mock

status: todo
deps: none
implements: none

## Scope

Tạo một static HTML mock không có logic/framework cho bốn surface quyết định ngôn ngữ thị giác của v1: home/timeline, content detail, search state và admin editor/review. Mock dùng copy Việt/Anh thật, các object/content thật, token/vocabulary đã sửa trong `DESIGN.md`, đủ desktop + mobile để các frontend card sau chỉ triển khai lại visual contract đã duyệt.

## Allowed files

- `DESIGN.md`
- `mockups/**`
- `cards/C-001.md`

## Verify (run these before calling the card done)

- [ ] `bash /Users/admin/.agents/skills/flow/runner/flow.sh design mockups/index.html` exit 0.
- [ ] Phục vụ `mockups/` tại `http://127.0.0.1:4173` và mở bằng browser ở 1440×900; home, detail, search và admin sections không vỡ layout.
- [ ] Mở lại ở viewport 390×844; navigation, timeline, source list và form vẫn đọc/điều hướng được, không tràn ngang.
- [ ] Kiểm tra semantic theo `DESIGN.md`: không emoji/engine word/raw JSON; form ≤6 field trước disclosure; token/affordance/object-first đúng.

## Done-evidence (world-state proof, named BEFORE building)

URL browser `http://127.0.0.1:4173` hiển thị static mock responsive đã được visual QA ở desktop/mobile; screenshot thật của ít nhất home và admin editor được lưu trong `artifacts/mockups/`.

## Evidence (paste the actual proof here when done)

(empty until done)
