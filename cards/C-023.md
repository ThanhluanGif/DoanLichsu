# C-023 — Facet theo ngữ cảnh không có lựa chọn rỗng

status: todo
deps: [C-022]
implements: [FR16]

## Scope

Đưa collection, timeline và search sang `FacetView` được đếm ở server từ đúng tập kết quả
đã publish. Mọi option trả về có `publishedCount > 0`; thay filter ghi URL ổn định và
back/forward/copy-link phục hồi cùng kết quả. Grade/topic trả mảng rỗng có đúng shape cho
đến khi C-024 cung cấp curriculum data.

## Allowed files

- `flow/05-contract.md`
- `src/lib/content/public-repository.ts`
- `src/lib/public-client/**`
- `src/app/api/v1/[locale]/taxonomies/route.ts`
- `src/app/[locale]/timeline/page.tsx`
- `src/app/[locale]/[kind]/page.tsx`
- `src/app/[locale]/search/page.tsx`
- `src/components/public/SearchForm.tsx`
- `tests/public-api/**`
- `tests/public-ui/**`
- `tests/e2e/public-collection-filters.spec.ts`
- `artifacts/contextual-facets/**`
- `cards/C-023.md`

## Verify (run these before calling the card done)

- [ ] Contract/OpenAPI tests khớp `FacetQuery`/`FacetView`; lint/typecheck/test/build thoát 0.
- [ ] API collection/timeline/search không trả option count 0 trong từng scope/type.
- [ ] E2E desktop/mobile xác nhận keyboard, back, forward và copy-link giữ filter/kết quả.

## Done-evidence (world-state proof, named BEFORE building)

HTTPS collection và timeline chỉ có option có dữ liệu; JSON proof ghi URL trước/sau,
counts, keyboard/back/forward/copy-link và screenshot desktop/mobile.

## Evidence (paste the actual proof here when done)

(empty until done)
