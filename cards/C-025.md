# C-025 — Danh mục công khai Học theo lớp

status: done
deps: [C-024]
implements: [FR15]

## Scope

Thêm navigation và route `/{locale}/hoc-theo-lop` + grade detail dùng curriculum API.
Public chỉ hiển thị lớp/chủ đề có lesson published, chuyên đề lựa chọn được gắn nhãn riêng,
coverage summary không dùng chữ “đầy đủ” khi verified requirement chưa đạt 100%.

## Allowed files

- `DESIGN.md`
- `src/lib/i18n/config.ts`
- `src/lib/public-client/**`
- `src/components/public/**`
- `src/app/[locale]/hoc-theo-lop/**`
- `src/app/[locale]/learn-by-grade/**`
- `src/app/sitemap.ts`
- `src/app/globals.css`
- `tests/public-ui/**`
- `tests/e2e/curriculum-catalog.spec.ts`
- `artifacts/curriculum-catalog/**`
- `cards/C-025.md`

## Verify (run these before calling the card done)

- [x] Lint/typecheck/tests/build thoát 0; sitemap/metadata/hreflang đúng VI/EN.
- [x] Không grade/topic rỗng; mandatory/elective và published/verified không bị trộn.
- [x] Desktop/mobile/keyboard/axe và back/forward/copy-link qua grade/topic đều đạt.

## Done-evidence (world-state proof, named BEFORE building)

HTTPS `/vi/hoc-theo-lop` và `/en/learn-by-grade` mở được từ header, chỉ hiện mục có bài;
screenshots/proof JSON ghi counts, locale, keyboard, a11y và URL history.

## Evidence (paste the actual proof here when done)

### Local verification

- `npm run typecheck` — exit 0.
- `npm run lint` — exit 0 (sau khi loại artifact trace tương thích tạm khỏi thư mục lint).
- `npm test` — `32` test files / `169` tests passed.
- `npm run build` — exit 0; production route table includes `/[locale]/hoc-theo-lop`,
  `/[locale]/hoc-theo-lop/[grade]`, `/[locale]/learn-by-grade` and
  `/[locale]/learn-by-grade/[grade]`, plus the public curriculum APIs.
- `npm run verify:standalone` — exit 0; standalone server/static assets, migration
  `0005_curriculum.sql` and bundled seed/migrate scripts are present.
- `git diff --check` — exit 0.
- `flow.sh design` — PASS for the five new public curriculum route/component files;
  no mechanical DESIGN violations.

### Live HTTPS verification

- URL: https://automobile-sunglasses-swimming-monitored.trycloudflare.com
- `GET /healthz` — HTTP 200, `{"status":"ok","version":"0.1.0","database":"ok"}`.
- `GET /vi/hoc-theo-lop` — HTTP 200; heading `Học lịch sử theo lớp` and grade cards
  render from the public curriculum response.
- `GET /en/learn-by-grade` — HTTP 200; heading `Learn history by grade` and grade
  cards render from the English public curriculum response.
- `GET /api/v1/vi/curriculum/12` — `requirementCount=9`,
  `publishedRequirementCount=4`, `verifiedRequirementCount=0`; all four visible
  requirements have `publishedCount > 0`, lessons, separate `MANDATORY` track,
  official programme references (including Thông tư 17/2025/TT-BGDĐT) and required
  outcomes. No `MISSING` requirement is rendered publicly.
- `/sitemap.xml` contains `/vi/hoc-theo-lop`, `/en/learn-by-grade` and grade routes
  `6–12` for both locales; route generation filters on both published requirements
  and published lessons.

### Browser proof

The target Playwright E2E passed:

```text
E2E_BASE_URL=https://automobile-sunglasses-swimming-monitored.trycloudflare.com \
npx playwright test tests/e2e/curriculum-catalog.spec.ts
1 passed (24.4s)
```

Proof JSON: `artifacts/curriculum-catalog/curriculum-proof.json`

```json
{
  "publishedGrades": [6, 7, 8, 9, 10, 11, 12],
  "gradeDetail": {"grade": 12, "visibleRequirements": 4, "missingVisibleRequirements": 0, "copyLink": true, "backForward": true},
  "locales": {"vi": "/vi/hoc-theo-lop", "en": "/en/learn-by-grade"},
  "accessibility": {"desktopCriticalOrSerious": 0, "mobileCriticalOrSerious": 0},
  "desktopMetrics": {"clientWidth": 1440, "scrollWidth": 1440},
  "mobileMetrics": {"clientWidth": 390, "scrollWidth": 390}
}
```

Screenshots:

- `artifacts/curriculum-catalog/curriculum-desktop.png` — 1440px desktop grade 12
  detail with mandatory sections and lesson cards visible.
- `artifacts/curriculum-catalog/curriculum-mobile.png` — 390px English mobile detail;
  keyboard focus, copy-link and history journey are exercised before capture.

The existing compatibility suite was also sampled. Its timeline test remains a
pre-existing label-association issue (`getByLabel("Thời kỳ")` cannot resolve a
nested `<select>` although the role is present), and one collection run recorded a
flaky LCP of 2576ms versus the 2500ms budget. Neither path is modified by C-025.
