# C-067 — External-evidence collection pack and release gate ledger

status: done
deps: [C-066]
implements: [release-governance]

## Scope

Tạo evidence ledger có schema rõ ràng cho các mục M1/M6/M7/M8/M9/M11/M12 còn phụ thuộc
người thật hoặc hệ thống external: official domain, uptime, Council members/sign-off,
golden-set approval, 2-model comparison, DPIA, partner permission/MOU, pilot consent/data,
schools/universities/users, pen-test, budget/rota. Ledger mặc định `PENDING_EXTERNAL_EVIDENCE`,
không tự điền tên/chữ ký/số liệu.

## Allowed files

- `docs/operations/external-evidence-ledger.md`
- `artifacts/operations/external-evidence-ledger.json`
- `scripts/operations-check.mjs`
- `artifacts/operations/report.json`
- `cards/C-067.md`

## Verify (run these before calling the card done)

- [x] `npm run operations:check` passes structural checks and reports `externalEvidence=PENDING`,
  `realPilotCompleted=false`, `realCouncilSignoff=false`.
- [x] Ledger contains owner, required artifact, acceptance test, status and no fabricated
  personal data/signatures.

## Done-evidence (world-state proof, named BEFORE building)

`docs/operations/external-evidence-ledger.md` is a handoff-ready checklist, while
`artifacts/operations/external-evidence-ledger.json` machine-checks every external gate as
pending. The public dashboard remains `NOT_READY` until evidence is supplied by authorized
owners.

## Evidence (paste the actual proof here when done)

Proof: `npm run operations:check` → PASS structural checks, `externalEvidence=PENDING_EXTERNAL_EVIDENCE`, `realPilotCompleted=false`, `realCouncilSignoff=false`, 11 ledger items. The handoff ledger and JSON explicitly contain null owners/artifacts until authorized people supply evidence; `fabricatedPersonalData=false` and `databaseMutation=false`.
