# C-057 — N-019 release calendar, pilot protocol và owner ledger

status: done
deps: [C-055, C-056]
implements: [operations]

## Scope

Đóng gói 90-day content calendar theo tuần, incident rota/RACI, user-pilot protocol và
release evidence ledger. Mỗi mục có owner, input, exit criteria, evidence URL/file và
trạng thái `PENDING_EXTERNAL_EVIDENCE`; checker fail nếu thiếu owner hoặc biến protocol
thành claim đã pilot.

## Allowed files

- `docs/operations/**`
- `scripts/operations-check.mjs`
- `package.json`
- `artifacts/operations/**`
- `cards/C-057.md`

## Verify (run these before calling the card done)

- [x] `npm run operations:check` exits 0 for complete calendar/rota/protocol and reports
      pending external evidence honestly.
- [x] Red fixture missing owner exits 1.
- [x] `npm run lint`, `npm run typecheck`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Versioned 90-day operating plan and machine-readable evidence ledger suitable for handoff;
pilot/uptime/signoff fields remain visibly pending until real-world proof exists.

## Evidence (paste the actual proof here when done)

- Operations checker passed 7 required markers and explicitly reports external evidence pending, no real pilot/council/fixed-domain claim: `artifacts/operations/report.json`.
- Versioned plan: `docs/operations/90-day-calendar.md`; consolidated proof: `artifacts/operations/operations-proof.json`.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit 0.
