# C-035 — Bản đồ địa danh lịch sử có fallback

status: done
deps: [C-034]
implements: [FR19]

## Scope

Thêm place/point/precision, API và route bản đồ dùng GeoJSON cục bộ. Marker/filter/link tới
detail dùng cùng dữ liệu; HTML/SVG narrative đầy đủ khi canvas/WebGL không chạy, không phụ
thuộc tile provider ngoài và không biến tọa độ xấp xỉ thành điểm chính xác.

## Allowed files

- `flow/05-contract.md`
- `DESIGN.md`
- `migrations/0008_places.sql`
- `src/data/maps/**`
- `src/lib/content/**`
- `src/lib/openapi/**`
- `src/lib/public-client/**`
- `src/app/api/v1/[locale]/places/**`
- `src/app/[locale]/ban-do/**`
- `src/components/public/map/**`
- `src/app/globals.css`
- `tests/public-api/**`
- `tests/public-ui/**`
- `tests/e2e/historical-map.spec.ts`
- `tests/foundation/migrations.test.ts`
- `tests/recovery/recovery.test.ts`
- `artifacts/historical-map/**`
- `cards/C-035.md`

## Verify (run these before calling the card done)

- [x] API/OpenAPI/migration/build và place precision/empty-facet tests thoát 0.
- [x] Pointer/keyboard marker/filter/detail và no-WebGL fallback cùng dữ liệu.
- [x] Mobile/desktop/axe/overflow/LCP ≤2,5 giây.

## Done-evidence (world-state proof, named BEFORE building)

Live map + fallback URL, screenshots mobile/desktop, JSON places/precision và keyboard/a11y proof.

## Evidence (paste the actual proof here when done)

Live HTTPS proof: `https://cheese-vessels-unified-travel.trycloudflare.com/vi/ban-do` and `https://cheese-vessels-unified-travel.trycloudflare.com/en/ban-do`; filtered fallback: `https://cheese-vessels-unified-travel.trycloudflare.com/vi/ban-do?precision=APPROXIMATE`. Live curl returned HTTP 200 for `/healthz`, both place APIs, both map pages, and the fallback URL. The live API returned 9 VI places and 9 EN places; `place-bach-dang` is `APPROXIMATE` with `{longitude:106.75,latitude:20.96}` and three published related entries. JSON evidence is in `artifacts/historical-map/live-places-vi.json`, `artifacts/historical-map/live-places-en.json`, and `artifacts/historical-map/historical-map-proof.json`.

Live Playwright: 3 passed — desktop filter + keyboard marker + detail links + fallback narrative, mobile approximate filter with zero horizontal overflow, and HTML-response fallback proof with no WebGL/canvas/tile dependency. Desktop axe critical/serious violations: 0; desktop viewport `clientWidth=1280`, `scrollWidth=1280`; LCP `456ms`. Screenshots: `artifacts/historical-map/map-desktop.png` and `artifacts/historical-map/map-mobile.png`.

Targeted Vitest: 64 passed across public places API, OpenAPI contract shape, map UI source contract, and existing public API routes. `npm run typecheck`, `npm run lint`, and `npm run build` passed. Migration `0008_places.sql` applied successfully on the production database (schema version 8). Migration/recovery expectations are updated to assert the new schema version and full regression is run before merge.
