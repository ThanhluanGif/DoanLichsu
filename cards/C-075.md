# C-075 — Harden human AI review ledger validation

status: done
deps: [C-072, C-074]
implements: [ai-governance]

## Scope

Thêm chế độ `--validate` cho human golden-set ledger: kiểm tra snapshot hash, đúng 500 ID
duy nhất, verdict/reviewer/timestamp hợp lệ, không partial approval thiếu evidence, và chỉ
PASS khi cả 500 dòng có dual APPROVE. Generator mặc định vẫn tạo pending ledger rỗng.

## Allowed files

- `scripts/ai-human-review-ledger.mjs`
- `artifacts/ai-eval/human-review-ledger.json`
- `artifacts/ai-eval/human-review-ledger.md`
- `cards/C-075.md`

## Verify (run these before calling the card done)

- [x] `node scripts/ai-human-review-ledger.mjs --validate` fails closed on current pending
  ledger with 0 dual approvals and no fabricated identity.
- [x] A temporary malformed ledger fixture is rejected for hash/duplicate/partial approval
  errors without modifying the canonical ledger.

## Done-evidence (world-state proof, named BEFORE building)

Validator output is a durable gate showing precisely why human review is still pending and what
fields an authorized Council reviewer must provide before Public Beta can be considered.

## Evidence (paste the actual proof here when done)

Proof: `node scripts/ai-human-review-ledger.mjs --validate` exits non-zero with `PENDING_HUMAN_REVIEW`, `dualApproved=0`, `violations=0` on the canonical pending ledger. Validator now checks 500 unique IDs, frozen dataset/report hashes, verdict enum, timestamps and reviewer evidence for every approval; it never mutates the canonical ledger from external input.
