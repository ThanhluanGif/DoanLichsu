# C-046 — N-009 AI eval và red-team harness

status: done
deps: [C-045]
implements: [ai-eval]

## Scope

Tạo golden/eval harness chạy gateway trên các nhóm câu hỏi grounded, unsupported, sai tiền
đề và prompt injection. Dataset có schema cho 500 câu (250 curriculum, 100 chính trị,
50 so sánh/nhân quả, 50 false-premise, 50 injection); card này cung cấp seed 12 câu và
generator deterministic để mở rộng, chấm citation/refusal/unsupported leakage. Harness
fail-closed và không tuyên bố đạt ngưỡng beta nếu chưa chạy đủ 500 câu.

## Allowed files

- `scripts/ai-eval.mjs`
- `package.json`
- `artifacts/ai-eval/**`
- `cards/C-046.md`

## Verify (run these before calling the card done)

- [x] Seed eval runs and reports category counts, citation precision, grounded support,
      refusal correctness and injection leak; missing 500-question target is visible.
- [x] Red policy fixture is detected as fail and does not mutate DB.
- [x] `npm run lint`, `npm run typecheck`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Machine-readable eval report with per-question outputs, category metrics, target gap and
explicit beta gate status; red-team prompts are retained without secrets or user data.

## Evidence (paste the actual proof here when done)

- Seed harness ran 12 questions across all five categories: accuracy 100%, citation-valid 100%, injection leak 0%, but explicit target gap 488/500 keeps beta gate FAIL. Report: `artifacts/ai-eval/report.json`; proof: `artifacts/ai-eval/eval-proof.json`.
- The 500-question requirement remains visible and blocks public AI; this card does not claim beta readiness or invent a passing golden set.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit 0; no database mutation.
