# C-005 — Chứng minh runtime API không drift planning contract

status: todo
deps: C-004
implements: none

## Scope

Tạo một contract-test suite độc lập chạy qua production HTTP server, đối chiếu mọi method/path HTTP trong `flow/05-contract.md` với live `/openapi.json`, rồi gọi happy/error/edge cases để kiểm tra status, auth và shapes sâu. Suite chỉ chạy vào localhost với SQLite disposable dưới thư mục tạm: trước mutation, nó được phép đặt rồi hoàn nguyên một marker trực tiếp trên một summary đã publish để chứng minh `CONTRACT_DATABASE_PATH` chính là DB phía sau HTTP target; sau đó chỉ xóa exact run-owned IDs và bắt buộc database digest trở về baseline. Không thêm feature hoặc sửa response cho “khớp test” nếu planning contract chưa được amend trước. Corrective seams: bổ sung bốn plumbing rows (`/openapi.json`, `/docs`, `/sitemap.xml`, `/robots.txt`), sửa `AdminTranslation.translationStatus` dùng toàn bộ `TranslationStatus`, và khai báo role metadata trong OpenAPI. C-005 phục vụ sitemap XML rỗng hợp lệ vì canonical HTML routes thuộc C-006; tuyệt đối không quảng bá API JSON URLs như canonical pages.

## Allowed files

- `package.json`
- `package-lock.json`
- `tests/contract/**`
- `scripts/contract-check.*`
- `scripts/contract-shape.*`
- `artifacts/contract/**`
- `src/app/sitemap.ts`
- `src/app/robots.ts`
- `src/lib/openapi/**`
- `flow/05-contract.md`
- `cards/C-005.md`

## Verify (run these before calling the card done)

- [ ] `CONTRACT_DATABASE_PATH=/tmp/contract.db npm run test:contract -- --base-url http://127.0.0.1:3000` exit 0 against `next start`, không import server internals; preflight marker được hoàn nguyên trước mutation và teardown chỉ xóa exact run-owned rows.
- [ ] Report khẳng định 100% HTTP method/path trong planning table tồn tại live OpenAPI, không có method/path runtime ngoài allowlist health/docs/spec/sitemap/robots.
- [ ] Contract cases kiểm tra list/single/error envelope, ISO timestamps, deterministic pagination, invalid input 400, 401/403/409/422/429 và không bao giờ trả hash/token.
- [ ] Suite bao phủ public, auth, content/source/media/workflow/user/audit endpoints; report ghi tổng pass/fail và response-shape diff hữu ích khi cố ý đổi một expected field.

## Done-evidence (world-state proof, named BEFORE building)

Live `/openapi.json` và API URL được suite bên ngoài process gọi thật; `artifacts/contract/latest.md` ghi số endpoint/case đã kiểm, 0 drift và các live URL đã dùng.

## Evidence (paste the actual proof here when done)

(empty until done)
