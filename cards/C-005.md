# C-005 — Chứng minh runtime API không drift planning contract

status: todo
deps: C-004
implements: none

## Scope

Tạo một contract-test suite độc lập chạy qua production HTTP server, đối chiếu mọi method/path HTTP trong `flow/05-contract.md` với live `/openapi.json`, rồi gọi happy/error/edge cases để kiểm tra status, auth và các field bắt buộc. Suite phải tạo dữ liệu riêng, dọn dữ liệu sau run và xuất report máy đọc + report Markdown; không thêm feature hoặc sửa response cho “khớp test” nếu planning contract chưa được amend trước.

## Allowed files

- `package.json`
- `package-lock.json`
- `tests/contract/**`
- `scripts/contract-check.*`
- `artifacts/contract/.gitkeep`
- `cards/C-005.md`

## Verify (run these before calling the card done)

- [ ] `npm run test:contract -- --base-url http://127.0.0.1:3000` exit 0 against `next start`, không import server internals.
- [ ] Report khẳng định 100% HTTP method/path trong planning table tồn tại live OpenAPI, không có method/path runtime ngoài allowlist health/docs/spec/sitemap/robots.
- [ ] Contract cases kiểm tra list/single/error envelope, ISO timestamps, deterministic pagination, invalid input 400, 401/403/409/422/429 và không bao giờ trả hash/token.
- [ ] Suite bao phủ public, auth, content/source/media/workflow/user/audit endpoints; report ghi tổng pass/fail và response-shape diff hữu ích khi cố ý đổi một expected field.

## Done-evidence (world-state proof, named BEFORE building)

Live `/openapi.json` và API URL được suite bên ngoài process gọi thật; `artifacts/contract/latest.md` ghi số endpoint/case đã kiểm, 0 drift và các live URL đã dùng.

## Evidence (paste the actual proof here when done)

(empty until done)
