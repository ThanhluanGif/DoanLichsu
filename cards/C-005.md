# C-005 — Chứng minh runtime API không drift planning contract

status: done
deps: C-004
implements: none

## Scope

Tạo một contract-test suite độc lập chạy qua production HTTP server, đối chiếu mọi method/path HTTP trong `flow/05-contract.md` với live `/openapi.json`, rồi gọi happy/error/edge cases để kiểm tra status, auth và shapes sâu. Suite chỉ chạy vào localhost với SQLite disposable dưới thư mục tạm: trước mutation, nó được phép đặt rồi hoàn nguyên một marker trực tiếp trên một summary đã publish để chứng minh `CONTRACT_DATABASE_PATH` chính là DB phía sau HTTP target, sau đó tạo ba auth fixtures gắn `runId` trực tiếp để không đăng nhập bằng seed users; teardown chỉ xóa exact run-owned IDs và bắt buộc database digest trở về baseline. Không thêm feature hoặc sửa response cho “khớp test” nếu planning contract chưa được amend trước. Corrective seams: bổ sung bốn plumbing rows (`/openapi.json`, `/docs`, `/sitemap.xml`, `/robots.txt`), sửa `AdminTranslation.translationStatus` dùng toàn bộ `TranslationStatus`, và khai báo role metadata trong OpenAPI. C-005 phục vụ sitemap XML rỗng hợp lệ vì canonical HTML routes thuộc C-006; tuyệt đối không quảng bá API JSON URLs như canonical pages.

## Allowed files

- `package.json`
- `package-lock.json`
- `tests/contract/**`
- `scripts/contract-check.*`
- `scripts/contract-shape.*`
- `artifacts/contract/**`
- `src/app/sitemap.ts`
- `src/app/robots.ts`
- `src/lib/openapi/**`
- `flow/05-contract.md`
- `cards/C-005.md`

## Verify (run these before calling the card done)

- [x] `CONTRACT_DATABASE_PATH=/tmp/contract.db npm run test:contract -- --base-url http://127.0.0.1:3000` exit 0 against `next start`, không import server internals; preflight marker được hoàn nguyên trước mutation và teardown chỉ xóa exact run-owned rows.
- [x] Report khẳng định 100% HTTP method/path trong planning table tồn tại live OpenAPI, không có method/path runtime ngoài allowlist health/docs/spec/sitemap/robots.
- [x] Contract cases kiểm tra list/single/error envelope, ISO timestamps, deterministic pagination, invalid input 400, 401/403/409/422/429 và không bao giờ trả hash/token.
- [x] Suite bao phủ public, auth, content/source/media/workflow/user/audit endpoints; report ghi tổng pass/fail và response-shape diff hữu ích khi cố ý đổi một expected field.

## Done-evidence (world-state proof, named BEFORE building)

Live `/openapi.json` và API URL được suite bên ngoài process gọi thật; `artifacts/contract/latest.md` ghi số endpoint/case đã kiểm, 0 drift và các live URL đã dùng.

## Evidence (paste the actual proof here when done)

- 2026-08-07: production build hoàn tất và `next start` phục vụ DB disposable tại `http://127.0.0.1:3000`; `CONTRACT_DATABASE_PATH=/tmp/qsv-contract-c005.W7tTuX/contract.db npm run test:contract -- --base-url http://127.0.0.1:3000` exit 0.
- Live report run `dd5fb6fd` tại `artifacts/contract/latest.{json,md}`: planning/runtime đều 37 operations, 189/189 cases pass, 170 live responses được validate schema, 0 missing/extra/shape drift. Suite gọi thật `/healthz`, `/openapi.json`, `/docs`, `/sitemap.xml`, `/robots.txt` và public/auth/admin APIs.
- Access/error proof: 23 protected operations trả 401 khi không có session, 57 allowed-role probes, 12 denied-role probes; cases có 400/401/403/409/422/429, pagination ổn định, ISO timestamp và secret-field scan.
- DB identity/cleanup proof: reversible marker được quan sát qua HTTP trên `en/ARTIFACT/bach-dang-wooden-stakes`, sau teardown exact run-owned rows digest trở về baseline `3087f8725697f4741ac2efc2e0a1772ef72a309c96b31e94f6ed48a9045b3da9` với 50 content nodes, 100 translations, 50 sources, 10 media, 3 users, 0 audit/rate-limit rows, schema v3.
- Negative drift proof: `tests/contract/contract-shape.test.ts` cố ý đổi field/enum/query/body/security/role/status/media/error shape và nhận path-specific diff; full `npm test` pass 68/68. `npm run typecheck`, `npm run lint`, `npm run build` và `npm run verify:standalone` đều exit 0.
