# C-110 — Check DoD audit and matrix consistency

status: done
deps: C-109
implements: infra

## Scope

Add a deterministic consistency checker that compares the DoD audit with the
Section 17 matrix: overall status, Public Beta flag, and the set of canonical
external blocker IDs must agree. It must fail on drift and keep the existing
honest `NOT_READY` state.

## Allowed files

scripts/dod-matrix-consistency.mjs
tests/operations/dod-matrix-consistency.test.ts
artifacts/release/dod-matrix-consistency.json
artifacts/release/dod-matrix-consistency.md
cards/C-110.md

## Verify (run these before calling the card done)

- [x] `node scripts/dod-matrix-consistency.mjs` exits 0 with `consistent: true`.
- [x] `npm test -- --run tests/operations/dod-matrix-consistency.test.ts` passes,
      including a temporary drift fixture.
- [x] `npm run typecheck` passes.

## Done-evidence (world-state proof, named BEFORE building)

Consistency artifact proves audit/matrix agreement while preserving
`NOT_READY`, `publicBeta: false`, and all 11 canonical blockers.

## Evidence (paste the actual proof here when done)

Consistency command exited 0 with `consistent: true`, all three checks true, and
11 canonical blockers. Focused test passed (2 tests), including a temporary drift
fixture that correctly failed; typecheck passed. Public Beta remains disabled.
