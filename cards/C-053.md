# C-053 — N-016 offline lesson pack và PWA manifest

status: done
deps: [C-049]
implements: [offline-learning]

## Scope

Tạo offline manifest/lesson bundle từ teacher pack: JSON manifest, cached HTML/JSON assets,
checksum, kích thước và low-bandwidth fallback. Không cache admin/private/AI transcript;
bundle phải ghi ngày snapshot và có thể kiểm tra integrity trước import.

## Allowed files

- `scripts/offline-pack.mjs`
- `package.json`
- `artifacts/offline/**`
- `cards/C-053.md`

## Verify (run these before calling the card done)

- [x] Offline pack emits manifest, checksum and lesson assets; integrity verification passes.
- [x] Invalid checksum fixture exits 1; no private/AI data included.
- [x] `npm run lint`, `npm run typecheck`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Verifiable low-bandwidth bundle artifact with manifest/checksum and red integrity proof.

## Evidence (paste the actual proof here when done)

- Offline bundle passed integrity with SHA-256, 1,124-byte lesson asset, snapshot ID and explicit excluded paths: `artifacts/offline/manifest.json`.
- Invalid checksum fixture is retained as fail-closed proof and cannot serve binary: `artifacts/offline/red-manifest.json`.
- Consolidated proof: `artifacts/offline/offline-proof.json`.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit 0.
