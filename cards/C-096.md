# C-096 — Expose Wikimedia metadata integrity in transparency dashboard

status: done
deps: C-095
implements: infra

## Scope

Add the Wikimedia rights-review ledger's invalid metadata count and binary-serving
state to the public transparency artifact. Keep the dashboard explicitly
implementation-only, Public Beta disabled, and do not turn malformed metadata into
an external approval or publishable asset.

## Allowed files

scripts/transparency-dashboard.mjs
tests/operations/transparency-dashboard.test.ts
artifacts/transparency/dashboard.json
artifacts/transparency/dashboard.md
cards/C-096.md

## Verify (run these before calling the card done)

- [x] `npm test -- --run tests/operations/transparency-dashboard.test.ts` passes
      and asserts 11 blockers, invalid Wikimedia metadata visibility, and disabled
      binary serving.
- [x] `npm run typecheck` passes.
- [x] `node scripts/transparency-dashboard.mjs` exits 0.

## Done-evidence (world-state proof, named BEFORE building)

`artifacts/transparency/dashboard.json` shows Wikimedia `invalidMetadataCount: 1`,
`binaryDownloaded: false`, `binaryServingEnabled: false`, eleven external blockers,
and `publicBeta: false`.

## Evidence (paste the actual proof here when done)

`npm test -- --run tests/operations/transparency-dashboard.test.ts`: 1 test
passed. `npm run typecheck` passed. `node scripts/transparency-dashboard.mjs` exited
0 and the dashboard reports `invalidMetadataCount: 1`,
`binaryServingEnabled: false`, 11 external blockers, and `publicBeta: false`.
