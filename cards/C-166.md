# C-166 — Rebind immutable release evidence to current main

status: done
deps: C-165
implements: infra

## Scope

Refresh the local release evidence and GHCR runtime-smoke handoff after the
post-C-165 merge on `main` (`908ac3e`). Bind the evidence to the real current
HEAD and the successful GitHub Actions image publication for that commit. Keep
the distinction between supply-chain/runtime evidence and official production:
the card must preserve `publicBeta=false`, `productionDeployment=false`,
`officialProduction=false`, `databaseMutation=false`, the 105 pending human
history attestations and all 11 external blockers. This card does not deploy a
service, mutate the database, create an owner or invent an approval.

## Allowed files

KE_HOACH_12_THANG_CONG_TRI_THUC_LICH_SU_VIET_NAM_AI.md
artifacts/operations/ghcr-runtime-smoke.json
artifacts/operations/ghcr-runtime-smoke.md
artifacts/production-readiness/report.json
artifacts/release/current-head-evidence.json
artifacts/release/current-head-evidence.md
artifacts/release/dod-audit.json
artifacts/release/dod-matrix-consistency.json
artifacts/release/global-master-consistency.json
artifacts/release/global-master-consistency.md
artifacts/release/year-one-dod-matrix.json
artifacts/release/year-one-dod-matrix.md
artifacts/transparency/dashboard.json
tests/operations/global-master-consistency-check.test.ts
cards/C-166.md

## Verify (run these before calling the card done)

- [x] `RELEASE_EVIDENCE_RUN=1 npm run release:evidence:current` exits 0 and
      records HEAD `908ac3e` with lint/typecheck/test/build exit codes `0,0,0,0`
      and local health/OpenAPI/search `200/200/200`.
- [x] The GitHub Actions publish run `31788508752` is successful for HEAD and
      its job log records digest
      `sha256:25a14c0a9846b791cdf5ff00e940a0e83c03517fbd8594b213d19af509d592b9`.
- [x] The immutable image at that digest starts in a disposable container and
      returns health `200/ok`, OpenAPI `200`, search `200`, Docker health
      `healthy`; the handoff remains explicitly non-production.
- [x] `node scripts/production-readiness.mjs`, `node scripts/dod-audit.mjs`,
      `node scripts/dod-matrix-consistency.mjs`, and the focused consistency
      test pass with DoD `NOT_READY`, Public Beta `false` and 11 blockers.

## Done-evidence (world-state proof, named BEFORE building)

The current main revision has hash-bound local release evidence and a real
content-addressed GHCR runtime smoke record. A handoff reader can identify the
exact image for `908ac3e` while the artifacts still clearly state that no
official production deployment or Public Beta approval exists.

## Evidence (paste the actual proof here when done)

- `RELEASE_EVIDENCE_RUN=1 npm run release:evidence:current` exited 0 with
  tested commit `908ac3ece1072619da27ed0889b81d835bcf46fd`, source-tree hash
  `5ef3075bfa5b50de252d49f0135484310333954c221fc2b4478d2d354b081444`,
  quality `[0,0,0,0]`, local health/OpenAPI/search `200/200/200`, security
  headers `true`, search p95 `37.17ms`, backup/restore checksums `true/true`, and no database
  mutation.
- GitHub Actions run `31788508752` passed for HEAD and recorded image digest
  `sha256:25a14c0a9846b791cdf5ff00e940a0e83c03517fbd8594b213d19af509d592b9`.
- The digest container `a9e8c0fc939d1c63e5efa2a9c214277d4d980c9d9f66137154be38dd326ddfdb`
  returned health `200` with `status=ok`, `database=ok`, OpenAPI `200`, search
  `200`, and Docker health `healthy` on `http://127.0.0.1:3321`.
- Production readiness is `PASS_LOCAL_ONLY`; DoD is `NOT_READY`, matrix
  consistency is `true`, Public Beta is `false`, and all 11 external blockers
  remain pending. The container and preview were stopped after verification.
