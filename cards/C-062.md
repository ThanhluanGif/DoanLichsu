# C-062 — Synchronize public transparency dashboard with 500-question eval

status: done
deps: [C-060, C-059]
implements: [public-transparency]

## Scope

Regenerate dashboard artifact và markdown từ report-500 thay vì report seed 12 câu; cập nhật
blocker copy để phân biệt machine eval PASS với human/council golden-set approval còn thiếu.
Giữ `NOT_READY`, Public AI DISABLED và không claim Public Beta.

## Allowed files

- `scripts/transparency-dashboard.mjs`
- `artifacts/transparency/**`
- `tests/e2e/transparency-dashboard.spec.ts`
- `cards/C-062.md`

## Verify (run these before calling the card done)

- [x] `node scripts/transparency-dashboard.mjs` reports AI `500/500`, gap `0`, release `NOT_READY`.
- [x] Live E2E asserts 500/500 and unchanged blockers/disclosure in VI/EN.
- [x] `npm run lint`, `npm run typecheck`, `npm test`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Public `/vi/minh-bach` and `/en/transparency` visibly report AI eval `500/500`, gap `0`,
machine status PASS, while release remains `NOT_READY` and the human/council approval blocker
remains visible.

## Evidence (paste the actual proof here when done)

Dashboard proof: `node scripts/transparency-dashboard.mjs` → AI status `PASS`, `actualQuestions=500`, `targetGap=0`, release `NOT_READY`; blocker explicitly says machine eval passed but golden answers/citations still lack Council approval. Updated `artifacts/transparency/dashboard.json`, `.md`, live proof and transparency proof.

Live E2E proof: `E2E_BASE_URL=https://intensity-models-macro-experiences.trycloudflare.com npm run test:e2e -- tests/e2e/transparency-dashboard.spec.ts` → 1 passed; VI/EN pages render `500/500`, `NOT_READY`, five blockers and non-endorsement disclosure.

Regression proof: `npm run lint` PASS (existing warnings only); `npm run typecheck` PASS; `npm run build` PASS. The failed `npm test -- tests/e2e/...` invocation was a wrong runner (Vitest cannot execute Playwright); the correct Playwright command above passed.
