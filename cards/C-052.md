# C-052 — N-015 privacy-friendly analytics dashboard

status: done
deps: [C-051]
implements: [analytics]

## Scope

Tạo report analytics aggregate, không cookie/PII: page views theo nhóm route, search
zero-result, AI abstain, lesson completion và correction SLA. Chỉ nhận event đã ẩn danh,
minimum aggregation threshold và retention ngắn; dashboard report dùng cho nghiên cứu,
không profiling trẻ em.

## Allowed files

- `scripts/privacy-analytics.mjs`
- `package.json`
- `artifacts/analytics/**`
- `cards/C-052.md`

## Verify (run these before calling the card done)

- [x] Aggregate fixture produces dashboard metrics without identifiers; low-count groups
      are suppressed.
- [x] PII fixture fails and is not counted; no DB mutation.
- [x] `npm run lint`, `npm run typecheck`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Privacy-safe aggregate dashboard artifact with suppression/retention metadata and red
PII proof.

## Evidence (paste the actual proof here when done)

- Aggregate report passed with 5 anonymous events, 2 visible groups and 1 suppressed low-count group; retention 30 days, no cookie/identifier/child profiling: `artifacts/analytics/report.json`.
- PII fixture exited 1, was not counted and did not mutate DB: `artifacts/analytics/red-report.json`.
- Consolidated proof: `artifacts/analytics/analytics-proof.json`.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit 0.
