# C-071 — Expose Wikimedia and AI comparison evidence publicly

status: done
deps: [C-069, C-070, C-066]
implements: [public-transparency]

## Scope

Mở rộng transparency artifact và dashboard để công khai số lượng Wikimedia metadata pilot
300 record và AI config comparison, với nhãn metadata-only/config-only/human approval pending.
Không hiển thị URL nội bộ nhạy cảm, không đổi Public Beta thành enabled.

## Allowed files

- `scripts/transparency-dashboard.mjs`
- `src/components/public/TransparencyDashboard.tsx`
- `artifacts/transparency/**`
- `tests/e2e/transparency-dashboard.spec.ts`
- `cards/C-071.md`

## Verify (run these before calling the card done)

- [x] Dashboard JSON reports Wikimedia `300` metadata-only and AI config comparison
  `PASS_CONFIG_ONLY`; release remains `NOT_READY`.
- [x] Production-like local live DOM finds both disclosures in VI and no secrets/PII; HTTPS
  quick-tunnel E2E is recorded as DNS-blocked, not passed falsely.
- [x] `npm run lint`, `npm run typecheck`, `npm test`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Public transparency routes visibly distinguish machine/config/metadata evidence from required
human rights, council, privacy and pilot approvals.

## Evidence (paste the actual proof here when done)

Artifact proof: `node scripts/transparency-dashboard.mjs` reports Wikimedia `300`, `LINK_ONLY/PENDING_REVIEW`, AI config `PASS_MACHINE_COMPARISON`, and release `NOT_READY`. Local standalone server `http://127.0.0.1:3230/vi/minh-bach` returned HTTP 200 with DOM markers `500/500`, `300`, `PASS_MACHINE_COMPARISON`, `NOT_READY`; OpenAPI reports `cookieAuth`. Proof: `artifacts/transparency/head-live-proof.json`.

Boundary: the new HTTPS quick tunnel DNS did not resolve, while older tunnel served stale 12/500 content. No HTTPS E2E PASS or official-domain claim is made.
