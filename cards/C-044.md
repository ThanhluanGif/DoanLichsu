# C-044 — N-007 approved corpus snapshot và indexer

status: done
deps: [C-038, C-041]
implements: [ai-corpus]

## Scope

Tạo snapshot bất biến của approved corpus từ SQLite: chỉ lấy nội dung `PUBLISHED`, bản
dịch public, source `VERIFIED`, claim `VERIFIED` và evidence có locator. Snapshot có
SHA-256, commit/schema metadata và lexical index đơn giản để gateway AI dùng sau này.
Fail-closed nếu có bài public thiếu source verified, claim evidence hoặc provenance;
script chỉ đọc DB, không tự sửa hay tự publish.

## Allowed files

- `scripts/approved-corpus.mjs`
- `package.json`
- `artifacts/approved-corpus/**`
- `cards/C-044.md`

## Verify (run these before calling the card done)

- [x] Current DB snapshot exits 0, contains only approved records, hash/index and counts.
- [x] Red fixture exits 1 when a published item lacks verified source/evidence and leaves
      the DB unchanged.
- [x] `npm run lint`, `npm run typecheck`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Auditable corpus snapshot and search index artifacts with hash, coverage counts and an
explicit red fail-closed report suitable as the only input boundary for AI retrieval.

## Evidence (paste the actual proof here when done)

- Green snapshot `npm run corpus:snapshot -- --output artifacts/approved-corpus/snapshot.json` passed with 55 published lesson content, 110 VI/EN translations, 114 verified sources, 110 verified claims and 3,516 lexical index terms; SHA-256 recorded in snapshot/proof.
- Red item `event-dien-bien-phu` exited 1 because source/claim evidence is not verified; database is read-only and unchanged: `artifacts/approved-corpus/red-report.json`.
- Consolidated proof: `artifacts/approved-corpus/corpus-proof.json`.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit 0; `next-env.d.ts` restored after build.
