# C-002 — Dựng deployable runtime và contract plumbing

status: todo
deps: C-001
implements: infra

## Scope

Khởi tạo repository Next.js 16/TypeScript chạy được như một Node deployable unit, có SQLite migration/versioning, config/secrets mẫu, quality commands và CI. Runtime phải phục vụ `/healthz`, `/openapi.json`, `/docs` ngay từ đầu theo contract; chưa xây public/admin feature API hay UI ngoài shell/placeholder đã được mock định hướng.

## Allowed files

- `.gitignore`
- `.env.example`
- `.github/workflows/ci.yml`
- `package.json`
- `package-lock.json`
- `tsconfig.json`
- `next.config.*`
- `eslint.config.*`
- `vitest.config.*`
- `next-env.d.ts`
- `README.md`
- `src/app/layout.tsx`
- `src/app/globals.css`
- `src/app/page.tsx`
- `src/app/healthz/**`
- `src/app/openapi.json/**`
- `src/app/docs/**`
- `src/lib/env.ts`
- `src/lib/db/**`
- `src/lib/openapi/**`
- `migrations/**`
- `scripts/migrate.*`
- `tests/foundation/**`
- `cards/C-002.md`

## Verify (run these before calling the card done)

- [ ] `npm ci && npm run db:migrate && npm run lint && npm run typecheck && npm test && npm run build` đều exit 0.
- [ ] Chạy production server rồi `curl -fsS http://127.0.0.1:3000/healthz` trả `status=ok`, `database=ok`, version và ISO timestamp.
- [ ] `curl -fsS http://127.0.0.1:3000/openapi.json` trả OpenAPI `3.1.x` có `/healthz`; `http://127.0.0.1:3000/docs` render API reference.
- [ ] Restart production server không làm mất `schema_migrations`; health vẫn xanh với cùng `DATABASE_PATH`.

## Done-evidence (world-state proof, named BEFORE building)

Một URL production-process có thể click mở `/docs`; curl thật tới `/healthz` và `/openapi.json` trả đúng shape từ một process `next start`, cùng truy vấn DB chứng minh migration tồn tại sau restart.

## Evidence (paste the actual proof here when done)

(empty until done)
