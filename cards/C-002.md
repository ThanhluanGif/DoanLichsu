# C-002 — Dựng deployable runtime và contract plumbing

status: done
deps: C-001
implements: infra

## Scope

Khởi tạo repository Next.js 16/TypeScript chạy được như một Node deployable unit, có SQLite migration/versioning, config/secrets mẫu, quality commands và CI. Runtime phải phục vụ `/healthz`, `/openapi.json`, `/docs` ngay từ đầu theo contract; chưa xây public/admin feature API hay UI ngoài shell/placeholder đã được mock định hướng.

## Allowed files

- `.gitignore`
- `.env.example`
- `.github/workflows/ci.yml`
- `package.json`
- `package-lock.json`
- `tsconfig.json`
- `next.config.*`
- `eslint.config.*`
- `vitest.config.*`
- `next-env.d.ts`
- `README.md`
- `src/app/layout.tsx`
- `src/app/globals.css`
- `src/app/page.tsx`
- `src/app/healthz/**`
- `src/app/openapi.json/**`
- `src/app/docs/**`
- `src/lib/env.ts`
- `src/lib/db/**`
- `src/lib/openapi/**`
- `migrations/**`
- `scripts/migrate.*`
- `tests/foundation/**`
- `cards/C-002.md`

## Verify (run these before calling the card done)

- [x] `npm ci && npm run db:migrate && npm run lint && npm run typecheck && npm test && npm run build` đều exit 0.
- [x] Chạy production server rồi `curl -fsS http://127.0.0.1:3000/healthz` trả `status=ok`, `database=ok`, version và ISO timestamp.
- [x] `curl -fsS http://127.0.0.1:3000/openapi.json` trả OpenAPI `3.1.x` có `/healthz`; `http://127.0.0.1:3000/docs` render API reference.
- [x] Restart production server không làm mất `schema_migrations`; health vẫn xanh với cùng `DATABASE_PATH`.

## Done-evidence (world-state proof, named BEFORE building)

Một URL production-process có thể click mở `/docs`; curl thật tới `/healthz` và `/openapi.json` trả đúng shape từ một process `next start`, cùng truy vấn DB chứng minh migration tồn tại sau restart.

## Evidence (paste the actual proof here when done)

- Full gate trên DB mới `data/c002-release-gate.db`: `npm ci`, migrate, lint, typecheck,
  4 test files/16 tests, Next 16.3 production build và `npm run verify:standalone` đều
  exit 0. Migration stdout:
  `{"databasePath":"/Volumes/sdd anh/Doan1-card-C-002/data/c002-release-gate.db","applied":[1],"currentVersion":1}`.
- Production `next start` smoke tại `http://127.0.0.1:3202`: `/healthz`,
  `/openapi.json`, `/docs` đều HTTP 200; health trả
  `{"status":"ok","version":"c002-next-start","database":"ok","timestamp":"2026-08-06T10:25:54.302Z"}`.
- Deployable artifact chạy trực tiếp bằng `.next/standalone/server.js`; URL đang kiểm tra:
  `http://127.0.0.1:3203/docs`. Curl `/healthz` sau restart trả
  `{"status":"ok","version":"c002-release-restarted","database":"ok","timestamp":"2026-08-06T10:35:32.333Z"}`;
  `/openapi.json` là `3.1.0`, có path `/healthz` và responses `200`, `503`; `/docs`
  HTTP 200 (9632 bytes) và CSS HTTP 200 (4451 bytes).
- Cùng DB sau restart vẫn có
  `[{"version":1,"name":"0001_foundation.sql","applied_at":"2026-08-06T10:34:15.705Z"}]`.
  Artifact tự chạy lại `scripts/migrate.mjs` cho kết quả `applied:[]`, `currentVersion:1`.
- Failure-world proof: standalone `/healthz` với đường dẫn DB chưa tồn tại trả HTTP 503,
  body 0 byte và `db_created=no`. Test race migration đã đỏ với `SQLITE_BUSY` trước sửa,
  rồi xanh; hai process đồng thời hiện cho đúng một `applied:[1]` và một `applied:[]`.
