# C-040 — N-002 governance pack và sensitive-topic policy

status: done
deps: [C-039]
implements: [governance]

## Scope

Đóng gói bốn chính sách vận hành (Editorial Charter, Source & Rights Policy,
Sensitive Topic Policy, Correction Policy), RACI/owner register và một registry
machine-readable để kiểm tra fail-closed. Bộ tài liệu phân biệt rõ trạng thái
`DRAFT_PENDING_COUNCIL_SIGNOFF` với phê duyệt thật; không tự nhận đã có Hội đồng
sử học ký. Sensitive topics phải dual-review, bất đồng chuyển Hội đồng và AI
không được tự hòa giải.

## Allowed files

- `docs/governance/**`
- `scripts/governance-check.mjs`
- `package.json`
- `artifacts/governance/**`
- `cards/C-040.md`

## Verify (run these before calling the card done)

- [x] `npm run governance:check` exits 0 and reports all four policies, RACI roles,
      decision states, dual-review rules, AI prohibition and correction SLA.
- [x] A red fixture with a missing required control exits non-zero without mutating
      the production database.
- [x] `npm run lint`, `npm run typecheck`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Versioned governance documents and a machine-readable PASS report are present in the
release artifact; the report explicitly shows the council sign-off gate is still
pending and names the owners needed to release it.

## Evidence (paste the actual proof here when done)

- `npm run governance:check -- --output artifacts/governance/report.json` exited 0 with 4 policies, 8 RACI role rows and 5 mandatory controls. JSON/Markdown reports: `artifacts/governance/report.json`, `artifacts/governance/report.md`.
- Red fixture with only an incomplete charter exited 1 and reported missing controls; no database path is opened or mutated: `artifacts/governance/red-report.json`.
- Consolidated proof: `artifacts/governance/governance-proof.json`.
- Governance documents explicitly remain `DRAFT_PENDING_COUNCIL_SIGNOFF`; no independent historian council approval is invented and release is blocked until real sign-off exists.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit 0 after the card changes.
