# C-134 — Refresh post-commit release/DoD evidence coherence

status: done
deps: C-133
implements: infra

## Scope

Regenerate the dependent release, production-readiness, DoD audit, matrix,
transparency and operations reports after the C-133 commit so every artifact
uses the same current source-tree hash and tested commit. Preserve the honest
`NOT_READY`/Public Beta false state, 105 missing published-history attestations
and all 11 external blockers. No product feature or approval ledger changes.

## Allowed files

artifacts/release/current-head-evidence.json
artifacts/release/current-head-evidence.md
artifacts/production-readiness/report.json
artifacts/production-readiness/report.md
artifacts/release/dod-audit.json
artifacts/release/dod-audit.md
artifacts/release/dod-matrix-consistency.json
artifacts/release/dod-matrix-consistency.md
artifacts/transparency/dashboard.json
artifacts/transparency/dashboard.md
artifacts/operations/report.json
artifacts/operations/report.md
cards/C-134.md

## Verify (run these before calling the card done)

- [x] `npm run release:evidence:current` exits 0 and records the current HEAD
      source hash.
- [x] `npm run production:readiness` and `npm run operations:check` exit 0;
      readiness is `PASS_LOCAL_ONLY` and operations is `PASS`.
- [x] `node scripts/dod-audit.mjs` may exit 1 only for honest NOT_READY state;
      JSON reports `sourceTreeMatches: true`, `status: NOT_READY`, Public Beta
      false, 105 missing history rows and 11 external blockers.
- [x] `node scripts/dod-matrix-consistency.mjs` and
      `npm run transparency:dashboard` exit 0; matrix is consistent and
      dashboard source hash matches current tree.
- [x] `npm run typecheck` exits 0.

## Done-evidence (world-state proof, named BEFORE building)

The committed release evidence, readiness report, DoD audit, consistency
matrix, transparency dashboard and operations report all identify the same
current HEAD/source tree. A reviewer can inspect those artifacts and see an
honest local-only release with Public Beta disabled and every unresolved gate
listed; no stale pre-C-133 hash remains.

## Evidence (paste the actual proof here when done)

Verified 2026-08-14 after C-133 commit:

- `release:evidence:current`: exit 0; current HEAD
  `a8b3e642cdec3045f9c6e04e101cf7854838d764`; source hash
  `30281a0f63b0043ffeb388071e0467a67e5cc0d50cefabfa0605d1abbcdb7e0e`.
- Production readiness: `PASS_LOCAL_ONLY`; operations check: `PASS`; typecheck: exit 0.
- DoD audit: `NOT_READY`, Public Beta false, 105 missing published-history rows,
  11 external blockers; current-head release evidence `PASS_LOCAL_ONLY` with
  `sourceTreeMatches: true`.
- DoD/matrix consistency: `consistent: true`; transparency dashboard source hash
  matches current tree and Public Beta remains false.
