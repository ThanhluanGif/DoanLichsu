# C-008 — Ship và chứng minh release v1 có thể phục hồi

status: todo
deps: C-007
implements: FR14

## Scope

Đóng gói release Node/Docker có persistent database, lệnh backup/restore đúng contract, tài liệu vận hành và một E2E suite chạy trên deployed HTTPS URL. Release gate tổng hợp public/admin journeys, contract, security headers/dependency scan, axe, responsive/performance smoke và restore rehearsal thành report; chỉ khi live surface và DB restored được chứng minh mới coi v1 shipped.

## Allowed files

- `.dockerignore`
- `.env.example`
- `Dockerfile`
- `docker-compose.yml`
- `next.config.*`
- `DESIGN.md`
- `src/app/globals.css`
- `src/components/admin/ContentEditor.tsx`
- `mockups/styles.css`
- `package.json`
- `package-lock.json`
- `playwright.config.*`
- `README.md`
- `docs/**`
- `scripts/backup.*`
- `scripts/contract-check.mjs`
- `scripts/restore.*`
- `scripts/release-check.*`
- `tests/e2e/**`
- `tests/security/**`
- `tests/recovery/**`
- `artifacts/release/**`
- `AUTO-LOG.md`
- `cards/C-008.md`

## Verify (run these before calling the card done)

- [ ] Clean install chạy migrate/seed/lint/typecheck/unit/integration/contract/build/E2E exit 0; report ghi command, duration, version/commit và không che fail.
- [ ] `npm run db:backup` tạo snapshot + manifest SHA-256; restore sang path sạch exit 0, checksum/schema/count khớp 50 nodes/100 translations/≥50 sources/3 users; app đọc được restored DB.
- [ ] Playwright chạy trên HTTPS URL ngoài process: ba public journeys và login→create→review→publish→public, gồm negative RBAC; screenshots/traces chỉ giữ khi fail, không ghi credential.
- [ ] Axe không có Critical/Serious; response có security headers; dependency/security scan không còn Critical/High; log không chứa password/cookie/token.
- [ ] Production-like measurement ghi LCP home/detail ≤2,5 s và p95 search ≤1 s với 50 records hoặc report fail rõ để sửa trước done.
- [ ] `curl -fsS <HTTPS_URL>/healthz`, `/openapi.json`, `/api/v1/vi/search?q=dien%20bien%20phu` và browser locale/admin flow đều chạy sau restart deploy.

## Done-evidence (world-state proof, named BEFORE building)

Một HTTPS URL có thể click từ bên ngoài chạy toàn bộ v1 sau restart; `artifacts/release/` chứa curl live, E2E report, accessibility/performance/security summary và restore manifest/checksum/count chứng minh dữ liệu phục hồi được.

## Evidence (paste the actual proof here when done)

(empty until done)
