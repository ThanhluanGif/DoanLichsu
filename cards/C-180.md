# C-180 — Generate an external evidence submission kit

status: todo
deps: C-179
implements: infra

## Scope

Generate a bilingual, machine-readable submission kit from the canonical
external evidence ledger, then rebind the derived local release/readiness
artifacts to the final source snapshot. The kit must list all 11 gates, their
required owner role, required evidence and next action, plus the exact fields
an authorised owner must provide (`owner`, `authority`, `verifiedAt`, `artifact`,
`sha256`, and an optional note). It must remain a blank handoff template: all
gates stay `PENDING`, `publicBeta=false`, `releaseAllowed=false`, and no
secret, reviewer, signature or approval is invented. This reduces submission
errors; it does not change the canonical ledger or grant external approval.

## Allowed files

KE_HOACH_12_THANG_CONG_TRI_THUC_LICH_SU_VIET_NAM_AI.md
package.json
scripts/external-evidence-submission-kit.mjs
tests/operations/external-evidence-submission-kit.test.ts
artifacts/operations/external-evidence-submission-kit.json
artifacts/operations/external-evidence-submission-kit.md
artifacts/operations/external-evidence-handoff.json
artifacts/operations/external-evidence-handoff.md
artifacts/operations/external-evidence-verification.json
artifacts/operations/local-standalone-smoke.json
artifacts/operations/production-handoff-check.json
artifacts/operations/report.json
artifacts/operations/report.md
artifacts/production-readiness/report.json
artifacts/production-readiness/report.md
artifacts/release/current-head-evidence.json
artifacts/release/current-head-evidence.md
artifacts/release/dod-audit.json
artifacts/release/dod-audit.md
artifacts/release/dod-matrix-consistency.json
artifacts/release/dod-matrix-consistency.md
artifacts/release/global-master-consistency.json
artifacts/release/global-master-consistency.md
artifacts/release/year-one-dod-matrix.json
artifacts/release/year-one-dod-matrix.md
artifacts/transparency/dashboard.json
artifacts/transparency/dashboard.md
cards/C-180.md

## Verify (run these before calling the card done)

- [ ] `npm run operations:evidence:kit` exits 0, creates the JSON/Markdown kit,
      lists exactly 11 gates, and keeps every gate `PENDING` with
      `publicBeta=false`, `releaseAllowed=false`, `databaseMutation=false`.
- [ ] Focused test proves the kit contains the six required submission fields,
      no owner/artifact/hash/timestamp values are fabricated, no secret-looking
      values are emitted, and the source ledger hash is recorded.
- [ ] `npm run typecheck`, the focused Vitest file, `git diff --check`, and
      `bash /Users/admin/.agents/skills/flow/runner/flow.sh check C-180` pass.
- [ ] After the final source snapshot, `RELEASE_EVIDENCE_RUN=1 npm run
      release:evidence:current` exits 0; serial readiness/DoD/matrix/
      transparency/handoff generators preserve `NOT_READY`, 11 external
      blockers, `publicBeta=false`, and `sourceTreeMatches=true`.

## Done-evidence (world-state proof, named BEFORE building)

An authorised external reviewer can download one generated kit, see exactly
what evidence each release gate requires, fill the fields without touching the
canonical ledger, and submit a hash-bound artifact for validation. The kit is
observable in the repository and explicitly remains a pending handoff, not a
Public Beta approval.

## Evidence (paste the actual proof here when done)

(empty until done)
