# C-019 — Hệ logo dấu ấn dòng chảy lịch sử

status: done
deps: [C-018]
implements: none

## Scope

Thay monogram chữ `QS` bằng một biểu tượng SVG dùng chung, kết hợp vòng tròn/trống đồng
và hai nét dòng chảy lịch sử, áp dụng nhất quán ở header/footer công khai, trang đăng
nhập, sidebar quản trị, tài liệu API và favicon. Giữ nguyên wordmark “Quân Sử Việt”,
navigation, auth, API, dữ liệu và nội dung; logo dùng `currentColor`, stroke-only,
không tải asset ngoài và vẫn rõ ở kích thước 38–42 px.

## Allowed files

- `src/components/BrandMark.tsx`
- `src/components/public/PublicShell.tsx`
- `src/components/admin/AdminShell.tsx`
- `src/components/admin/LoginForm.tsx`
- `src/app/docs/page.tsx`
- `src/app/icon.svg`
- `src/app/globals.css`
- `tests/public-ui/brand-system.test.ts`
- `tests/e2e/brand-system.spec.ts`
- `artifacts/brand-system/**`
- `cards/C-019.md`

## Verify (run these before calling the card done)

- [x] `npm run lint`, `npm run typecheck`, `npm test` và `npm run build` đều thoát 0.
- [x] Public header/footer, admin login/sidebar và docs dùng cùng `BrandMark`; không còn
  monogram text `QS`, ảnh raster hoặc asset/network dependency cho logo.
- [x] Logo SVG dùng `currentColor`, stroke 1.6–2, có favicon cùng hình học và không làm
  thay đổi accessible name của brand links.
- [x] Pointer/keyboard brand link vẫn về đúng homepage; `/admin/login` và `/docs` hiển thị
  logo mới, auth/navigation/API không đổi.
- [x] Desktop/mobile không tràn ngang, axe không có Critical/Serious, LCP ≤ 2,5 giây;
  logo rõ ở 38–42 px và giữ typography/token hiện có.

## Done-evidence (world-state proof, named BEFORE building)

- URL HTTPS `/vi`, `/admin/login` và `/docs` hiển thị cùng logo SVG mới; keyboard trên
  brand link về homepage và favicon trả SVG thật.
- `artifacts/brand-system/brand-system-proof.json` ghi surface consistency, SVG geometry,
  keyboard navigation, favicon, accessibility, overflow và LCP.
- `artifacts/brand-system/brand-system-desktop.png` và
  `artifacts/brand-system/brand-system-mobile.png` chụp logo thật ở public/admin.

## Evidence (paste the actual proof here when done)

- Live HTTPS: `https://forecast-bachelor-sacrifice-elliott.trycloudflare.com`.
- `/vi`, public footer, `/admin/login`, admin sidebar và `/docs` dùng chung `BrandMark`;
  monogram chữ `QS` đã được thay bằng SVG stroke-only 1.8 dùng `currentColor`.
- `/icon.svg` trả 200 `image/svg+xml` và dùng cùng outer circle, sun và history-flow paths.
- Playwright live `tests/e2e/brand-system.spec.ts`: 1/1 pass; pointer VI và keyboard EN
  trên brand link đều về đúng homepage; admin login/docs hiển thị logo mới.
- `npm run lint`, `npm run typecheck`, `npm test` (26 files, 128 tests) và
  `npm run build`: exit 0. Hai test I/O seed/recovery đã được rerun riêng 4/4 trước full pass.
- Proof: `artifacts/brand-system/brand-system-proof.json`; axe Critical/Serious = 0,
  không overflow; LCP desktop 1456 ms, mobile 1428 ms.
- Screenshots: `artifacts/brand-system/brand-system-desktop.png` và
  `artifacts/brand-system/brand-system-mobile.png`.
