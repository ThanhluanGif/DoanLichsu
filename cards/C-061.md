# C-061 — Context-bound AI tutor and authenticated alpha access

status: done
deps: [C-059, C-060]
implements: [ai-tutor-safety]

## Scope

Sửa trợ giảng alpha để câu hỏi trên trang bài học luôn truy hồi đúng bài học hiện tại
qua `contextSlug`, và chỉ người dùng đã đăng nhập với vai trò biên tập/reviewer/admin mới
được gọi API alpha. Public Beta vẫn bị khóa; người chưa đăng nhập nhận lỗi an toàn.

## Allowed files

- `src/app/api/v1/[locale]/ai/answer/route.ts`
- `src/lib/ai/repository.ts`
- `src/components/public/ContextualTutor.tsx`
- `src/app/[locale]/[kind]/[slug]/page.tsx`
- `src/lib/openapi/public-catalog.ts`
- `tests/contract/ai-answer.test.ts`
- `tests/editorial-api/ai-answer.test.ts`
- `cards/C-061.md`

## Verify (run these before calling the card done)

- [x] Unit/contract test: context-bound question returns the requested lesson slug; a missing
  context does not silently select a different lesson.
- [x] API auth test: no session and spoofed alpha header return `401/403`; an authenticated
  reviewer session can obtain a grounded answer with citations.
- [x] OpenAPI documents cookie security and `contextSlug`.
- [x] `npm run lint`, `npm run typecheck`, `npm test`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

On the live alpha origin, an unauthenticated POST with `x-qsv-ai-alpha: internal` is rejected,
while a logged-in reviewer session receives a citation-backed answer whose `suggestedNext.slug`
matches the supplied lesson context. The lesson page sends `contextSlug` in its request body.

## Evidence (paste the actual proof here when done)

Contract proof: focused tests `2 files / 5 tests PASS`; context-bound retrieval returns the supplied published slug and an unknown context abstains. Route tests prove a spoofed alpha header without a session returns `401`, a session without the alpha header returns `403`, and a reviewer session returns `200` with citations and the requested context slug.

Regression proof: `npm run lint` PASS (existing warnings only); `npm run typecheck` PASS; `npm test` PASS (49 files, 219 tests); `npm run build` PASS.

OpenAPI proof: `/openapi.json` AI route declares `cookieAuth` security and `contextSlug`; Public Beta remains disabled.

Live proof: `artifacts/ai-product/ai-context-auth-live-proof.json` from commit `b3b5da6` on the production-like origin. The quick tunnel is temporary and explicitly not an official production domain.
