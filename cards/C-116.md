# C-116 — Surface published-history gap in DoD matrix

status: done
deps: C-115
implements: infra

## Scope

Add the published-content audit as a distinct Section 17 matrix row and DoD check.
The row must be `BLOCKED_INTERNAL` while 105 published rows lack audit history;
the audit must not be confused with Council sign-off or external gates. Preserve
Public Beta false and all eleven external blockers.

## Allowed files

scripts/year-one-dod-matrix.mjs
scripts/dod-audit.mjs
tests/operations/year-one-dod-matrix.test.ts
tests/operations/dod-audit.test.ts
artifacts/release/year-one-dod-matrix.json
artifacts/release/year-one-dod-matrix.md
artifacts/release/dod-audit.json
artifacts/release/dod-audit.md
cards/C-116.md

## Verify (run these before calling the card done)

- [x] `node scripts/dod-audit.mjs` reports a `published-content-history` check
      with `BLOCKED_INTERNAL` and keeps 11 external blockers.
- [x] `node scripts/year-one-dod-matrix.mjs` includes the same internal row.
- [x] Focused DoD/matrix tests pass; `npm run typecheck` passes.

## Done-evidence (world-state proof, named BEFORE building)

DoD surfaces visibly identify the 105-row correction-history gap instead of implying
that mandatory coverage alone satisfies all content requirements.

## Evidence (paste the actual proof here when done)

DoD now reports `published-content-history: BLOCKED_INTERNAL` with
`missingContent: 105`; the matrix contains the same internal row and retains 11
external blockers. Focused DoD/matrix tests passed (2 tests), typecheck passed,
and no audit history was fabricated.
