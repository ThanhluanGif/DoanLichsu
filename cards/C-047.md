# C-047 — N-010 contextual AI tutor internal alpha

status: done
deps: [C-046]
implements: [ai-tutor]

## Scope

Tạo contextual tutor CLI/internal-alpha wrapper quanh AI gateway: nhận lesson slug/grade,
hiển thị 3 guided prompts (giải thích, kiểm chứng nguồn, câu hỏi Socratic), chuyển câu hỏi
qua contract grounded/abstain và gắn nhãn rõ `INTERNAL_ALPHA_NOT_PUBLIC`. Không tài khoản,
không lưu danh tính, không mở free-form cho trẻ; khi gateway fail tutor trả fallback bài
học/search.

## Allowed files

- `scripts/ai-tutor.mjs`
- `package.json`
- `artifacts/ai-tutor/**`
- `cards/C-047.md`

## Verify (run these before calling the card done)

- [x] Tutor alpha returns guided prompts, grounded answer/citations and alpha safety label.
- [x] Unsupported question returns abstention/fallback and never public-beta status.
- [x] `npm run lint`, `npm run typecheck`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Internal-alpha transcript artifacts show contextual prompts, grounded citation path,
abstention fallback, and explicit non-public safety boundary.

## Evidence (paste the actual proof here when done)

- Contextual alpha transcript has 3 guided prompts, grounded citations and explicit `INTERNAL_ALPHA_NOT_PUBLIC`: `artifacts/ai-tutor/alpha.json`.
- Injection transcript abstains and falls back to lesson/search with no identity storage: `artifacts/ai-tutor/abstain.json`.
- Consolidated proof: `artifacts/ai-tutor/tutor-proof.json`; public beta remains disabled because eval target is incomplete.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit 0.
