# C-125 — Add reviewer attestation form to content editor

status: done
deps: C-124
implements: infra

## Scope

Add a reviewer-only attestation form to the published content editor. The form
must be visible only for a published item without an existing history review,
require an evidence locator, a note, and an explicit HUMAN_REVIEWED checkbox,
submit the current version to C-123, and reload the queue state after success.

## Allowed files

src/components/admin/ContentEditor.tsx
tests/admin-ui/static-contract.test.ts
cards/C-125.md

## Verify (run these before calling the card done)

- [x] Reviewer UI contains the attestation fields, explicit checkbox, current
      version, endpoint path, and successful reload behavior; editors cannot
      see or submit it.
- [x] Focused static-contract tests and `npm run typecheck` pass.

## Done-evidence (world-state proof, named BEFORE building)

An authenticated reviewer can complete one attestation directly from the content
editor and receive a success state; no automatic or bulk history is created.

## Evidence (paste the actual proof here when done)

Verified 2026-08-13:

- `ContentEditor` now renders a reviewer-only published-content attestation
  panel with evidence locator, review note, explicit `HUMAN_REVIEWED` checkbox,
  current `detail.version`, and the C-123 endpoint path.
- The submit control remains disabled until all three fields are completed;
  success state is shown in the current session and no bulk operation exists.
- `npx vitest run tests/admin-ui/static-contract.test.ts
  tests/editorial-api/published-history-review.test.ts
  tests/editorial-api/published-history-queue.test.ts` passed 3 files, 9 tests.
- `npm run typecheck` exited 0.
