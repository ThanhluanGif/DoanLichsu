# C-102 — Refresh dashboard provenance after release evidence

status: done
deps: C-101
implements: infra

## Scope

Regenerate the transparency dashboard from the current release evidence so its
tested commit and source-tree hash match the current release. Preserve the
implementation-only disclosure, `NOT_READY`, Public Beta disabled, and 11 blockers.

## Allowed files

artifacts/transparency/dashboard.json
artifacts/transparency/dashboard.md
cards/C-102.md

## Verify (run these before calling the card done)

- [x] `node scripts/transparency-dashboard.mjs` exits 0.
- [x] `npm test -- --run tests/operations/transparency-dashboard.test.ts` passes.
- [x] Dashboard release source hash matches current release evidence and
      `sourceTreeMatches: true`.

## Done-evidence (world-state proof, named BEFORE building)

The checked-in transparency dashboard contains the current tested commit and source
hash, with 11 blockers and Public Beta disabled.

## Evidence (paste the actual proof here when done)

`node scripts/transparency-dashboard.mjs` exited 0; focused transparency test
passed (1 test). The dashboard now matches release evidence commit
`bf1b48dd499bd04ccfaab0c4d772a2c7fa2f8434` and source hash
`3560a5d342f018414ef7596b8dc37b561e0f7cea6854c18f23f334a5af7095a1`, with
`sourceTreeMatches: true`, 11 blockers, and `publicBeta: false`.
