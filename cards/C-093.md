# C-093 — Harden external-evidence ledger coherence

status: done
deps: C-092
implements: infra

## Scope

Make the external-evidence verifier reject internally contradictory ledgers. A
ledger may use `READY` only when all eleven required items are `PASS`, and the
top-level `realCouncilSignoff`, `realPilotCompleted`, and `officialProductionDomain`
flags must agree with their corresponding item statuses. Keep the existing
owner/artifact/SHA validation and the honest pending-ledger behavior unchanged.

## Allowed files

scripts/external-evidence-verify.mjs
tests/operations/external-evidence-verify.test.ts
artifacts/operations/external-evidence-verification.json
artifacts/operations/external-evidence-verification.md
cards/C-093.md

## Verify (run these before calling the card done)

- [x] `npm test -- --run tests/operations/external-evidence-verify.test.ts`
      passes, including contradictory READY and top-level flag fixtures.
- [x] `node scripts/external-evidence-verify.mjs` exits 0 for the real pending
      ledger and writes `PASS_LEDGER_VALID`, `releaseAllowed: false`, with all
      eleven gates still pending.
- [x] `npm run typecheck` passes.

## Done-evidence (world-state proof, named BEFORE building)

The checked-in verification report records the real ledger as valid but not
releaseable, and contradictory temporary ledgers fail with named coherence errors
instead of being accepted as external evidence.

## Evidence (paste the actual proof here when done)

`npm test -- --run tests/operations/external-evidence-verify.test.ts`: 5 tests
passed. `npm run typecheck`: passed. `node scripts/external-evidence-verify.mjs`:
`{"status":"PASS_LEDGER_VALID","releaseAllowed":false,"pending":11,"errors":0}`.
Temporary contradictory READY and top-level claim fixtures fail with explicit
coherence errors; no external ledger item was changed.
