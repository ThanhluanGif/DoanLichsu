# C-004 — Xây governed editorial API với auth, RBAC và publish workflow

status: todo
deps: C-003
implements: FR8, FR9, FR10, FR11, FR13

## Scope

Xây một đường biên tập được quản trị end-to-end ở API: login/logout/me với session cookie, ba role và deny server-side; CRUD content/translation/source/media metadata; state transitions submit/approve/reject/publish/archive; user management và audit log. Publish theo locale bị chặn nếu thiếu source, translation fields hoặc media credit/license/alt, mutation dùng optimistic version và mọi endpoint khớp contract/OpenAPI.

## Allowed files

- `.env.example`
- `DEBT.md`
- `flow/05-contract.md`
- `package.json`
- `package-lock.json`
- `migrations/**`
- `scripts/seed.*`
- `src/lib/auth/**`
- `src/lib/audit/**`
- `src/lib/content/**`
- `src/lib/db/**`
- `src/lib/openapi/**`
- `src/lib/rate-limit/**`
- `src/lib/validation/**`
- `src/app/api/v1/auth/**`
- `src/app/api/v1/admin/**`
- `src/app/docs/**`
- `tests/foundation/migrations.test.ts`
- `tests/public-api/seed-openapi.test.ts`
- `tests/editorial-api/**`
- `cards/C-004.md`

## Verify (run these before calling the card done)

- [ ] `npm test -- tests/editorial-api` exit 0 với happy/negative cases cho session, Origin check, Argon2 verify, rate limit, RBAC và không trả password/session secret.
- [ ] Integration test chứng minh Editor bị 403 khi approve/publish/manage user; Reviewer bị 403 khi manage user; Admin không thể disable/demote Admin cuối cùng.
- [ ] Integration test tạo draft VI, thêm source, submit, reject bắt buộc reason, resubmit, approve/publish VI; public thấy VI, alternate EN `null`; sau approve/publish EN thì alternate hai chiều đúng.
- [ ] Publish thiếu source hoặc media credit/license/alt trả 422 `details.violations`; stale version trả 409; public vẫn không thấy IN_REVIEW/REJECTED/APPROVED.
- [ ] Curl live bằng cookie thật qua login→me→workflow và Admin audit logs cho thấy actor/action/object/time, không chứa password/token; endpoint hiện trong `/openapi.json` và `/docs`.

## Done-evidence (world-state proof, named BEFORE building)

Trên URL live, chuỗi curl có cookie tạo một draft, chuyển trạng thái hợp lệ và làm nó xuất hiện ở public detail; curl trái role nhận 403, publish thiếu nguồn nhận 422 và audit query ghi đúng hành động mà không lộ secret.

## Evidence (paste the actual proof here when done)

(empty until done)
