# C-004 — Xây governed editorial API với auth, RBAC và publish workflow

status: done
deps: C-003
implements: FR8, FR9, FR10, FR11, FR13

## Scope

Xây một đường biên tập được quản trị end-to-end ở API: login/logout/me với session cookie, ba role và deny server-side; CRUD content/translation/source/media metadata; state transitions submit/approve/reject/publish/archive; user management và audit log. Publish theo locale bị chặn nếu thiếu source, translation fields hoặc media credit/license/alt, mutation dùng optimistic version và mọi endpoint khớp contract/OpenAPI.

## Allowed files

- `.env.example`
- `DEBT.md`
- `flow/05-contract.md`
- `package.json`
- `package-lock.json`
- `migrations/**`
- `scripts/seed.*`
- `src/lib/auth/**`
- `src/lib/audit/**`
- `src/lib/content/**`
- `src/lib/db/**`
- `src/lib/openapi/**`
- `src/lib/rate-limit/**`
- `src/lib/validation/**`
- `src/app/api/v1/auth/**`
- `src/app/api/v1/admin/**`
- `src/app/docs/**`
- `tests/foundation/migrations.test.ts`
- `tests/public-api/seed-openapi.test.ts`
- `tests/editorial-api/**`
- `cards/C-004.md`

## Verify (run these before calling the card done)

- [x] `npm test -- tests/editorial-api` exit 0 với happy/negative cases cho session, Origin check, Argon2 verify, rate limit, RBAC và không trả password/session secret.
- [x] Integration test chứng minh Editor bị 403 khi approve/publish/manage user; Reviewer bị 403 khi manage user; Admin không thể disable/demote Admin cuối cùng.
- [x] Integration test tạo draft VI, thêm source, submit, reject bắt buộc reason, resubmit, approve/publish VI; public thấy VI, alternate EN `null`; sau approve/publish EN thì alternate hai chiều đúng.
- [x] Publish thiếu source hoặc media credit/license/alt trả 422 `details.violations`; stale version trả 409; public vẫn không thấy IN_REVIEW/REJECTED/APPROVED.
- [x] Curl live bằng cookie thật qua login→me→workflow và Admin audit logs cho thấy actor/action/object/time, không chứa password/token; endpoint hiện trong `/openapi.json` và `/docs`.

## Done-evidence (world-state proof, named BEFORE building)

Trên URL live, chuỗi curl có cookie tạo một draft, chuyển trạng thái hợp lệ và làm nó xuất hiện ở public detail; curl trái role nhận 403, publish thiếu nguồn nhận 422 và audit query ghi đúng hành động mà không lộ secret.

## Evidence (paste the actual proof here when done)

Verified 2026-08-06 on standalone `a9ad27b` at `http://127.0.0.1:3404` using fresh migration 1→3 and seed stdout `{"contentNodes":50,"translations":100,"sources":50,"users":3}`.

- Automated: `npm test` = 7 files / 36 tests passed; editorial suite = 6 passed; lint, strict TypeScript, production build and `verify:standalone` all exit 0. Negative coverage includes tampered/logout-invalidated sessions, exact Origin, raw HTML/oversized streaming JSON, sequential and concurrent datastore rate limits, Argon capacity load shedding, wrong-role routes, last-Admin cross-process race, stale versions, archived/published immutability, production seed secrets, missing source/media metadata and audit redaction.
- Live cookie: ADMIN/EDITOR/REVIEWER login all returned 200 with `HttpOnly; SameSite=Lax; Secure`; `/auth/me` returned the expected role. Editor manage-user/approve returned 403; invalid Origin returned 403 `INVALID_ORIGIN`.
- Live workflow object `c98a0578-2b2f-435e-86ce-36fd635a9d8e`: DRAFT 201 → IN_REVIEW v2 → REJECTED v3 with required reason → resubmit/APPROVED v5. Publish without source returned 422 with `details.violations=["content requires at least one source"]`; stale v5 after source attachment returned 409; publish v6 returned 200/PUBLISHED v7. Public VI detail then returned 200 with the attached citation and alternate EN `null`; pre-publish public read returned 404.
- Live audit returned seven ordered events with actor/action/objectId/createdAt and no password/token/session secret. `/docs` returned 200 and `/openapi.json` contained `/api/v1/admin/contents/{id}/publish`. Final immutable-reference regression: Editor PATCH of the published source returned 422 `PUBLISHED_REFERENCE_IMMUTABLE`, and the public citation remained unchanged; `/healthz` reported version `a9ad27b`.
- Native STRIDE: spoofing (sealed cookie + Argon2id + sessionVersion), tampering (Origin + optimistic versions + immutable published rows), repudiation (sanitized audit), disclosure (server RBAC/no secrets/redacted dashboard), DoS (persistent reservation + trusted-IP option + 8-check semaphore + streamed 1 MiB cap), elevation (deny-by-default role guards + serialized last-Admin invariant) all triaged with zero Critical/High remaining.
- Independent review: TypeScript reviewer found 3 High and first post-repair security reviewer found 2 High; all were reproduced, repaired and regression-tested. Generic acceptance review approved with zero Critical/High and its one Medium workflow drift was fixed. A later security pass found one published-reference High, fixed at `a9ad27b`; fresh zero-High reviewer then APPROVED and independently reran tests/build. Antigravity liveness passed but the engine refused repository security audit, so it was recorded as unavailable and never counted as approval.
