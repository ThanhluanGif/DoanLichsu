# C-064 — Honest readiness and coverage reporting

status: done
deps: [C-062, C-063]
implements: [release-transparency]

## Scope

Sửa các checker/artifact để phản ánh đúng bằng chứng hiện có: coverage dashboard đọc summary
định lượng; readiness dùng đường dẫn absolute thật, liên kết backup rehearsal C-063 và report
AI 500/500 machine PASS. Các blocker chưa có bằng chứng external (domain, uptime, council,
privacy, load) vẫn giữ nguyên và status release vẫn FAIL/NOT_READY.

## Allowed files

- `scripts/transparency-dashboard.mjs`
- `scripts/production-readiness.mjs`
- `artifacts/production-readiness/**`
- `artifacts/transparency/**`
- `cards/C-064.md`

## Verify (run these before calling the card done)

- [x] `npm run production:readiness` records absolute DB path, AI eval 500/500 and backup
  rehearsal proof while remaining FAIL because fixed domain/load/council/privacy are absent.
- [x] `node scripts/curriculum-coverage.mjs` source summary is rendered as a numeric dashboard.
- [x] `npm run lint`, `npm run typecheck`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Readiness and public transparency artifacts agree on the same facts: 46/46 mandatory coverage,
500/500 machine eval, verified disposable backup rehearsal, and explicit unresolved external
blockers. No green Public Beta claim is emitted.

## Evidence (paste the actual proof here when done)

Readiness proof: `npm run production:readiness` exits 1 by design and records absolute existing DB, quality commands all exit 0, disposable backup rehearsal `verified=true`, AI eval `500/500 PASS`, and unresolved fixed-domain/load/uptime/council/privacy blockers in `artifacts/production-readiness/report.json`.

Transparency proof: `node scripts/transparency-dashboard.mjs` now reports coverage `46/46`, AI `500/500`, backup rehearsal `true`, while release remains `NOT_READY`; artifact and markdown are in `artifacts/transparency/`.
