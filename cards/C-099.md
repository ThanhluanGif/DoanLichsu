# C-099 — Bind operations readiness to the external ledger snapshot

status: done
deps: C-098
implements: infra

## Scope

Record the SHA-256 and exact pending-gate count of the external evidence ledger in
the operations readiness report. Require the report to see all eleven required
gates and preserve the no-claim flags for council, pilot, and official production.
This is provenance only; it must not mark any gate PASS or enable Public Beta.

## Allowed files

scripts/operations-check.mjs
tests/operations/operations-check.test.ts
artifacts/operations/report.json
artifacts/operations/report.md
cards/C-099.md

## Verify (run these before calling the card done)

- [x] `npm test -- --run tests/operations/operations-check.test.ts` passes and
      checks ledger hash, 11 gates, and no-claim flags.
- [x] `npm run typecheck` passes.
- [x] `node scripts/operations-check.mjs` exits 0 with status `PASS`, external
      evidence pending, and 11 ledger items.

## Done-evidence (world-state proof, named BEFORE building)

The operations artifact records `ledgerSha256`, `pendingGateCount: 11`,
`realPilotCompleted: false`, `realCouncilSignoff: false`, and
`fixedProductionDomain: false`, while retaining `status: PASS` for the checklist
only (not release approval).

## Evidence (paste the actual proof here when done)

`npm test -- --run tests/operations/operations-check.test.ts`: 1 test passed;
`npm run typecheck` passed; `node scripts/operations-check.mjs` exited 0 with
`status: PASS`, `externalEvidence: PENDING_EXTERNAL_EVIDENCE`, `ledgerItems: 11`,
`pendingGateCount: 11`, `noExternalApprovalClaim: true`, and ledger SHA-256
`d85b395290e9bbd3b4bdf78c79008423262ea54f923ff61f580d3dd57d93c51d`.
