# C-128 — Repair history queue lint and effect scheduling

status: done
deps: C-124
implements: infra

## Scope

Repair the published-history queue component so its asynchronous initial load
does not trigger the React `set-state-in-effect` lint error, while preserving
pagination, role gating, loading, and error behavior.

## Allowed files

src/components/admin/PublishedHistoryQueue.tsx
cards/C-128.md

## Verify (run these before calling the card done)

- [x] `npm run lint -- --quiet` exits 0 and queue behavior remains covered by
      the existing focused tests.
- [x] `npm run typecheck` passes.

## Done-evidence (world-state proof, named BEFORE building)

The review queue is lint-clean and still presents its authenticated paginated
operator workflow without changing any audit or release state.

## Evidence (paste the actual proof here when done)

Verified 2026-08-13:

- `npm run lint -- --quiet` exited 0 after moving the initial queue load onto a
  scheduled callback and keeping pagination loads explicit.
- `npx vitest run tests/editorial-api/published-history-queue.test.ts
  tests/admin-ui/static-contract.test.ts` passed 2 files, 7 tests.
- `npm run typecheck` exited 0.
