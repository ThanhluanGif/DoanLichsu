# C-026 — Trang bài học có phân tích và nguồn tách bạch

status: done
deps: [C-025]
implements: [FR17]

## Scope

Mở rộng content/translation cho learning objectives, original summary, analysis, debates và
`asOf`, rồi render một layout lesson duy nhất phân biệt rõ tóm tắt biên tập, phân tích,
luận điểm đã kiểm chứng, góc nhìn còn tranh luận và nguồn. Existing non-lesson detail dùng
shape null/stub và không đổi hành vi.

## Allowed files

- `flow/05-contract.md`
- `DESIGN.md`
- `migrations/0006_lessons.sql`
- `package.json`
- `scripts/contract-check.mjs`
- `tests/foundation/migrations.test.ts`
- `tests/recovery/recovery.test.ts`
- `src/lib/content/**`
- `src/lib/public-client/**`
- `src/components/public/**`
- `src/components/admin/ContentEditor.tsx`
- `src/app/[locale]/[kind]/[slug]/page.tsx`
- `src/app/globals.css`
- `tests/public-api/**`
- `tests/editorial-api/**`
- `tests/public-ui/**`
- `tests/e2e/lesson-detail.spec.ts`
- `artifacts/lesson-detail/**`
- `cards/C-026.md`

## Verify (run these before calling the card done)

- [x] Migration/contract/admin/public tests và lint/typecheck/build thoát 0.
- [x] HTML/JSON phân biệt các section; claim/evidence chỉ hiện theo verification law.
- [x] `asOf` bắt buộc cho update đến hiện tại; non-lesson vẫn render an toàn với null.
- [x] Detail đạt mobile/desktop/keyboard/axe và nguồn mở được bằng link thật.

## Done-evidence (world-state proof, named BEFORE building)

Một lesson HTTPS song ngữ hiển thị đủ section và nguồn; live JSON, screenshots và DB row
chứng minh shape, reviewer/asOf và claim/evidence.

## Evidence (paste the actual proof here when done)

### Local verification

- `npm run typecheck` — exit 0.
- `npm run lint` — exit 0.
- `npm test` — `33` test files / `171` tests passed.
- `npm run build` — exit 0; route table keeps the shared detail route and public
  curriculum/API routes, with the lesson shape compiled into the server bundle.
- `npm run verify:standalone` — exit 0 and checks
  `.next/standalone/migrations/0006_lessons.sql` alongside the runtime assets.
- Temporary migrated+seeded DB proof: migration ledger applies `[1,2,3,4,5,6]`;
  `lesson_translations` contains both locales with 3 objectives, 2 debates and
  `as_of=2026-08-10T00:00:00.000Z`; `content_claims` remains `0` and no source is
  promoted by migration.
- `git diff --check` — exit 0.

### Live HTTPS verification

- URL: https://automobile-sunglasses-swimming-monitored.trycloudflare.com
- Container log: `{"applied":[6],"currentVersion":6}`; `/healthz` returns HTTP 200
  with `database:"ok"`.
- `GET /api/v1/vi/contents/EVENT/chien-dich-dien-bien-phu` — lesson is non-null,
  3 learning objectives, 2 debates, `asOf=2026-08-10T00:00:00.000Z`, 0 public claims
  and 1 linked source.
- `GET /api/v1/en/contents/EVENT/battle-of-dien-bien-phu` — lesson is non-null with
  3 objectives and 2 debates; the same `asOf` is preserved.
- `GET /api/v1/en/contents/PERSON/vo-nguyen-giap-en` — `lesson:null`, `asOf:null`;
  the non-lesson detail contract remains safe.
- Database row proof from the running container: schema version `6`, two lesson rows
  (`vi`/`en`) for `event-dien-bien-phu`, each with `objectives=3`, `debates=2` and the
  editorial reviewer label; existing sources remain `NEEDS_REVIEW` and claims remain
  `0`, so no unverified claim is displayed as fact.

### Browser proof

Target E2E passed:

```text
E2E_BASE_URL=https://automobile-sunglasses-swimming-monitored.trycloudflare.com \
npx playwright test tests/e2e/lesson-detail.spec.ts
1 passed (27.5s)
```

The proof covers the VI and EN JSON, distinct HTML sections for objectives, editorial
summary, analysis, debates, verified claims and sources, real source link, keyboard
copy-link, back/forward, and the non-lesson null shape. Axe reports 0 critical/serious
violations on desktop and mobile; desktop is 1440px wide with `scrollWidth=1440`, mobile
is 390px wide with `scrollWidth=390`.

Proof JSON: `artifacts/lesson-detail/lesson-proof.json`

```json
{
  "lesson": {"objectives": 3, "debates": 2, "asOf": "2026-08-10T00:00:00.000Z", "claimsPublished": 0, "sources": 1, "copyLink": true, "backForward": true, "keyboard": true},
  "nonLesson": {"path": "/en/people/vo-nguyen-giap-en", "lesson": null},
  "accessibility": {"desktopCriticalOrSerious": 0, "mobileCriticalOrSerious": 0},
  "desktopViewport": {"clientWidth": 1440, "scrollWidth": 1440},
  "mobileViewport": {"clientWidth": 390, "scrollWidth": 390}
}
```

Screenshots:

- `artifacts/lesson-detail/lesson-desktop.png` — Vietnamese Điện Biên Phủ lesson with
  source-separated panels and the live source link.
- `artifacts/lesson-detail/lesson-mobile.png` — English Điện Biên Phủ lesson at 390px,
  including keyboard copy-link and every source-aware section.
