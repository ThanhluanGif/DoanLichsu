# C-042 — N-004 Wikimedia metadata importer và review queue

status: done
deps: [C-041]
implements: [source-ingestion]

## Scope

Xây importer metadata Wikimedia Commons phía server, có User-Agent, timeout, batch nhỏ,
revision/credit/license extraction và review queue dạng JSON. Importer chỉ lưu metadata,
không tải binary, không auto-publish và không biến Wikimedia thành nguồn duy nhất cho
claim nhạy cảm. URL API hoặc fixture có thể truyền vào để kiểm thử tái lập; mỗi record
được gắn `PENDING_REVIEW` và `rightsStatus: LINK_ONLY` mặc định.

## Allowed files

- `scripts/wikimedia-import.mjs`
- `package.json`
- `artifacts/wikimedia/**`
- `cards/C-042.md`

## Verify (run these before calling the card done)

- [x] Fixture import exits 0, emits queue JSON/Markdown, preserves revision/license
      metadata and marks every item `PENDING_REVIEW`/`LINK_ONLY`.
- [x] Live Wikimedia API import (one file title) exits 0 or records a clear fail-closed
      report; no binary is downloaded and no auto-publish flag is present.
- [x] `npm run lint`, `npm run typecheck`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Review queue artifact containing Wikimedia page/revision identifiers, source/description
URLs, creator, credit, license fields and explicit pending rights decision; a live API
capture or fail-closed diagnostic is attached.

## Evidence (paste the actual proof here when done)

- Fixture metadata import passed with page ID, revision, artist, credit and license fields; record is `PENDING_REVIEW` + `LINK_ONLY`, `binaryDownloaded=false`, `autoPublished=false`: `artifacts/wikimedia/review-queue.json` and `.md`.
- Live Commons API import passed for `File:Flag of Vietnam.svg` (page 383863, revision 1254205846); metadata only, no binary download or auto-publish: `artifacts/wikimedia/live-report.json`.
- Network failure fixture exited 1 with explicit fail-closed remediation: `artifacts/wikimedia/live-fail.json`.
- Consolidated proof: `artifacts/wikimedia/wikimedia-proof.json`.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit 0; `next-env.d.ts` restored after build.
