# C-179 — Rebind release evidence to the post-C-178 main state

status: todo
deps: C-178
implements: infra

## Scope

Run the local release-evidence suite against the exact clean `main` state after
C-178, then regenerate every release/readiness/handoff artifact that consumes
that evidence. The purpose is provenance: `current-head-evidence.json` must
record the actual checked-out commit and a matching source-tree hash, rather
than the parent commit recorded during the C-178 branch run. Preserve
`NOT_READY`, `publicBeta=false`, the 105-row human-history gap and all 11
external blockers. Do not alter product source, database, governance, rights,
or human-review ledgers; this is implementation evidence only.

## Allowed files

KE_HOACH_12_THANG_CONG_TRI_THUC_LICH_SU_VIET_NAM_AI.md
artifacts/operations/external-evidence-handoff.json
artifacts/operations/external-evidence-handoff.md
artifacts/operations/report.json
artifacts/operations/report.md
artifacts/release/current-head-evidence.json
artifacts/release/current-head-evidence.md
artifacts/production-readiness/report.json
artifacts/production-readiness/report.md
artifacts/release/dod-audit.json
artifacts/release/dod-audit.md
artifacts/release/dod-matrix-consistency.json
artifacts/release/dod-matrix-consistency.md
artifacts/release/global-master-consistency.json
artifacts/release/global-master-consistency.md
artifacts/release/year-one-dod-matrix.json
artifacts/release/year-one-dod-matrix.md
artifacts/transparency/dashboard.json
artifacts/transparency/dashboard.md
cards/C-179.md

## Verify (run these before calling the card done)

- [ ] `RELEASE_EVIDENCE_RUN=1 npm run release:evidence:current` exits 0 and
      records the exact clean `main` commit, source-tree match, quality
      `[0,0,0,0]`, local health/OpenAPI/search `200/200/200`, security headers
      `true`, backup/restore checksums `true/true`, and `databaseMutation=false`.
- [ ] Run serially: `npm run production:readiness`, `npm run operations:check`,
      `node scripts/dod-audit.mjs` (intentional exit 1 with `NOT_READY`),
      `node scripts/dod-matrix-consistency.mjs`,
      `npm run transparency:dashboard`, and
      `npm run operations:evidence:handoff`; all non-audit commands exit 0,
      handoff has `pendingCount=11`, and no generated artifact invents evidence.
- [ ] `npm test -- --testTimeout=15000`, `npm run typecheck`,
      `bash /Users/admin/.agents/skills/flow/runner/flow.sh check C-179`, and
      `git diff --check` pass after finalization.

## Done-evidence (world-state proof, named BEFORE building)

The checked-in release and handoff artifacts identify the exact current main
commit used for the local production-like verification, with a matching source
hash and passing quality/runtime checks. The evidence remains explicitly local
only: no official production URL, external owner, reviewer, approval, pilot,
rights decision, DPIA approval, uptime record or Public Beta promotion is
created by this card.

## Evidence (paste the actual proof here when done)

(empty until done)
