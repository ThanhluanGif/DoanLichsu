# C-051 — N-014 correction inbox, moderation và changelog

status: done
deps: [C-048, C-050]
implements: [corrections]

## Scope

Tạo correction inbox append-only dạng JSONL với schema tối thiểu: content/claim, category,
evidence locator, urgency, consent và timestamp; moderation state machine RECEIVED →
TRIAGED → IN_REVIEW/NEEDS_COUNCIL → CORRECTED/DECLINED. Có SLA 1 ngày cho rights/safety,
3 ngày cho issue hợp lệ, public changelog không lộ reporter. Red input bị từ chối nếu có
PII quá mức hoặc thiếu nội dung bắt buộc.

## Allowed files

- `scripts/correction-inbox.mjs`
- `package.json`
- `artifacts/corrections/**`
- `cards/C-051.md`

## Verify (run these before calling the card done)

- [x] Valid report is accepted, redacted and appears in inbox/changelog with SLA/state.
- [x] PII-heavy/empty report exits 1 and is not appended.
- [x] `npm run lint`, `npm run typecheck`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Append-only correction inbox and public-safe changelog artifact with state/SLA audit trail;
invalid report proof shows no write.

## Evidence (paste the actual proof here when done)

- Valid correction entered append-only inbox as `RECEIVED`, SLA 72h, reporter data excluded from public changelog: `artifacts/corrections/report.json`, `inbox.jsonl`, `changelog.json`.
- PII-heavy/empty locator input exited 1 with `appended=false`: `artifacts/corrections/red-report.json`.
- Consolidated proof: `artifacts/corrections/correction-proof.json`.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit 0.
