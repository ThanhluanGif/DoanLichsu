# C-097 — Make DoD audit verify release-source coherence

status: done
deps: C-096
implements: infra

## Scope

Require the year-one DoD audit's current-release check to include the recorded
source-tree hash and compare it with the checked-out source tree. A stale or
missing hash must block the local release-evidence check; a matching hash remains
local-only and does not alter the eleven external blockers or Public Beta flag.

## Allowed files

scripts/dod-audit.mjs
tests/operations/dod-audit.test.ts
scripts/source-tree-hash.mjs
artifacts/release/current-head-evidence.json
artifacts/release/current-head-evidence.md
artifacts/release/dod-audit.json
artifacts/release/dod-audit.md
cards/C-097.md

## Verify (run these before calling the card done)

- [x] `npm run release:evidence:current` exits 0 and records a source-tree hash.
- [x] `npm test -- --run tests/operations/dod-audit.test.ts` passes, including
      the current-release source hash assertion.
- [x] `node scripts/dod-audit.mjs` reports `NOT_READY`, `publicBeta: false`, and
      exactly 11 external blockers.

## Done-evidence (world-state proof, named BEFORE building)

The checked-in DoD report records the current release source hash match as a
`PASS_LOCAL_ONLY` check while preserving `NOT_READY`, `publicBeta: false`, and
all 11 pending external gates.

## Evidence (paste the actual proof here when done)

`npm run release:evidence:current` passed with quality `[0,0,0,0]`, local
health/OpenAPI/search `200/200/200`, security headers true, and backup/restore
checksums true. `npm test -- --run tests/operations/dod-audit.test.ts`: 1 test
passed; `npm run typecheck` passed. `node scripts/dod-audit.mjs` reports
`NOT_READY`, `publicBeta: false`, 14 checks, and 11 external blockers; the
current-release check is `PASS_LOCAL_ONLY` with matching source hash
`254cd8f5c165f4bf0da4f67df5afaac7c9f135048291d6e1fb67c67ef4257124`.
