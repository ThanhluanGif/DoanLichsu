# C-070 — Frozen-set AI configuration comparison

status: done
deps: [C-060, C-069]
implements: [ai-evaluation]

## Scope

Chạy cùng frozen 500-question set qua hai configuration deterministic được định danh rõ
(baseline retrieval và strict-context retrieval), thu accuracy/citation/refusal/leak/latency.
Báo cáo phải nói rõ đây là hai config của cùng gateway, không giả là hai model độc lập và
không biến machine comparison thành Hội đồng phê duyệt.

## Allowed files

- `scripts/ai-config-compare.mjs`
- `artifacts/ai-eval/**`
- `cards/C-070.md`

## Verify (run these before calling the card done)

- [x] Comparison runs all 500 questions for both configs against the same input checksum.
- [x] Report records per-config metrics and explicitly labels model independence and human
  approval status honestly.

## Done-evidence (world-state proof, named BEFORE building)

`artifacts/ai-eval/config-comparison.json` contains two reproducible config runs, frozen-set
hash, metrics and a `PENDING_HUMAN_MODEL_REVIEW` note; Public Beta remains disabled.

## Evidence (paste the actual proof here when done)

Proof: `node scripts/ai-config-compare.mjs` → `PASS_MACHINE_COMPARISON`, both configurations run 500/500 against frozen set SHA-256 `8b4171471c85a2035f0d3eb6a1e79260a10888760a68b2db8bffe30d8ad867b9`. Baseline and strict-context each report 100% machine accuracy/citation and 0% injection leak. Strict config applies a real minimum-two-term evidence threshold; report explicitly says same deterministic gateway, not two independent models, and `PENDING_HUMAN_MODEL_REVIEW`.
