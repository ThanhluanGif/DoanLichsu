# C-077 — Refresh release evidence on current HEAD

status: done
deps: [C-076]
implements: [release-evidence]

## Scope

Chạy release-check/recovery/security/performance evidence từ commit hiện tại, dùng database tạm
và standalone server riêng. Lưu commit hash, quality exit codes, contract, backup/restore,
dependency audit, security headers, accessibility/performance và live route proof. Không dùng
artifact cũ, không claim official domain/90-day uptime nếu không có.

## Allowed files

- `scripts/release-evidence-current.mjs`
- `artifacts/release/current-head-*.json`
- `artifacts/release/current-head-*.md`
- `cards/C-077.md`

## Verify (run these before calling the card done)

- [x] Evidence runner records current `git rev-parse HEAD`, lint/type/test/build, dependency
  audit, disposable backup/restore, local standalone health/OpenAPI/search/security headers.
- [x] Failed HTTPS tunnel is represented as `NOT_RUN_IN_THIS_LOCAL_RUN` rather than a false PASS.

## Done-evidence (world-state proof, named BEFORE building)

Current-HEAD release evidence artifact is reproducible and explicitly distinguishes local
production-like checks from official production/uptime/security approvals.

## Evidence (paste the actual proof here when done)

Proof: `node scripts/release-evidence-current.mjs` → commit `5f7dabb...`, quality 4/4 PASS, migrate/seed/backup/restore PASS, local health/OpenAPI/search `200/200/200`, search p95 ≈54 ms, security headers PASS, backup/restore checksum PASS, `databaseMutation=false`. Artifact: `artifacts/release/current-head-evidence.json`; HTTPS E2E and official-domain/uptime/external approvals remain explicitly pending.
