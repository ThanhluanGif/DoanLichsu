# C-091 — Add privacy notice routes to canonical sitemap

status: done
deps: [C-090]
implements: [privacy]

## Scope

Include the bilingual public privacy notice routes in the canonical sitemap, with
the same HTTPS origin and no API/admin URLs. Add a regression test for both locale
URLs so privacy transparency is discoverable.

## Allowed files

- `src/app/sitemap.ts`
- `tests/public-ui/sitemap.test.ts`
- `cards/C-091.md`

## Verify (run these before calling the card done)

- [x] `npm test -- --testTimeout=15000 tests/public-ui/sitemap.test.ts` passes with
      both privacy routes.
- [x] `npm run typecheck` and `npm run lint` pass.
- [x] Live HTTPS `/sitemap.xml` contains `/vi/privacy` and `/en/privacy`.

## Done-evidence (world-state proof, named BEFORE building)

The public sitemap advertises both privacy notices as canonical pages, making the
AI/privacy controls discoverable without implying legal approval.

## Evidence (paste the actual proof here when done)

Sitemap test → 3 tests passed. Build, typecheck and lint pass (7 existing warnings).
After restarting the standalone bundle on the HTTPS tunnel, live `/sitemap.xml`
returned HTTP 200 and contained both `/vi/privacy` and `/en/privacy`. The sitemap
exposes the notice as a canonical public page without changing its pending DPIA
status.
