# C-107 — Assert complete external-gate coverage in DoD matrix

status: done
deps: C-106
implements: infra

## Scope

Add a strict invariant to the year-one DoD matrix generator and test: all eleven
canonical external gate IDs must appear exactly once as `BLOCKED_EXTERNAL` or
`EVIDENCED_EXTERNAL`; no external gate may be silently omitted or represented as a
local implementation pass. Preserve `publicBeta: false` until every gate has real
artifact/owner evidence.

## Allowed files

scripts/year-one-dod-matrix.mjs
tests/operations/year-one-dod-matrix.test.ts
artifacts/release/year-one-dod-matrix.json
artifacts/release/year-one-dod-matrix.md
cards/C-107.md

## Verify (run these before calling the card done)

- [x] `npm test -- --run tests/operations/year-one-dod-matrix.test.ts` passes and
      asserts exact external-gate ID coverage and no fabricated evidence.
- [x] `node scripts/year-one-dod-matrix.mjs` exits 0 with 11 canonical blocked
      external rows (plus the separate Wikimedia rights-review row).
- [x] `npm run typecheck` passes.

## Done-evidence (world-state proof, named BEFORE building)

The matrix artifact contains exactly the eleven canonical external gate IDs once
each, all `BLOCKED_EXTERNAL`, while retaining `NOT_READY` and `publicBeta: false`.

## Evidence (paste the actual proof here when done)

`node scripts/year-one-dod-matrix.mjs` exited 0 with 18 rows; all eleven canonical
external ledger IDs appear exactly once and remain blocked, with Wikimedia rights
review represented as an additional external evidence row. Focused test passed (1
test), `npm run typecheck` passed, `publicBeta: false`, and no fabricated evidence
was introduced.
