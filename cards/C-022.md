# C-022 — Logo loading mang nhận diện thương hiệu

status: done
deps: [C-021]
implements: [FR21]

## Scope

Thay loading route công khai đang dùng skeleton + chữ bằng một brand mark SVG ở giữa trang,
có vòng nét và gợn sóng chuyển động ngắn theo ngôn ngữ bảo tàng hiện có. Loader không có
thời lượng tối thiểu, không che nội dung sau khi stream xong, giữ nhãn trạng thái chỉ dành
cho screen reader và trở thành logo tĩnh khi `prefers-reduced-motion: reduce`.

## Allowed files

- `DESIGN.md`
- `src/app/[locale]/loading.tsx`
- `src/app/globals.css`
- `tests/public-ui/brand-loader.test.ts`
- `tests/e2e/brand-loader.spec.ts`
- `artifacts/brand-loader/**`
- `cards/C-022.md`

## Verify (run these before calling the card done)

- [x] `npm run lint`, `npm run typecheck`, test loader và `npm run build` thoát 0.
- [x] Route loading chỉ render brand SVG/vòng trang trí trực quan, không còn skeleton/chữ
  hiển thị; có `role=status`, title tạm thời và nhãn song ngữ cho screen reader.
- [x] Desktop 1440 px, mobile 390 px không overflow/layout shift; axe không có
  Critical/Serious và route đích vẫn keyboard dùng được.
- [x] Reduced motion có `animation-name: none`, logo tĩnh/đọc được và không có overlay
  giữ trang sau khi nội dung sẵn sàng.

## Done-evidence (world-state proof, named BEFORE building)

Production-like streaming route có thể quan sát brand loader trước khi nội dung xuất hiện;
evidence gồm screenshot desktop/mobile, reduced-motion computed style, axe/overflow và xác
nhận keyboard trên trang đã tải trong `artifacts/brand-loader/brand-loader-proof.json`.

## Evidence (paste the actual proof here when done)

- Live HTTPS upstream: `https://backed-bar-acc-hang.trycloudflare.com`; `GET /healthz`
  trả `200 {"status":"ok","version":"0.1.0","database":"ok",...}` ngày 10/08/2026.
- Production standalone streaming proof PASS 1/1, dùng proxy **chỉ trong test** để giữ
  response detail đủ lâu cho loading boundary xuất hiện; production code không có delay,
  overlay, listener hoặc dependency mới. Artifact ghi upstream HTTPS ở
  `artifacts/brand-loader/brand-loader-proof.json`.
- Desktop 1440×1000: mark/orbit/ripple lần lượt có animation
  `brand-loader-breathe|brand-loader-orbit|brand-loader-ripple`; tâm emblem `(720,500)`
  trùng tâm viewport, CLS `0`, overflow `1440/1440`, axe Critical/Serious `0`.
- Mobile 390×844 với reduced motion: mọi animation là `none`, `rotate:none`, `scale:1`;
  tâm `(195,422)`, CLS `0`, overflow `390/390`, axe Critical/Serious `0`.
- Keyboard-only proof dùng Tab tới content link rồi Enter; title tạm
  `Quân Sử Việt — Đang tải / Loading` được thay bằng title detail; sau tải, skip-link và
  brand header nhận focus đúng thứ tự. Loader bị remove khỏi DOM.
- Screenshots đã kiểm tra trực quan:
  `artifacts/brand-loader/brand-loader-desktop.png` và
  `artifacts/brand-loader/brand-loader-mobile.png`.
- Final verification: `npm run lint`, `npm run typecheck`, `npm run build` exit 0;
  Vitest 29 files/142 tests PASS; loader static tests 2/2 PASS; Flow consistency PASS 14/14
  FR. Adversarial re-review PASS sau khi sửa keyboard, CLS/title, centering và cleanup.
