# C-098 — Publish release-source provenance in transparency dashboard

status: done
deps: C-097
implements: infra

## Scope

Expose the current release evidence's tested commit and source-tree SHA in the
transparency dashboard, alongside an explicit match flag. The dashboard must
remain implementation-only, `NOT_READY`, and Public Beta disabled; provenance
must never be represented as official production or external approval.

## Allowed files

scripts/transparency-dashboard.mjs
tests/operations/transparency-dashboard.test.ts
artifacts/release/current-head-evidence.json
artifacts/transparency/dashboard.json
artifacts/transparency/dashboard.md
cards/C-098.md

## Verify (run these before calling the card done)

- [x] `npm test -- --run tests/operations/transparency-dashboard.test.ts` passes
      and checks release commit/hash provenance plus 11 blockers.
- [x] `npm run typecheck` passes.
- [x] `node scripts/transparency-dashboard.mjs` exits 0 and writes matching
      `sourceTreeSha256` / `currentSourceTreeSha256` values.

## Done-evidence (world-state proof, named BEFORE building)

The transparency artifact contains `release.testedCommit`, matching source hashes,
`sourceTreeMatches: true`, `releaseStatus: NOT_READY`, `publicBeta: false`, and all
11 external blockers.

## Evidence (paste the actual proof here when done)

`npm run release:evidence:current` passed with quality `[0,0,0,0]`, local
health/OpenAPI/search `200/200/200`, security headers true, and backup/restore
checksums true. Focused transparency test passed (1 test), `npm run typecheck`
passed, and the dashboard command exited 0. The artifact records tested commit
`4c781ff049f34fdf735b0760c89f1992245d546c`, matching source hashes
`9fbbf4166f5586c8184559efea7ce2183d97822df44482d98102a4c847536ced`,
`sourceTreeMatches: true`, `NOT_READY`, `publicBeta: false`, and 11 blockers.
