# C-136 — Refresh release evidence after current global master commit

status: done
deps: C-135
implements: infra

## Scope

Regenerate current-head release evidence and the dependent production
readiness, DoD audit, matrix consistency, transparency and operations reports
after the C-135 global-plan commit. Run the generators serially so the
source-tree hash is coherent. Preserve `NOT_READY`, Public Beta false, the
105-row internal history gap and all 11 external blockers. Do not alter source,
database, governance, rights or human-review ledgers.

## Allowed files

artifacts/release/current-head-evidence.json
artifacts/release/current-head-evidence.md
artifacts/production-readiness/report.json
artifacts/production-readiness/report.md
artifacts/release/dod-audit.json
artifacts/release/dod-audit.md
artifacts/release/dod-matrix-consistency.json
artifacts/release/dod-matrix-consistency.md
artifacts/transparency/dashboard.json
artifacts/transparency/dashboard.md
artifacts/operations/report.json
artifacts/operations/report.md
cards/C-136.md

## Verify (run these before calling the card done)

- [x] `npm run release:evidence:current` exits 0 and current-head evidence
      records the checked-out commit and source hash.
- [x] Run `npm run production:readiness`, `npm run operations:check`,
      `node scripts/dod-audit.mjs` (expected NOT_READY exit 1),
      `node scripts/dod-matrix-consistency.mjs`, and
      `npm run transparency:dashboard` serially; all report commands except
      the intentional NOT_READY audit exit 0.
- [x] DoD audit has current-head `PASS_LOCAL_ONLY` and `sourceTreeMatches: true`,
      remains `NOT_READY`, Public Beta false, 105 missing history rows and 11
      external blockers; matrix is consistent.
- [x] `npm test -- --testTimeout=15000` and `npm run typecheck` exit 0 after
      evidence refresh.

## Done-evidence (world-state proof, named BEFORE building)

The committed evidence set and regression suite agree on the same current HEAD:
release provenance is coherent and local-only, while all pending human and
external gates remain explicitly visible.

## Evidence (paste the actual proof here when done)

Verified 2026-08-14 after the C-135 commit:

- `npm run release:evidence:current` exited 0 for commit
  `bc8a5042166602763fab71ab09649852a6be6148`; source-tree SHA-256 is
  `4d8fec5ae3db384cde8f4f1a7f7b685b299fa11978fe8cbfc84212c725493a84`.
- Serial readiness/operations/DoD/matrix/transparency commands completed with
  `PASS_LOCAL_ONLY`, `PASS`, intentional `NOT_READY`, `consistent: true`, and
  `releaseStatus: NOT_READY`; Public Beta remains false, 105 history rows are
  missing and 11 external blockers remain.
- DoD current-head check reports `PASS_LOCAL_ONLY` and `sourceTreeMatches: true`.
- `npm test -- --testTimeout=15000`: 69 files and 252 tests passed; typecheck
  exited 0.
