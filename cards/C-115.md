# C-115 — Audit published content correction history

status: done
deps: C-114
implements: infra

## Scope

Add a read-only audit script for every published content node. It must verify
published translations, reviewer/reviewed_at/asOf, verified claims with evidence,
verified sources, and the presence of an editorial audit-log trail. Report counts
and IDs without claiming historian-council approval; any missing correction/audit
history must remain visible as a blocker for this internal content requirement.

## Allowed files

scripts/published-content-audit.mjs
tests/operations/published-content-audit.test.ts
artifacts/curriculum-completeness/published-content-audit.json
artifacts/curriculum-completeness/published-content-audit.md
cards/C-115.md

## Verify (run these before calling the card done)

- [x] `node scripts/published-content-audit.mjs` reports
      `BLOCKED_INTERNAL_AUDIT` with all 105 published rows missing an editorial
      audit-history trail; this expected diagnostic result is retained as evidence.
- [x] `npm test -- --run tests/operations/published-content-audit.test.ts` passes,
      including a fixture detecting missing audit history.
- [x] `npm run typecheck` passes.

## Done-evidence (world-state proof, named BEFORE building)

The audit artifact gives exact published-content totals, missing metadata/history
IDs, and a truthful `PASS_INTERNAL_AUDIT`/`BLOCKED_INTERNAL_AUDIT` status separate
from external Council approval.

## Evidence (paste the actual proof here when done)

The audit found a real internal gap: 105/105 published content rows lack an
`audit_logs` editorial-history entry, so the report is `BLOCKED_INTERNAL_AUDIT`
with `completeContent: 0`; no audit rows were fabricated. Focused tests passed (2
tests), and typecheck passed. Council approval remains `NOT_EVALUATED`.
