# C-048 — N-011 child privacy controls và DPIA

status: done
deps: [C-047]
implements: [privacy-safety]

## Scope

Đóng gói DPIA và policy vận hành cho người chưa thành niên: no-account pilot, data
minimization, no names/school/location, retention/delete, incident response, parental/
teacher escalation và AI disable-by-default cho trẻ. Checker xác nhận các control bắt
buộc có mặt; không thu thập dữ liệu thật.

## Allowed files

- `docs/privacy/**`
- `scripts/privacy-check.mjs`
- `package.json`
- `artifacts/privacy/**`
- `cards/C-048.md`

## Verify (run these before calling the card done)

- [x] `npm run privacy:check` exits 0 with DPIA, minimization, retention/delete and
      incident controls; red fixture exits 1.
- [x] `npm run lint`, `npm run typecheck`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Versioned DPIA/privacy controls and machine-readable PASS/FAIL reports; public AI remains
disabled until eval and safety gates pass.

## Evidence (paste the actual proof here when done)

- Privacy checker passed 8 required controls and explicitly keeps public AI disabled pending review: `artifacts/privacy/report.json`.
- Incomplete policy fixture exited 1 with all controls missing; no database access/mutation: `artifacts/privacy/red-report.json`.
- DPIA: `docs/privacy/dpia.md`; consolidated proof: `artifacts/privacy/privacy-proof.json`.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit 0.
