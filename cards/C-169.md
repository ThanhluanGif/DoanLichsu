# C-169 — Add hash-bound internal evidence to external handoff

status: done
deps: C-168
implements: infra

## Scope

Extend the external-evidence handoff packet with a read-only `internalEvidence`
section that records the current release commit, source-tree hash, release
artifact hash, GHCR runtime image/digest, runtime artifact hash and local
security-pack status. Generate these values from checked-in artifacts rather
than hard-coding them. Preserve all 11 external rows as `PENDING`, keep
`publicBetaAllowed=false`, and never treat local evidence as Council,
production, rights, DPIA, pilot or independent-security approval.

## Allowed files

KE_HOACH_12_THANG_CONG_TRI_THUC_LICH_SU_VIET_NAM_AI.md
scripts/external-evidence-handoff.mjs
tests/operations/external-evidence-handoff.test.ts
tests/operations/global-master-consistency-check.test.ts
artifacts/operations/external-evidence-handoff.json
artifacts/operations/external-evidence-handoff.md
artifacts/production-readiness/report.json
artifacts/release/current-head-evidence.json
artifacts/release/current-head-evidence.md
artifacts/release/dod-audit.json
artifacts/release/dod-matrix-consistency.json
artifacts/release/year-one-dod-matrix.json
artifacts/transparency/dashboard.json
cards/C-169.md

## Verify (run these before calling the card done)

- [x] While C-169 is in flight, the consistency fixture accepts one in-flight
      card and the handoff focused test passes.
- [x] `npm run operations:evidence:handoff` (or the underlying script) exits 0,
      reports 11 pending rows and includes non-null hash-bound internal release,
      runtime and security metadata without changing external row statuses.
- [x] `RELEASE_EVIDENCE_RUN=1 npm run release:evidence:current` exits 0 after
      the source-tree change with quality `0/0/0/0` and local
      health/OpenAPI/search `200/200/200`.
- [x] Full `npm test -- --testTimeout=15000` passes after finalization and
      Flow check C-169 passes.

## Done-evidence (world-state proof, named BEFORE building)

The handoff packet gives an external reviewer a reproducible starting point for
the exact release/runtime/security evidence while clearly retaining every
external approval gate as pending.

## Evidence (paste the actual proof here when done)

- `npm run operations:evidence:handoff` exited 0 with 11 pending rows, no
  fabricated evidence and hash-bound release/runtime/security metadata. The
  handoff focused tests passed (`2 files`, `3 tests`).
- `RELEASE_EVIDENCE_RUN=1 npm run release:evidence:current` exited 0 with
  tested commit `58d4be5880025774144adc17bba045e7e6f8a594`, source-tree hash
  `62169c86fd77b70555364d90714496e561313418c921e1fcef9e23615249845a`,
  quality `[0,0,0,0]`, local health/OpenAPI/search `200/200/200`, security
  headers `true`, search p95 `35.44ms`, backup/restore checksums `true/true`,
  and no database mutation.
- Full `npm test -- --testTimeout=15000` exited 0: **83 test files, 288
  tests passed** while C-169 was in flight.
