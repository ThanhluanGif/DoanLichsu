# C-055 — N-018 public transparency dashboard

status: done
deps: [C-038, C-041, C-046, C-054]
implements: [transparency]

## Scope

Tạo machine-readable/public-safe transparency report tổng hợp curriculum coverage, source
verification, rights served/link-only, AI eval target/actual, privacy state, correction SLA
và release blockers. Report dùng artifact hiện có, không tự nâng trạng thái; phân biệt rõ
verified nội bộ với council sign-off và fixed production URL.

## Allowed files

- `scripts/transparency-dashboard.mjs`
- `package.json`
- `artifacts/transparency/**`
- `cards/C-055.md`

## Verify (run these before calling the card done)

- [x] Dashboard report emits all KPI groups and lists blockers without secret/PII.
- [x] Red fixture with missing source/AI eval is marked NOT_READY, not green.
- [x] `npm run lint`, `npm run typecheck`, `npm run build` exit 0.

## Done-evidence (world-state proof, named BEFORE building)

Public-safe transparency artifact that can be linked from the existing source/transparency
route, with honest release state and blocker list.

## Evidence (paste the actual proof here when done)

- Transparency dashboard reports `NOT_READY`, AI 12/500 with public beta disabled, rights 1 served/1 LINK_ONLY, privacy PASS, correction SLA 72h, operations FAIL and 5 blockers; no secret/PII: `artifacts/transparency/dashboard.json`.
- Consolidated proof: `artifacts/transparency/transparency-proof.json`; disclosure states it is not historian council endorsement.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit 0.
